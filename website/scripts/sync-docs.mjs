// Placeholder — replaced by the real content-sync script in the next task.
// Exists so `pnpm build` / `pnpm dev` run end-to-end at this layer of the build.
console.log('sync-docs: placeholder (no content synced yet)');
