---
phase: 117-release-v1.29
id: 117-01
tier: standard
status: PENDING
---

# 117-01 — Release v1.29.0 — non-TTY gate auto-bypass

## Objective

Cut v1.29.0: add + consume a changeset for phase 116 (non-TTY auto-bypass for the
approve + interactive-verdict gates), bump all four published packages
`1.28.0 → 1.29.0` in lockstep, and lead the CLAUDE.md + MILESTONES.md narrative
with `1.29.0`. npm publish + the `v1.29.0` tag are the operator-triggered manual
`Release` workflow (not part of this phase).

## Acceptance Criteria

### AC-1: lockstep version bump + changeset consumed
Given the release
When the packages are inspected
Then `cadence-core`, `cadence-types`, `cadence-host-claude-code`, and
`cadence-host-codex` are all at `1.29.0`, the changeset is consumed, and each
package CHANGELOG carries the v1.29.0 entry.

### AC-2: CLAUDE.md leads with 1.29.0 (doc-sync gate passes)
Given `CLAUDE.md`
When read after this phase
Then its latest-version narrative leads with `1.29.0` and summarizes phase 116,
so the doc-sync gate (canonical version vs CLAUDE.md) passes.

### AC-3: MILESTONES records v1.29.0
Given `.cadence/MILESTONES.md`
When read after this phase
Then it has a `v1.29.0` section noting the non-TTY-gate-bypass milestone (phase
116, rec-20260617-005).

### AC-4: full suite green
Given the version bump + narrative edits
When `pnpm turbo run lint typecheck test build` runs
Then all four legs pass.

## Tasks

### T1: changeset + lockstep version bump
- files: `.changeset/*.md`, `packages/*/package.json`, `packages/*/CHANGELOG.md`
- action: add a minor changeset covering the four published packages, then run
  `pnpm changeset version` to bump `1.28.0 → 1.29.0` and write CHANGELOGs.
- verify: all four published packages read `1.29.0`
- done: AC-1

### T2: narrative — CLAUDE.md + MILESTONES.md
- files: `CLAUDE.md`, `.cadence/MILESTONES.md`
- action: lead CLAUDE.md's latest-version paragraph with 1.29.0 + a phase-116
  summary; add a v1.29.0 MILESTONES section.
- verify: doc-sync gate passes (CLAUDE.md mentions 1.29.0); manual read
- done: AC-2, AC-3

### T3: full gate
- files: (none — verification only)
- action: run the four-command pipeline.
- verify: `pnpm turbo run lint typecheck test build` green
- done: AC-4

## Boundaries

- DO NOT publish to npm or push tags — that is the operator-triggered manual
  `Release` workflow.
- DO NOT change feature code — this is a release/narrative phase only.
- Keep the version bump lockstep across all four published packages; `testkit`
  stays private/unpublished.
