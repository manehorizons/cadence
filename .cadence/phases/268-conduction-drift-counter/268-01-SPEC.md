---
phase: 268-conduction-drift-counter
id: 268-01
status: APPROVED
---

# 268-01 — Conduction drift counter

## Objective

cadence doctor's conduction-reachability (phase 251) answers a point-in-time capability question ('can this repo conduct a real finding') but nothing answers the trend question ('has it, lately'). That gap is exactly how 263 settles accumulated under mock with zero escalation in the v1.54 audit. Derive a read-only streak counter from the SUMMARY corpus (consecutive settles with no non-mock verifier identity in provenance), surface it in cadence doctor/status, and escalate severity by streak length.

## Acceptance Criteria

### AC-1: Read-only streak counter derived from the SUMMARY corpus
Given the settled-phase SUMMARY.json corpus under `.cadence/phases/**`
When a read-only, best-effort utility walks it in chronological order
Then it returns the count of consecutive most-recent settles with no non-mock provider identity in `assurance.verifierRollup`, degrading to indeterminate (never throwing) on missing or malformed data, consistent with the phase-208 `phase-freshness` best-effort precedent

### AC-2: DoctorSeverity gains an `indeterminate` rung, all existing consumers unaffected
Given `packages/core/src/doctor/model.ts`'s `DoctorSeverity` type (`'ok' | 'warning' | 'error'`) per `dec-20260810-005`
When it is extended to `'ok' | 'warning' | 'error' | 'indeterminate'`
Then `DoctorReport.ok`'s roll-up boolean, the `fail()` helper family, `packages/core/src/doctor/fix.ts`'s fix-planner classification, and `packages/core/src/cli/commands/doctor.ts`'s CLI/JSON rendering + exit code all handle the new rung explicitly (not via a fallthrough default), and every pre-existing doctor check's rendered output and exit-code behavior is byte-for-byte unchanged (regression-tested)

### AC-3: Streak surfaced in doctor and status
Given the streak counter from AC-1 and the `indeterminate` rung from AC-2
When `cadence doctor` or `cadence status` runs
Then the current streak value and a human-readable explanation appear in their output, non-blocking — an indeterminate counter renders as `indeterminate` severity with explanatory text, never silently as `ok`

### AC-4: Severity escalates by streak length against a provisional threshold
Given a streak length and a threshold of 3 (borrowed from `dec-20260801-003`'s `config.convergence.maxAttempts` default per `dec-20260810-004`, explicitly marked provisional in both code and rendered output)
When the streak reaches or exceeds the threshold
Then `cadence doctor`'s severity for this check escalates one rung, and this never blocks or refuses a settle (O.5) — it is a warning only, with no new refusal path added anywhere in the settle pipeline

### AC-5: Counter is correct against a fixture corpus
Given a fixture SUMMARY.json corpus with a known, hand-computed streak (including a case with a non-mock entry resetting it, and a case with missing/malformed `verifierRollup` data)
When AC-1's counter runs against the fixture
Then its returned streak value matches the hand-computed expectation exactly, including the indeterminate-degrade case

## Constraints

- No new settle refusal path (v1.56 §6 non-goal; O.5).
- No change to `deep-verify`/`per-task-verify` mock semantics, `isSelfInvocation`, `SELF_INVOCATION_ENV_VAR`, or the `DELTAS` matrix (v1.56 §6 non-goals).
- No historical `SUMMARY.json` is reinterpreted; the counter only reads already-persisted `assurance.verifierRollup`.
- The escalation threshold is rendered/documented as provisional, not final, per `dec-20260810-004`.
- O.6: the DRAFT/PROGRESS for this phase must record the counter's value against the *live* repo corpus and the exact command used, as a measured finding — first data point for `dec-20260810-004`'s deferred O.3 follow-up, not a claim that the Bar's "materially lower after v1.55" comparison is satisfied. This is a task-time record, not a separate testable AC.
- Per `dec-20260810-005`: the `DoctorSeverity` schema change must not alter any pre-existing doctor check's rendered output, JSON shape, or exit code — the new rung is additive, exercised only by this phase's own new check.

## Open Questions

- [operator] O.3's full measured before/after threshold is deferred per `dec-20260810-004` (operator choice, 2026-08-10) — this phase ships the counter and a provisional threshold (3, borrowed from `dec-20260801-003`), not the final validated one. Revisit once real-provider settles accumulate under the now-standard profile.
- [operator] Wiring this counter as the mechanical instrument for `dec-20260801-003`'s own three-settle revisit trigger is OUT of this phase's scope, not merely deferred like O.3 — the two are different signals (this counter is a run-length of *no* non-mock verifier identity at all; dec-20260801-003's trigger requires ≥3 settles that *each persisted* a non-mock code-review finding). A non-mock settle with zero findings resets this counter but does nothing for dec-20260801-003's trigger. The handoff (§8) places that wiring *after* Phase O lands, as its own follow-up — not conflate the two in implementation.
- [operator] `DoctorSeverity`'s `indeterminate` rung: resolved per `dec-20260810-005` (operator choice, 2026-08-10) — add the rung, don't fold indeterminate into `ok` or `warning`. This is a published-contract change (v1.55 J.2, previously unresolved); AC-2 scopes its blast radius to the 4 known consumer sites found by direct grep (`model.ts`, `fix.ts`, `cli/commands/doctor.ts`, plus `DoctorReport.ok`'s roll-up).
- [operator] `cadence next` surfacing (handoff O.2's "consider") was scoped out of this phase's ACs — doctor/status only. Not a faithfulness gap, a deliberate narrowing.
- [operator] Heads-up, not a SPEC defect: given the live corpus (3/280 non-mock, 0 postdating 267's flip), this check will report a streak in the hundreds on day one — every doctor run from this phase's merge onward opens with a warning-severity (once AC-4 lands) or unescalated (until the threshold trips) finding. Expected, not a bug, but worth knowing before merge.
- **As-built correction (whole-branch review, 2026-08-10):** the prediction above was falsified by an event this SPEC couldn't see coming — phase 267 (T6, the profile flip itself) landed as the most-recent settle in the corpus and carries a non-mock `verifierRollup` entry. The measured T4/O.6 finding is **streak = 0, determinate = true** on day one, not "hundreds." The check behaves correctly and honestly either way (it reports whatever the corpus actually says); this note exists so a future reader doesn't mistake a healthy streak-0 result for a bug against this SPEC's stale prediction.
