---
phase: 134-progress-json
id: 134-01
tier: standard
status: PENDING
---

# 134-01 — cadence progress --json

## Objective

Add `--json` to `cadence progress` so agent/MCP callers get the same
structured `{ command, reason }` payload the service already returns,
instead of having to regex the two rendered text lines — closing
rec-20260701-004 / audit finding F4.

## Acceptance Criteria

### AC-1: --json emits the structured payload instead of rendered text
Given any loop state
When `cadence progress --json` runs
Then stdout is a single JSON object `{ command, reason }` (parseable via
`JSON.parse`) and the rendered `Next:`/`Reason:` text lines are not printed

### AC-2: default (no flag) behavior is unchanged
Given any loop state
When `cadence progress` runs without `--json`
Then stdout is unchanged — the existing `Next: ...` / `Reason: ...` two-line
rendering

### AC-3: --json composes with every loop position
Given IDLE, DRAFT, BUILD, and SETTLE-adjacent states
When `cadence progress --json` runs in each
Then the JSON payload's `command` field matches what the non-JSON `Next:`
line would have said for that same state

## Tasks

### T1: add --json to progress, mirroring recommend --json
- files: `packages/core/src/cli/commands/progress.ts`,
  `packages/core/src/services/progress.ts`,
  `packages/core/tests/cli/progress.test.ts`
- action: `progressService` takes an `args: { json?: boolean }` param; when
  `json` is true it writes `JSON.stringify({ command, reason }) + '\n'` to
  stdout instead of the two rendered lines (same pattern as
  `recommendService`'s `--json` branch). The CLI command adds a
  `.option('--json', ...)` and passes it through. `CommandResult.data` is
  unchanged either way.
- verify: `pnpm --filter @manehorizons/cadence-core test -- cli/progress`
- done: AC-1, AC-2, AC-3

## Boundaries

- DO NOT change `progressService`'s existing non-JSON text output.
- DO NOT touch `draft`/`approve`/`build task`/`settle run` — progress only,
  per rec-004's own staged scope.
- DO NOT add a `--format` alias in this phase — separate follow-on.
