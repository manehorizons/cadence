---
phase: 165-host-cli-headless-verifier
id: 165-01
tier: standard
status: PENDING
---

# 165-01 — Host-CLI headless verifier provider

## Objective

Add a 4th verifier provider (`host-cli`) that authenticates by shelling out to the user's already-installed, already-authenticated host CLI (`claude` / `codex`) in headless mode, so independent AC verification works without a separately configured `ANTHROPIC_API_KEY`.

## Acceptance Criteria

### AC-1: host-cli provider produces schema-valid verdicts
Given `verification.provider` (or a per-family provider slice) is set to `host-cli` and the configured host CLI binary is installed and authenticated
When a verification gate runs (e.g. deep-verify)
Then the provider spawns the host CLI in headless/non-interactive mode with the per-AC prompt, parses its output into a schema-valid verdict via the shared repair-retry harness (AC-4), and returns the same verdict shape the `local`/`anthropic` providers return today.

> **As built (T7):** `deep-verify` (config `verifier`, `verify/factory.ts`) and
> `per-task-verify` (config `perTaskVerifier`, `verify/per-task-factory.ts`)
> are two distinct gates in this repo's canonical vocabulary — the "e.g.
> deep-verify" example above was imprecise. Only `per-task-verify` got a real
> `hostCli` builder wired in this phase; `deep-verify` and the other 4
> families (code-review, spec-review, plan-review, security-audit) still fall
> back to mock via the T3 Proxy wrapper. AC-1 is satisfied for `per-task-verify`
> specifically, not for the literal gate the example named. See
> `docs/providers.md`'s "Current scope: per-task-verify only" section.

### AC-2: Loud, non-blocking fallback when the host CLI is unavailable
Given `host-cli` is selected but the configured binary is not found on PATH or is not authenticated
When the verifier factory resolves the provider
Then it falls back to `mock` with the same stderr warning banner pattern used by `anthropic`/`local` today (never a silent fallback, never a hang waiting on interactive auth).

### AC-3: No new HostAdapter surface
Given the existing `HostAdapter`/`HostCapabilities` contract (`packages/types/src/host.ts`)
When the `host-cli` provider is implemented
Then it adds zero new fields/methods to `HostAdapter`/`HostCapabilities` — the provider lives entirely in `packages/core/src/verify/`, spawning the CLI binary directly the same way `local-client.ts` calls an arbitrary HTTP endpoint.

### AC-4: Repair-retry harness is shared, not duplicated
Given `local-client.ts`'s existing JSON-extraction + schema-repair-retry logic (`extractJson`, `safeJson`, the retry loop)
When the `host-cli` provider needs to coerce host-CLI stdout into a schema-valid verdict
Then that logic is factored into a shared, transport-agnostic module used by both `local` and `host-cli` (the only new transport-specific code is the subprocess spawn/capture, mirroring `callOnce`'s fetch call).

## Tasks

### T1: Extract the shared JSON-extraction + schema-repair-retry harness
- files: `packages/core/src/verify/local-client.ts`, `packages/core/src/verify/json-repair.ts`, `packages/core/tests/verify/local-client.test.ts`
- action: factor `extractJson`/`safeJson`/the repair-retry loop out of `local-client.ts` into a new transport-agnostic module (`json-repair.ts`) parameterized by a `callOnce`-shaped transport function; `local-client.ts`'s existing fetch-based `callOnce` becomes the transport it passes in
- verify: existing `local-client.test.ts` suite still passes unchanged against the refactored code
- done: AC-4

### T2: Implement the host-cli spawn transport and provider wiring
- files: `packages/core/src/verify/host-cli-client.ts`, `packages/core/src/verify/verifier-factory.ts`, `packages/core/tests/verify/host-cli-client.test.ts`
- action: implement a subprocess-spawn transport (non-interactive/headless flags, captured stdout) that plugs into the T1 harness; add `'host-cli'` to the `VerifierProvider` union and wire provider resolution in `verifier-factory.ts`, discovering the configured binary via PATH lookup (default `claude`, config-overridable) — no `HostAdapter`/`HostCapabilities` changes. Confirming the actual headless-JSON output flags for `claude`/`codex` (SPEC open question) is part of this task's implementation work.
- depends: T1
- verify: unit tests exercise `host-cli-client.ts` against a stubbed spawn — no real `claude`/`codex` binary is invoked by the suite
- done: AC-1, AC-3

### T3: Loud fallback when the binary is missing or unauthenticated
- files: `packages/core/src/verify/verifier-factory.ts`, `packages/core/tests/verify/verifier-factory.test.ts`
- action: when `host-cli` is selected but the binary can't be found or the spawned process signals an auth failure, fall back to `mock` with the existing `MOCK_FALLBACK_BANNER`-style stderr warning, mirroring the `anthropic`/`local` fallback branches; must not block waiting on interactive auth
- depends: T2
- verify: test asserts fallback to mock + stderr warning when the binary is absent (simulated), and completes without hanging
- done: AC-2

### T5: Accept 'host-cli' in config schema and CLI allowlists
- files: `packages/types/src/config.ts`, `packages/types/tests/config.test.ts`, `packages/core/src/cli/commands/activate.ts`, `packages/core/tests/cli/activate.test.ts`, `packages/core/src/cli/commands/settle.ts`, `packages/core/tests/cli/settle-auto.test.ts`
- action: Add 'host-cli' to the 6 provider z.enum([...]) fields in config.ts so a config file can declare it; add 'host-cli' to activate.ts's PROVIDERS allowlist; extend settle.ts's parseVerifier to accept it
- depends: T2
- verify: existing config/activate/settle tests pass; a new test confirms a config with provider: 'host-cli' parses successfully and 'cadence activate --provider host-cli' is accepted
- done: AC-1

### T6: Report host-cli readiness accurately in doctor/activate
- files: `packages/core/src/activate/assess.ts`, `packages/core/tests/activate/assess.test.ts`
- action: Add a host-cli branch to assess.ts's credsPresent so readiness checks look for CADENCE_HOST_CLI_BIN discovery instead of silently falling through to the local provider's baseURL/model check
- depends: T2
- verify: existing assess tests pass; a new test confirms host-cli readiness reporting is accurate (cadence doctor/activate don't misreport it as local)
- done: AC-1

### T7: Wire the per-task (deep-verify) family's hostCli builder
- files: `packages/core/src/verify/per-task.ts`, `packages/core/src/verify/per-task-factory.ts`, `packages/core/tests/verify/per-task.test.ts`, `packages/core/tests/gates/per-task-verify.test.ts`
- action: Add a HostCliPerTaskVerifier class in per-task.ts implementing PerTaskVerifier via hostCliJSON (T2's transport); wire hostCli: (o) => new HostCliPerTaskVerifier(o) into selectPerTaskVerifier's spec in per-task-factory.ts — the first real end-to-end path for AC-1, since deep-verify is AC-1's named example
- depends: T2, T5
- verify: existing per-task tests pass; a new test confirms selecting provider: 'host-cli' for perTaskVerifier resolves to HostCliPerTaskVerifier (not mock) and produces a schema-valid verdict from a stubbed spawn
- done: AC-1

### T4: Tests and docs covering the full provider
- files: `packages/core/tests/verify/*`, `docs/providers.md`
- action: cover the primary `host-cli` path and the fallback edge case per AC-1..AC-4; document the new provider in `docs/providers.md`, noting the binary-discovery strategy and that per-gate-run batching and quota-transparency messaging are explicitly out of scope for this slice
- depends: T1, T2, T3, T5, T6, T7
- verify: `pnpm --filter @manehorizons/cadence-core test` passes; `docs/providers.md` accurately reflects the shipped behavior
- done: AC-1, AC-2, AC-3, AC-4

## Boundaries

- DO NOT add any new field or method to `HostAdapter`/`HostCapabilities` (`packages/types/src/host.ts`) — this provider is core-only, per AC-3.
- DO NOT require a real `claude`/`codex` binary in the test suite — stub the subprocess transport; tests stay offline/deterministic (`mock` only convention).
- DO NOT implement per-gate-run batching (rec-20260710-004) or quota-transparency messaging (rec-20260710-006) in this slice — both are explicitly deferred per the SPEC's open questions.
- DO NOT implement the MCP-driven inversion alternative (rec-20260710-003) — it is deferred; this slice is the direct-subprocess approach only.
