---
phase: 152-release-v1-40-0
id: 152-01
tier: standard
status: PENDING
---

# 152-01 — Release v1.40.0: structured draft editing + housekeeping

## Objective

Document the three new structured-draft-editing subcommands (phase 151) in commands.md, then cut the v1.40.0 lockstep release bundling phases 139-151 — the accumulated unreleased work on main.

## Acceptance Criteria

### AC-1: commands.md documents the three phase-151 draft subcommands
Given docs/reference/commands.md's ### draft section, which documents new/check/approve but not set-objective/add-ac/add-task
When three new subsections are added following the existing draft-subcommand style (usage block, arguments/options tables, behavior prose)
Then each subcommand's flags (set-objective --text; add-ac --given/--when/--then/--name; add-task --files/--action/--verify/--done) and PENDING-only refusal behavior are documented

### AC-2: lockstep v1.40.0 release across all four published packages
Given the four published packages (cadence-core, cadence-types, cadence-host-claude-code, cadence-host-codex) all currently at 1.39.0
When a changeset is added (all four minor) and pnpm changeset version is run
Then all four package.json files read 1.40.0 and each package's CHANGELOG.md gets a new 1.40.0 entry

### AC-3: doc-sync gate passes and full monorepo verify is green
Given the repo's doc-sync pre-commit hook, which fails a commit changing packages/core/package.json's version unless CLAUDE.md mentions the new version
When CLAUDE.md's release narrative is updated to describe v1.40.0 (mentioning the literal string 1.40.0) alongside the version bump commit
Then the doc-sync hook passes, and pnpm build && pnpm typecheck && pnpm lint && pnpm test are all green

## Tasks

### T1: Document the three phase-151 draft subcommands
- files: `docs/reference/commands.md`
- action: Add three new subsections (#### draft set-objective, #### draft add-ac, #### draft add-task) after the existing #### draft approve subsection, matching its usage-block/arguments-table/options-table/behavior-prose style; read packages/core/src/cli/commands/draft.ts for the exact flag names and packages/core/src/parse/draft-mutate.ts for the PENDING-only refusal behavior
- verify: manual read-through against AC-1's wording; grep docs/reference/commands.md for each new flag name
- done: AC-1

### T2: Changeset + version bump + CLAUDE.md narrative
- files: `.changeset/structured-draft-editing-release.md`, `packages/core/package.json`, `packages/types/package.json`, `packages/host-claude-code/package.json`, `packages/host-codex/package.json`, `packages/*/CHANGELOG.md`, `CLAUDE.md`
- action: Add a changeset file (frontmatter: all four packages minor; body summarizing the bundled work since v1.39.0 -- phase 151 structured draft editing plus the housekeeping/parser-fix work in PR #139, modeled on prior release changesets e.g. git show e8101b8:.changeset/doctor-fix.md); run pnpm changeset version to consume it; append a new version-narrative paragraph to CLAUDE.md's package-history section describing v1.40.0
- verify: git diff --stat shows all four package.json at 1.40.0; grep CLAUDE.md for the literal string 1.40.0
- done: AC-2, AC-3

### T3: Full monorepo verify
- files: (none -- verification only)
- action: Run pnpm build && pnpm typecheck && pnpm lint && pnpm test from the repo root
- verify: all four commands exit 0
- done: AC-3

## Boundaries

- DO NOT touch `packages/core/src/parse/draft-mutate.ts`, `packages/core/src/services/draft-mutate.ts`, or `packages/core/src/cli/commands/draft.ts` -- phase 151 already shipped and reviewed that code; this phase is docs + release only.
- DO NOT hand-edit any package's `CHANGELOG.md` beyond what `pnpm changeset version` generates -- let the tool own that format.
- DO NOT bump versions to anything other than `1.40.0` -- confirm the pre-bump version is `1.39.0` in all four packages before running `changeset version`.
