---
phase: 254-security-advisory-remediation
id: 254-01
tier: standard
status: PENDING
---

# 254-01 — Security advisory remediation

## Objective

Retire the dead brace-expansion exception now that phase 253 patched it, re-justify the three security exceptions expiring 2026-08-13 with fresh reachability analysis (deferring the vitest-major upgrade as a named blocker rather than reviving stale PR #235), and capture the ip-address moderate-advisory evidence, closing the security-advisory-remediation half of the v1.55 integrity release ahead of the 2026-08-12 deadline.

## Acceptance Criteria

### AC-1: Dead brace-expansion exception retired
Given docs/security/audit-exceptions.md still carries the GHSA-mh99-v99m-4gvg (brace-expansion) exception row, now dead weight since phase 253 refreshed the override target past its patched floor
When the row is removed from the exceptions table and its now-orphaned narrative-content assertions in packages/core/tests/docs/security-ci.test.ts are removed with it
Then node scripts/check-audit-exceptions.mjs still exits 0 (the advisory is no longer flagged) and no test references the deleted row

### AC-2: Expiring exceptions re-justified, vitest-major deferred as named blocker
Given the three exceptions expiring 2026-08-13 (vitest GHSA-5xrq-8626-4rwp, vite GHSA-fx2h-pf6j-xcff, postcss GHSA-r28c-9q8g-f849) all resolve through one chain (vitest >=3.2.6), and that upgrade is out of scope this phase (Path B, decided 2026-08-04: PR #235 is stale and was closed as premised on the disproven pnpm-overrides-broken narrative)
When each row gets an independent, freshly-derived reachability re-verification (not a copy-paste renewal) with its expiry extended past this phase's ship date, and the vitest v4 upgrade is recorded as a named, deferred blocker rather than silently dropped
Then node scripts/check-audit-exceptions.mjs still exits 0 for all rows, no expiring row's justification text is byte-identical to its pre-254 version, and the deferred vitest-major blocker is written down (DRAFT and/or a filed recommendation)

## Tasks

### T1: Retire the dead brace-expansion exception row and its orphaned test assertions
- files: `docs/security/audit-exceptions.md`, `packages/core/tests/docs/security-ci.test.ts`
- action: Delete the GHSA-mh99-v99m-4gvg (brace-expansion) row from the exceptions table; delete its two now-orphaned assertions in the 253-01/AC-5 describe block (row content checks at ~lines 284-296) that assert text specific to that row; leave the generic row-shape test (~lines 211-219) untouched
- verify: node scripts/check-audit-exceptions.mjs exits 0; pnpm --filter @thomas-powers-jr/cadence-core test -- security-ci is green with zero references to GHSA-mh99-v99m-4gvg remaining in the test file
- done: AC-1

### T2: Capture ip-address moderate-advisory evidence (before/after)
- files: `.cadence/phases/254-security-advisory-remediation/254-01-T2-EVIDENCE.md`
- action: Capture before/after evidence for the two undocumented moderate ip-address advisories (GHSA-4xrf-jv44-h6hh, GHSA-22jq-vg5j-6vgg): before = 10.2.0 (both present), after = >=10.3.1 via phase 253's override (both cleared) -- source from phase 253's own 253-01-T3-EVIDENCE.md and remediation table, not a fresh audit of an old checkout. Evidence only -- no exceptions-table row (the parser only reads high/critical)
- verify: 254-01-T2-EVIDENCE.md exists and cites the phase-253 source artifact by path
- done: AC-2

### T3: Re-justify the three expiring exceptions; record the deferred vitest-major blocker
- files: `docs/security/audit-exceptions.md`
- action: Independently re-derive each of vitest/vite/postcss's unreachability justification against current repo usage (grep for --ui, vite preview/serve usage, CSS files under packages/*); extend each row's expiry past 2026-08-12; record the deferred vitest >=3.2.6 major-upgrade blocker, naming PR #235 / branch chore/security-vitest-and-transitive-bump as stale and not revivable (closed 2026-08-04 on the disproven overrides-broken premise)
- verify: node scripts/check-audit-exceptions.mjs exits 0; each of the 3 rows' expiry postdates 2026-08-12; diff against pre-254 audit-exceptions.md shows non-trivial justification-text changes for all 3 rows, not just expiry-date edits
- done: AC-2

## Boundaries

- Do NOT attempt to land the vitest 2→4 major upgrade in this phase (Path B decision, 2026-08-04) — record it as a deferred blocker only.
- Do NOT reopen, cherry-pick from, or otherwise revive PR #235 / branch `chore/security-vitest-and-transitive-bump` — closed 2026-08-04 as premised on the disproven pnpm-overrides-broken narrative.
- Do NOT add exceptions-table rows for the two moderate ip-address advisories — the parser only reads high/critical; a row would be dead weight.
- Do NOT re-investigate the exceptions-table placeholder-row parsing concern (Rec B / rec-20260804-002) — already measured and answered (unreachable, tested at `security-ci.test.ts:207`); cite the existing finding only.
- Do NOT hand-edit `.cadence/state.json` or `STATE.md`.
- Do NOT touch the two sibling dead worktrees (`kernel-arc-docs-review`, `phase249-refused-settle-post-gate`) or `.claude/worktrees/253-dependency-override-remediation` — unrelated, tracked separately.
