---
phase: 284-record-reconciliation
id: 284-01
tier: complex
status: PENDING
---

# 284-01 — Reconcile phase 282's AC-2/AC-4 record

## Objective

Reconcile phase 282's historical record: two host-cli deep-verify verdicts (AC-2, AC-4) came back pass:false and were overridden with --force, but 282's own As-built amendment blocks show independent reviewers had already found and corrected both underlying issues in ways the verifier's judgement doesn't reflect. Determine, from the artifacts, whether AC-2 and AC-4 were substantively delivered; amend the record or strengthen tests per that finding; and file the systemic amendment-vs-verifier gap this reveals (a legitimately amended AC has no path back to deep-verify) as a recommendation. Never rewrite 282-01-SUMMARY.json.

## Acceptance Criteria

### AC-1: Reconciliation decisions recorded, cited to artifacts
Given phase 282's four As-built amendment blocks (T1, T2, two under T4) and the host-cli deep-verify pass:false reasons for AC-2 and AC-4
When phase 284 records its reconciliation decisions for each
Then each of AC-2 and AC-4 has a decision entry naming the option taken (per HANDOFF-v1.62's D-V) and citing the specific amendment block or test case that settles it

### AC-2: AC-2's text formally superseded
Given AC-2's original heading text demands a pre-fix reproduction proven impossible by T2's independent reproduction (readdir is stable-but-non-canonical within one process, not run-to-run variant)
When 282-01-DRAFT.md's AC-2 heading is amended
Then no future reader is asked to reproduce that impossible defect shape; the amended text cites the T2 finding directly at the AC's own definition site, not only in the trailing footnote

### AC-3: Amendment-vs-verifier gap filed
Given a legitimately amended acceptance criterion has no mechanism to re-reach deep-verify, so honest in-flight correction manufactures a false verifier failure requiring --force, which (post-phase-283) now caps the assurance grade at mixed
When phase 284 files this systemic finding
Then recommendations.json contains a new recommendation citing evidence that points at 282's amendment blocks and the resulting --force/grade-cap chain

### AC-4: No historical SUMMARY.json touched; verify-all clean
Given the report-never-rewrite standing rule and this phase's own edits (DRAFT amendment, recommendation filing, new tests)
When cadence summary verify-all runs after this phase's changes
Then no historical SUMMARY.json's content hash changed, and the command reports 0 MISMATCH, 0 failed

### AC-5: 282-01/AC-4's deep-verify objection resolved on its 'runs summary verify-all' clause
Given host-cli's stated objection that the linked test 'only string-matches a report; it neither runs summary verify-all nor verifies phase-id enumeration in the settle summary, so the operational AC is untested'
When phase 284 investigates both halves of that objection independently
Then the phase-id-enumeration half is judged already satisfied by existing asserting tests (with reasoning recorded, since SUMMARY.json cannot be the enumeration vehicle under report-never-rewrite); the runs-summary-verify-all half is closed by a genuine operationally-real test extending the existing corpus-wide sweep, not a further string-match

## Tasks

### T1: Investigate and record the D-V/D-W reconciliation decisions
- files: `packages/core/tests/docs/phase284-record-reconciliation.test.ts`
- action: Record `cadence decision add` entries: (1) D-V for AC-2 = amend AC-2's text, citing the T2 amendment block (deep-verify's required repro is proven impossible); (2) D-V for AC-4 = split verdict -- the runs-summary-verify-all clause gets D-V option 3 (strengthen the test), the phase-id-enumeration clause is judged already satisfied by the existing drift-report tests (citing the T4 amendment); (3) D-W = file-only (option a), not a mechanism spec this arc. Write `packages/core/tests/docs/phase284-record-reconciliation.test.ts` with a `284-01/AC-1` asserting block reading `decision list --json` output and confirming all three decisions are present with their citations.
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- phase284-record-reconciliation`
- done: AC-1
- stop: the new test file references `284-01/AC-1` inside an asserting `it()`/`test()` block, all three decisions are queryable via `cadence decision list`, and the targeted test run is green.

### T2: Formally supersede 282-01/AC-2's text
- files: `.cadence/phases/282-coverage-scanner-determinism/282-01-DRAFT.md`, `packages/core/tests/docs/phase284-record-reconciliation.test.ts`
- depends: T1
- action: Amend 282-01-DRAFT.md's AC-2 heading (lines ~25-28) with an inline formal-supersession notice: strike the "proven to produce a non-deep-equal map across repeated runs" / run-to-run-variance framing and replace with the corrected Given (stable-but-non-canonical order within one process, no cross-process guarantee), citing the existing T2 As-built amendment block directly rather than only relying on the trailing footnote a hurried reader could miss. Do not touch AC-1/AC-3/AC-4's headings or any Task block in 282-01-DRAFT.md. Add a `284-01/AC-2` asserting block confirming the amended text is present (assert on the corrected prose, not on the literal `282-01/AC-2` token, to avoid registering unintended coverage evidence for that historical AC).
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- phase284-record-reconciliation`
- done: AC-2
- stop: the new test file references `284-01/AC-2` inside an asserting block, 282-01-DRAFT.md's AC-2 heading no longer demands the disproven reproduction, and the targeted test run is green.

### T3: File the amendment-vs-verifier gap as a recommendation
- files: `.cadence/intelligence/recommendations.json`, `.cadence/intelligence/evidence.json`, `packages/core/tests/docs/phase284-record-reconciliation.test.ts`
- depends: T1
- action: Run the dedup preflight (`recommendation list --filter-regex` against amendment/deep-verify/as-built keywords) and paste its output before writing. File a new recommendation via `cadence recommendation add` (priority high, readiness needs-decision) describing the amendment-vs-verifier gap: a legitimately amended AC cannot re-reach deep-verify, so honest in-flight correction manufactures a --force requirement that (post-283) caps assurance at mixed -- note explicitly that left unaddressed this quietly disincentivizes amending a wrong AC. Add evidence citing 282's four amendment blocks and the `gateBypasses` entry in 282-01-SUMMARY.json. Add a `284-01/AC-3` asserting block confirming the rec is present in recommendations.json with the expected priority/readiness/evidence linkage.
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- phase284-record-reconciliation`
- done: AC-3
- stop: the new test file references `284-01/AC-3` inside an asserting block, the rec + evidence exist in the ledger, and the targeted test run is green.

### T4: Close 282-01/AC-4's operational-test gap and confirm the corpus-wide sweep
- files: `packages/core/tests/parse/summary-verify-sweep.test.ts`
- depends: T2, T3
- action: Extend summary-verify-sweep.test.ts (the existing corpus-wide, spawn-once-in-`beforeAll` sweep already reused by phases 267/274/280 for exactly this shape of claim) with two new `it()` blocks reusing the shared `beforeAll` result, adding no new subprocess spawn: (a) `284-01/AC-4` -- this phase's own edits (DRAFT amendment, recommendation filing, new test files) did not push the corpus-wide sweep's failed count above 0; (b) `282-01/AC-4` -- `cadence summary verify-all` operationally passes against the live corpus, closing the runs-summary-verify-all half of host-cli's deep-verify objection to 282-01/AC-4 with a genuinely live-executing assertion (not a string-match against a hand-written report), while leaving the point-in-time-pinned corpus numbers (293/3-drifted/12-unverifiable) in phase282-coverage-drift-report.test.ts untouched, since those are legitimately point-in-time per that file's own doc comment. Then run `cadence summary verify-all` live one more time and paste its output confirming 0 MISMATCH, 0 failed and that the checked count only grew (no historical SUMMARY.json content changed).
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- summary-verify-sweep`; `node packages/core/bin/cadence.cjs summary verify-all`
- done: AC-4, AC-5
- stop: `summary-verify-sweep.test.ts` references both `284-01/AC-4` and `282-01/AC-4` inside asserting blocks, the full targeted test run is green, and the live `cadence summary verify-all` paste shows 0 MISMATCH / 0 failed. Additionally, AC-4's "no historical SUMMARY.json's content hash changed" clause is discharged by comparing this run's MATCH/NO_HASH split against the pre-worktree baseline captured in the primary checkout (`295 checked: 58 MATCH, 237 NO_HASH, 0 failed`): the MATCH count must not have decreased and no phase id present in the baseline's MATCH set may appear as MISMATCH now -- a rising checked/MATCH count alone is not sufficient, since that's also consistent with a hash having been silently changed and rehashed.

## Boundaries

- Do NOT hand-edit or rewrite `282-01-SUMMARY.json` in any way (content, contentHash, coverage fields, deepVerify verdicts) -- report-never-rewrite is absolute; the historical `pass:false`/`--force`/`mixed`-grade record stands as honest history.
- Do NOT touch `coverage.ts`'s dedup or sort logic, or `gates/deep-verify.ts`'s `VerifyInput.tests` construction -- both are shipped and correct (282's own fix); this phase is documentation/ledger/test-coverage reconciliation only.
- Do NOT re-litigate `dec-20260816-*` or any other existing decision.
- Do NOT touch 282-01-DRAFT.md's AC-1, AC-3, or AC-4 headings, or any of its Task blocks -- only AC-2's heading is in scope, per T2.
- Do NOT write the literal `282-01/AC-2` token anywhere in a test file -- assert around the amended prose instead (phase282-coverage-drift-report.test.ts's own header comment documents why: any historical `<phase-id>/AC-<n>` token inside an asserting test block registers as live coverage evidence for that phase).
- Do NOT attempt to make the point-in-time-pinned corpus numbers in phase282-coverage-drift-report.test.ts (293 total / 3 drifted / 12 unverifiable) live-re-derived -- they are deliberately point-in-time per that file's own comment; only the runs-summary-verify-all clause is being strengthened.
- Do NOT design or spec the amendment-vs-verifier mechanism itself (a re-verify-on-amendment path, or an `amendedAt`/`supersedes` field) -- D-W is filed as a recommendation only (option a), not built, this arc.
- Do NOT force-settle. If any task here needs `--force`, stop and report -- this phase is *about* a `--force` that shouldn't have been necessary; needing one of its own would be the same failure mode recurring.
