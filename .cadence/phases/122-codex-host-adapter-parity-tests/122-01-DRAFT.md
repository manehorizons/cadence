---
phase: 122-codex-host-adapter-parity-tests
id: 122-01
tier: quick-fix
status: PENDING
---

# 122-01 — Codex host adapter parity tests

## Objective

Close the remaining Codex host-adapter test-parity gaps by proving local hook roundtrip behavior and locking Codex prompt rendering to the shared command guidance catalog.

## Acceptance Criteria

### AC-1: Codex local hook install roundtrips through the workspace shim
Given a freshly initialized test repo
When the Codex adapter installs hooks with `local: true`
Then the generated `.codex/hooks.json` command uses the local host-codex shim and local core CLI, and a SessionStart payload runs end-to-end

### AC-2: Codex prompts share the canonical command guidance catalog
Given Claude Code commands and MCP prompts already source prose from `COMMAND_GUIDANCE`
When Codex prompts are installed
Then every shared command guidance key, including `cadence-scout`, is rendered as a Codex prompt

### AC-3: Codex prompt rendering remains Codex-shaped
Given Codex prompts are prompt templates rather than Claude Code autorun commands
When shared guidance is rendered for Codex
Then prompts keep YAML frontmatter, no Claude `allowed-tools` / `!` autorun line, and include the shared scout dialogue body

## Tasks

### T1: Add Codex local roundtrip coverage
- files: `packages/host-codex/tests/local-roundtrip.test.ts`
- action: Add a Codex equivalent of the Claude local install roundtrip test
- verify: `pnpm --filter @manehorizons/cadence-host-codex test -- tests/local-roundtrip.test.ts`
- done: AC-1

### T2: Source Codex prompts from shared guidance
- files: `packages/host-codex/src/install-commands.ts`, `packages/host-codex/tests/install-commands-parity.test.ts`
- action: Replace duplicated Codex command prose with `COMMAND_GUIDANCE` / `SCOUT_DIALOGUE` and add catalog parity tests
- verify: `pnpm --filter @manehorizons/cadence-host-codex test -- tests/install-commands-parity.test.ts tests/install-commands.test.ts`
- done: AC-2, AC-3

## Boundaries

- Do not change Codex hook event semantics unless a parity test exposes a real mismatch.
- Do not change Claude Code adapter behavior in this phase.
- Keep Codex prompts Codex-shaped; shared prose must not introduce Claude-only frontmatter or autorun syntax.
