---
phase: 256-real-provider-certification-prep
id: 256-01
tier: complex
profile: strict
status: PENDING
---

# 256-01 — Real-provider certification prep

## Objective

Prepare a disposable seeded-defect fixture, a paste-ready operator runbook, and this phase's own strict/complex DRAFT override so Thomas can certify code-review and security-audit against a real, non-mock provider from a real terminal -- build through BUILD only, never invoke settle from this session.

## Acceptance Criteria

### AC-1: Seeded defect fixture trips both gates' mock detectors, with a verified corrected counterpart
Given a disposable fixture file under .cadence/phases/256-real-provider-certification-prep/fixture/ containing a hardcoded, synthetic (non-real-provider-shaped) bearer-token-style credential AND a console.log statement that logs it -- two independent defects chosen because MockSecurityAuditVerifier only pattern-matches credential shapes (AUTH_HEADER_RE/JWT_RE) while MockCodeReviewVerifier only pattern-matches `console.log(` -- a credential-only fixture would give zero local signal on whether code-review (the gate dec-20260801-003's revisit trigger actually counts) would flag anything at all. No in-file comment discloses either as a seeded test defect, since that context would leak into the reviewer's diff and could cause a real reviewer to discount severity, defeating the certification
When the fixture is inspected directly and its added lines are checked against MockSecurityAuditVerifier's real AUTH_HEADER_RE and MockCodeReviewVerifier's real console.log detection regex
Then both regexes match (verified by direct node invocation, not by running cadence settle), the credential value does not resemble any real provider's recognizable token prefix, a corrected counterpart (credential read from env/config instead of hardcoded, no console.log) is specified precisely enough to apply mechanically for the second settle attempt, and all seeded-defect/do-not-merge context lives only in the DRAFT/runbook -- never inside the fixture file itself. Note: this only proves the fixture trips both MOCK detectors -- code-review's real system prompt lists "secrets in code" and "left-behind debug statements" as HIGH-severity bullets, making a real flag likely but not certain; the runbook (AC-2) must state this uncertainty rather than assume it.

### AC-2: CONDUCTION-RUNBOOK.md gives Thomas exact, paste-ready, sequenced commands
Given docs/providers.md's documented code-review/security-audit procedures (lines 539-659), this repo's live config (codeReview.provider already host-cli; securityAudit.provider still mock), rec-20260801-011/dec-20260802-002's confirmed-shipped refused-summary preservation (a timestamp-slugged -SUMMARY-snapshot.json sibling, filename unpredictable ahead of time), and this machine's ~/.bashrc:167 CADENCE_HOST_CLI_BIN=codex (the claude-only self-invocation guard is not the operative constraint here -- codex is unguarded regardless of terminal)
When CONDUCTION-RUNBOOK.md is read
Then it states, in order: (0) a mock dry run of cadence settle run with securityAudit.provider still mock, confirming the seeded fixture refuses before spending a real run; (1) that this must be run by Thomas personally (not delegated back to an agent), because CADENCE's own thesis is that an agent's self-report isn't proof, not because of the claude-specific guard; (2) cadence config set securityAudit.provider host-cli (or the operator's chosen non-mock provider); (3) apply the fixture and run cadence settle run for real, expecting refusal on a CRITICAL security-audit finding; (4) capture the exact refused-attempt -SUMMARY-snapshot.json filename and confirm its gates[]/assurance.verifierRollup[] show a non-mock provider and model; (5) revert to the corrected counterpart and run cadence settle run again, expecting a clean pass; (6) delete the runbook, the fixture directory, AND packages/core/tests/docs/phase256-conduction-prep.test.ts before any settle commit lands, since all three are prep scratch, not shipped code -- the test file included, since it readFileSync's the other two and would break pnpm test permanently if left behind after they're deleted (a real per-task-verify call caught this). Also states the provenance gap (HostCliSecurityAuditVerifier/HostCliCodeReviewVerifier's provider name is hardcoded 'host-cli' regardless of underlying family, so the persisted SUMMARY won't distinguish claude from codex) and that code-review's coverage is likely but not certain.

## Tasks

### T1: Seed the disposable hardcoded-credential + console.log fixture + corrected counterpart
- files: `.cadence/phases/256-real-provider-certification-prep/fixture/seeded-defect.ts`, `.cadence/phases/256-real-provider-certification-prep/fixture/seeded-defect.fixed.ts`
- action: Write a minimal disposable fixture under .cadence/phases/256-real-provider-certification-prep/fixture/ that hardcodes a synthetic bearer-token-style credential value into source (a random-looking token, not resembling any real provider's recognizable prefix -- no AKIA/sk-/ghp_/eyJ shapes) AND logs it via console.log. No comment in the file discloses either as a seeded test defect -- that framing stays in the DRAFT/runbook only. Write seeded-defect.fixed.ts as the corrected counterpart (credential read from an env var / config, never hardcoded, no console.log). Verify the seeded lines trip both MockSecurityAuditVerifier's real AUTH_HEADER_RE and MockCodeReviewVerifier's real console.log detection regex via direct node -e regex checks (not by invoking cadence settle) and capture both commands + their true/false output as evidence in this task's notes.
- verify: Both fixture files exist and are non-empty; both captured node -e regex checks printed true against the seeded lines. (The 256-01/AC-1 doc-content assertion is added in T2, alongside AC-2's, and covers this AC too.)
- done: AC-1

### T2: Write the operator runbook + doc-content test
- depends: T1
- files: `.cadence/phases/256-real-provider-certification-prep/CONDUCTION-RUNBOOK.md`, `packages/core/tests/docs/phase256-conduction-prep.test.ts`
- action: Write CONDUCTION-RUNBOOK.md sourced from docs/providers.md's documented procedure (lines 539-659), covering the 7-step sequence from AC-2 (mock dry run through post-settle-commit cleanup, including deleting the doc-content test itself) with literal paste-ready commands. Add packages/core/tests/docs/phase256-conduction-prep.test.ts (readFileSync + toContain/toMatch pattern, matching packages/core/tests/docs/readme-shakedown.test.ts and phase 251's phase251-ledger.test.ts precedent) with two asserting it() blocks carrying the literal tokens 256-01/AC-1 and 256-01/AC-2, checking the fixture files' and runbook's required properties. This test file is itself scratch -- listed in the runbook's Step 6 cleanup alongside the fixture and runbook, since it reads both and would break permanently otherwise; it provides real test-coverage-gate evidence for AC-1/AC-2 during Steps 0/3/5 (all of which run before Step 6's deletion), so no coverage bypass is needed at any of those settle attempts.
- verify: pnpm --filter @thomas-powers-jr/cadence-core test -- tests/docs/phase256-conduction-prep.test.ts green; both phase-qualified tokens present in asserting it() blocks.
- done: AC-2

## Boundaries

- Do NOT modify `isSelfInvocation`, `SELF_INVOCATION_ENV_VAR`, the spawn guard in `packages/core/src/verify/host-cli-client.ts`, or the `DELTAS` matrix in `packages/core/src/gates/engine.ts` -- this phase's DRAFT-level profile override is the only sanctioned mechanism, per phase 251's own boundary and `dec-20260803-001`.
- Do NOT invoke `cadence settle run` in this session, in any form (including a mock-only dry run). Correction recorded mid-BUILD: the reason is NOT that the self-invocation guard would mock-fallback code-review -- this machine's `CADENCE_HOST_CLI_BIN=codex` (`~/.bashrc:167`) bypasses that guard entirely (confirmed empirically: T1's per-task-verify made a real, non-mock codex call). The real reason is CADENCE's own thesis that an agent's self-report of "done" isn't proof -- a codex-backed settle run narrated back to Thomas by the same agent that seeded the defect is still a self-report; Thomas running it himself and watching the refusal happen is the actual independent check. Fixture verification happens by direct regex inspection (T1), never by running settle.
- Do NOT change `securityAudit.provider` away from `mock` in this session -- that flip is step 2 of the runbook, for Thomas to run immediately before his real settle, not something to leave changed in the working tree during BUILD.
- Do NOT push this branch to origin or open a PR until the fixture has been reverted to its corrected counterpart and Thomas has completed both real settle runs.
- Do NOT let the seeded-defect fixture, CONDUCTION-RUNBOOK.md, or packages/core/tests/docs/phase256-conduction-prep.test.ts survive into this phase's eventual settle commit -- all three are prep scratch per the source handoff's "do not commit permanently" instruction; their deletion is a runbook step (Step 6) for Thomas, not a task in this DRAFT (this phase's task list ends at BUILD).
- Do NOT add any in-file comment to the fixture disclosing it as a seeded test defect -- that context must live only in this DRAFT, the runbook, and task/PROGRESS notes, never in the diffed fixture file itself.
- Do NOT treat this phase as reopening or fully unblocking `rec-20260801-010` -- it supplies settle 1 of 3 toward `dec-20260801-003`'s revisit trigger only, and even that is conditional on how the trigger's eventual offline analyzer globs for `-SUMMARY-snapshot.json` siblings (see AC-2's Context) -- flag this explicitly rather than assuming it counts.
