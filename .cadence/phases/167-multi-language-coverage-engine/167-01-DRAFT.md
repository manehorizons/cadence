---
phase: 167-multi-language-coverage-engine
id: 167-01
tier: complex
profile: standard
status: PENDING
---

# 167-01 — Shared-lexer multi-language assertion-coverage engine

## Objective

Generalize `findTestSpans` (`packages/core/src/verify/test-spans.ts`) from a hardcoded JS/TS-only scanner into one shared, string/comment-aware engine parameterized by a per-language profile — opener/assertion patterns, comment/string tables, and one of four block-boundary strategies (call-expression / brace-delimited / indentation-delimited / do-end-keyword) — shipping built-in profiles for python, go, rust, and php alongside a re-expressed js/ts profile with per-file dispatch by extension, a `verification.coverageProfiles` config escape hatch (add-only, load-time validated) so an unsupported language is never a dead end, a `cadence verify coverage --explain` diagnostic, and a doc-tested supported-language matrix; draft-time decisions (2026-07-11, operator-confirmed): widen init rust globs to include `src/**/*.rs`, ship the keyword strategy engine-side exercised via a custom-profile fixture, `--explain` gets `--json`, custom profiles may not override built-in extensions.

## Acceptance Criteria

### AC-1: shared profile-parameterized engine re-expresses js/ts with unchanged behavior
Given the existing JS/TS assertion-span behavior covered by the current `findTestSpans` test suite
When the scanner is rebuilt as a shared string/comment-aware engine that takes a language profile (opener pattern, assertion pattern, comment/string syntax tables, block-boundary strategy) and js/ts becomes just one profile using the call-expression strategy
Then every pre-existing js/ts span-finding test passes unchanged against the new engine — identical spans for identical input — and the js/ts profile is registered in a single profile registry that dispatch (AC-6) and docs (AC-9) both read from.

### AC-2: built-in python profile (indentation-delimited)
Given pytest-style fixture files containing `def test_*` functions (module-level and class-based) with `assert` statements
When the engine scans them with the built-in python profile
Then an AC token inside an asserting test-function body yields a span bounded by indentation (the whole test function), tokens in comments/docstrings/strings yield no span, and a non-asserting test function yields no assertion-mode span.

### AC-3: built-in go profile (brace-delimited)
Given Go test fixtures with `func TestX(t *testing.T)` including table-driven tests and `t.Run` subtests, asserting via `t.Error*`/`t.Fatal*`/testify-style calls
When the engine scans them with the built-in go profile
Then an AC token inside an asserting test-function body (including inside a subtest closure or table entry) yields a brace-bounded span, and tokens in comments or string literals yield no span.

### AC-4: built-in rust profile (brace-delimited, attribute-aware)
Given Rust test fixtures with `#[test]` / `#[should_panic]` functions (including inside `#[cfg(test)] mod tests` blocks) asserting via `assert!`/`assert_eq!`/`assert_ne!`
When the engine scans them with the built-in rust profile
Then an AC token inside an asserting `#[test]` function yields a brace-bounded span whose opener detection is attribute-aware (the `#[test]` attribute marks the block, not just the `fn` keyword), and tokens in comments or string literals yield no span.

### AC-5: built-in php profile (Pest call-expression + PHPUnit method shape)
Given PHP test fixtures in both Pest style (`it(...)`/`test(...)` closures with `expect(...)`) and PHPUnit style (`public function testX` methods calling `$this->assert*`)
When the engine scans them with the built-in php profile
Then AC tokens inside either shape's asserting block yield correct spans for that shape, and tokens in comments or string literals yield no span.

### AC-6: per-file dispatch by extension, unknown shapes yield zero spans
Given a mixed-language repo whose configured test globs match `.test.ts`, `_test.go`, `test_*.py`, and an extension no profile claims
When assertion-mode coverage evidence is gathered across those files
Then each file is scanned with the profile registered for its extension, the unclaimed-extension file contributes zero spans (never a partial or wrong match — false-negative-over-false-positive is the invariant), and the same zero-spans rule applies inside any file where the profile does not positively recognize the block shape.

### AC-7: `verification.coverageProfiles` operator-extensible custom profiles (add-only)
Given a `.cadence/config.json` defining a custom entry under `verification.coverageProfiles` (extensions, opener/assertion patterns, block-boundary strategy — including `keyword`, exercised by a Ruby-style do-end custom-profile fixture)
When config is loaded and assertion-mode coverage runs over matching files
Then the custom profile is validated at config-load time — a bad regex, missing required field, or an extension collision with a built-in profile refuses loudly with the offending field/collision named and a suggested fix, never silently ignored — and a valid custom profile makes the otherwise-unsupported language's files yield real assertion spans via the shared engine.

### AC-8: coverage explain diagnostic
Given a project with assertion-mode coverage configured and a draft with ACs
When the operator runs `cadence verify coverage --explain AC-N` (with `--json` for machine consumption)
Then it prints, without mutating any state, which test files matched the globs, which profile scanned each file, every span found for the AC token, and for each span why it did or didn't satisfy assertion mode (e.g. token present but block not asserting), so a failing gate is diagnosable without reading engine source; `--json` emits the same facts as structured stdout with diagnostics on stderr.

### AC-9: supported-language matrix doc kept honest by a doc-content test
Given `docs/reference/config.md` documents assertion-mode coverage support
When the built-in profile set changes
Then the docs carry a supported-language matrix (language, extensions, block strategy, recognized test/assertion shapes) and a doc-content test under `packages/core/tests/docs/` asserts the matrix matches the live profile registry, failing on any drift in either direction.

### AC-10: init rust glob defaults widened for inline unit tests
Given a fresh `cadence init` in a project detected as rust (per phase 166's marker-file detection)
When init writes the default `verification.testGlobs`
Then the rust defaults include `src/**/*.rs` alongside phase 166's `tests/**/*.rs` / `**/*_test.rs` so idiomatic inline `#[cfg(test)]` unit tests are scannable, existing configs are never rewritten (init-time only, phase 139/166 precedent), and the attribute-aware rust profile guarantees non-test source in the widened glob yields zero spans.

## Tasks

### T1: shared lexer engine, four block strategies, js/ts profile, registry
- files: `packages/core/src/verify/test-spans.ts`, `packages/core/src/verify/coverage-profiles/` (new: engine + strategy modules + registry + js/ts profile), `packages/core/tests/verify/`
- action: extract the string/comment-aware scanner core; implement the four block-boundary strategies (call-expression, brace-delimited, indentation-delimited, do-end-keyword) as engine primitives; re-express js/ts as a call-strategy profile registered in a new profile registry; `findTestSpans` becomes a thin wrapper dispatching to the engine
- verify: entire pre-existing `packages/core/tests/verify/` span suite passes unchanged; new unit tests cover each strategy primitive incl. comment/string masking
- done: AC-1

### T2: built-in python profile + pytest fixtures
- files: `packages/core/src/verify/coverage-profiles/python.ts` (new), `packages/core/tests/verify/` (+ pytest fixture files)
- action: python profile using the indentation strategy: `def test_*` openers (module-level and class-based), `assert`-statement assertion pattern, `#` comments, docstring/string tables
- verify: fixture tests prove span/no-span behavior per AC-2 (comments, docstrings, non-asserting functions)
- depends: T1
- done: AC-2

### T3: built-in go profile + fixtures
- files: `packages/core/src/verify/coverage-profiles/go.ts` (new), `packages/core/tests/verify/` (+ Go fixture files)
- action: go profile using the brace strategy: `func TestX(t *testing.T)` openers, `t.Error*`/`t.Fatal*`/testify assertion patterns, line/block comments, string/rune/raw-string tables; table-driven and `t.Run` subtest fixtures
- verify: fixture tests prove span/no-span behavior per AC-3
- depends: T1
- done: AC-3

### T4: built-in rust profile (attribute-aware) + widened init rust globs
- files: `packages/core/src/verify/coverage-profiles/rust.ts` (new), `packages/core/src/init/plan.ts`, `packages/core/tests/verify/` (+ Rust fixture files), `packages/core/tests/init/`
- action: rust profile using the brace strategy with attribute-aware openers (`#[test]`, `#[should_panic]`, inside `#[cfg(test)] mod`), `assert!`/`assert_eq!`/`assert_ne!` assertion patterns, comment/string/raw-string tables; widen init's rust default testGlobs to add `src/**/*.rs`
- verify: fixture tests prove span/no-span behavior per AC-4; init tests assert the widened rust globs and that existing configs are untouched
- depends: T1
- done: AC-4, AC-10

### T5: built-in php profile (Pest + PHPUnit shapes) + fixtures
- files: `packages/core/src/verify/coverage-profiles/php.ts` (new), `packages/core/tests/verify/` (+ PHP fixture files)
- action: php profile handling both shapes: Pest `it()`/`test()` call-expression openers with `expect()` assertions, and PHPUnit `public function testX` method openers (brace strategy) with `$this->assert*` assertions; PHP comment/string tables incl. heredoc
- verify: fixture tests prove span/no-span behavior per AC-5 for both shapes
- depends: T1
- done: AC-5

### T6: per-file dispatch wired into the coverage gate, zero-span invariant
- files: `packages/core/src/verify/coverage.ts`, `packages/core/src/verify/coverage-profiles/registry.ts`, `packages/core/tests/verify/`
- action: assertion-mode evidence gathering dispatches each globbed file to the profile claiming its extension; files with no claiming profile contribute zero spans; phase 166's glob-miss vs span-miss diagnostics keep working and name the profile (or absence) per file
- verify: mixed-language fixture repo test proves per-file dispatch and the zero-span rule per AC-6
- depends: T1, T2, T3
- done: AC-6

### T7: `verification.coverageProfiles` schema + load-time validation + keyword fixture
- files: `packages/types/src/config.ts`, `packages/core/src/verify/coverage-profiles/registry.ts`, `packages/core/tests/verify/`, `packages/core/tests/config/` (or existing config-test home)
- action: additive Zod schema for `verification.coverageProfiles` (`.optional()`/`.default([])`); load-time validation refusing bad regex / missing fields / built-in extension collisions with refuse+suggest messages; registry merges valid custom profiles (add-only); Ruby-style do-end custom-profile fixture exercising the keyword strategy end-to-end
- verify: config tests prove each refusal names the offending field and suggests a fix; keyword fixture yields real spans through the shared engine per AC-7
- depends: T1, T6
- done: AC-7

### T8: `cadence verify coverage --explain` CLI + `--json`
- files: `packages/core/src/cli/commands/verify.ts` (new), `packages/core/src/cli/register.ts`, `packages/core/src/verify/coverage.ts` (explain seam), `packages/core/tests/cli/`, `docs/reference/commands.md`
- action: new read-only `verify coverage` subcommand with `--explain AC-N` and `--json`: prints matched files, per-file profile, found spans, and per-span satisfy/not-satisfy reasons; pure explain function + thin CLI wrapper (pure core / impure shell); structured output on stdout, diagnostics on stderr; command reference updated in the same change
- verify: CLI tests (dist rebuild first) prove human and `--json` output on a fixture repo and that no state is mutated; docs-command test passes
- depends: T6
- done: AC-8

### T9: supported-language matrix doc + doc-content test
- files: `docs/reference/config.md`, `packages/core/tests/docs/` (new doc-content test)
- action: document the supported-language matrix (language, extensions, block strategy, recognized shapes) and the `coverageProfiles` config shape; doc-content test derives the matrix from the live profile registry and fails on drift in either direction
- verify: doc-content test passes and demonstrably fails when a profile is added without a doc row (test includes the negative assertion)
- depends: T2, T3, T4, T5, T7
- done: AC-9

## Boundaries

- Do NOT add any runtime dependency — no tree-sitter, no parser/lexer libraries; the engine is homegrown (dec-20260711-001, LOCKED).
- Do NOT let an unrecognized shape produce a span — zero spans on anything not positively recognized; false positives defeat the gate.
- Do NOT change mention-mode behavior, gate ordering, or any gate semantics beyond assertion-mode span-finding and its diagnostics.
- Do NOT edit existing js/ts span-test expectations to make AC-1 parity pass — parity means the engine matches old behavior, not that tests are adjusted to the engine.
- Do NOT allow custom profiles to override built-in extensions — add-only, refuse + suggest on collision (operator decision 2026-07-11).
- Do NOT rewrite existing operator configs at init (AC-10 is fresh-init defaults only).
- Do NOT touch verifier providers (`mock`/`anthropic`/`local`/host-cli), host adapter packages, or MCP surfaces beyond what the new CLI command auto-registers.
- Do NOT invoke real language toolchains (pytest/go/cargo/php) in tests — fixtures are static files; the suite stays offline and deterministic.
