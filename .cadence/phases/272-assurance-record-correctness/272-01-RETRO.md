# Retro

## Gate bypasses

- WARN test-coverage via --allow-missing-coverage: test-coverage gate bypassed via --allow-missing-coverage
- ERROR settle via --force: settle --force bypassed failing verdicts (structural: AC-7; deep: AC-1, AC-4, AC-7)

## Code review findings

- packages/core/tests/docs/phase272-assurance-correctness.test.ts: HIGH — CI runs on Windows, where `grep` is not guaranteed on PATH; this test throws ENOENT. Use a Node-only line count or skip it on win32. (line 60)
