---
phase: 272-assurance-record-correctness
id: 272-01
tier: complex
status: PENDING
---

# 272-01 — assurance-record.ts correctness pass

## Objective

Fix the raw NUL byte in assurance-record.ts that makes the file grep-classify as binary, add a regression guard against recurrence, resolve the deriveAssuranceRecord weak-classification docstring/code mismatch, and record a decision on rec-20260808-007's deep-verify/per-task-verify provenance exclusion — an encoding and correctness pass, not a schema or behavior change.

## Acceptance Criteria



### AC-1: regression guard proves the defect, then confirms the fix
Given assurance-record.ts:87 contains a raw 0x00 byte before this phase's fix
When the no-raw-NUL regression test (packages/*/src/**/*.ts) is run before the fix and again after it
Then it fails before (naming assurance-record.ts, verbatim output captured) and passes after (verbatim output captured), both pasted into the SUMMARY

### AC-2: NUL byte is fixed via an encoding change, not a behavior change
Given the regression test is red
When the literal NUL at assurance-record.ts:87 is replaced with an escaped \u0000 in the template literal
Then CMD-2 (NUL count) re-run reports NUL count=0, and the delimiter value used at runtime is unchanged

### AC-3: the file is no longer grep-invisible
Given the NUL byte is fixed
When grep -c "" packages/core/src/gates/assurance-record.ts runs
Then it returns a line count rather than "binary file matches"

### AC-4: corpus-wide content hashes still verify
Given no AssuranceRecordZ/GateProvenanceZ schema field changed and no schemaVersion bump occurred
When cadence summary verify-all runs
Then it exits 0 corpus-wide and the verbatim tail is pasted into the SUMMARY

### AC-5: deriveAssuranceRecord's weak-classification docstring matches its code
Given rec-20260801-006's docstring/code mismatch on the weak classification, plus its untested branch
When the docstring is corrected to match actual behavior and a test exercises the previously-untested branch
Then the docstring and code agree, the branch has coverage, and rec-20260801-006 is transitioned to a closed status referencing this phase

### AC-6: rec-20260808-007 has a recorded decision
Given Phase 263's changeset deliberately excluded deep-verify/per-task-verify from provenance persistence
When a decision is recorded either reaffirming the exclusion for v1.56 or scheduling it for v1.57 — no implementation
Then a decision id exists in .cadence/intelligence/decisions.json and rec-20260808-007's status reflects that resolution

### AC-7: real code-review conduction proves D-F's tier override took
Given this phase settles at tier: complex with CADENCE_HOST_CLI_BIN=codex set explicitly on the settle invocation
When this phase's own SUMMARY.json is inspected after settle
Then its code-review gate entry records status=ran, provider=host-cli, providerSelection=configured — if it instead records skipped or providerSelection=empty-diff/fallback, the override did not take and settle must not proceed until fixed

## Tasks



### T1: Write the regression guard, prove it red
- files: `packages/core/tests/gates/assurance-record-encoding.test.ts`
- action: write a test asserting no file under packages/*/src/**/*.ts contains a raw 0x00 byte (globs packages/{core,types,host-claude-code,host-codex,host-toolkit,testkit}/src); run it now and capture the RED output verbatim (assurance-record.ts:87 still has the literal NUL) — corpus-before-code per the v1.56 closeout handoff §8
- verify: the test fails, naming assurance-record.ts, before any fix is applied
- done: AC-1

### T2: Fix the NUL byte, prove the guard green
- files: `packages/core/src/gates/assurance-record.ts`, `packages/core/tests/gates/assurance-record-encoding.test.ts`, `packages/core/tests/docs/phase272-assurance-correctness.test.ts`
- action: replace the literal NUL at assurance-record.ts:87 with an escaped \u0000 in the template literal (same delimiter value, encoding only); add a one-line comment naming why NUL is the delimiter (provider/model names cannot contain it); re-run T1's regression test and capture the GREEN output verbatim; re-run CMD-2 and the grep -c check
- verify: CMD-2 reports NUL count=0; grep -c "" returns a line count, not "binary file matches"; T1's test now passes
- done: AC-1, AC-2, AC-3

### T3: Resolve rec-20260801-006 (docstring/branch fix)
- files: `packages/core/src/gates/assurance-record.ts`, `packages/core/tests/gates/assurance-record.test.ts`
- action: resolve rec-20260801-006: correct deriveAssuranceRecord's docstring to match its actual weak-classification behavior, and add a test for the previously-untested branch
- verify: docstring and code agree; the new test exercises the untested branch and passes; rec-20260801-006 transitioned to a closed status referencing this phase
- done: AC-5

### T4: Record a decision on rec-20260808-007
- files: `.cadence/intelligence/decisions.json`, `.cadence/intelligence/recommendations.json`, `packages/core/tests/docs/phase272-assurance-correctness.test.ts`
- action: record a decision on rec-20260808-007 (deep-verify/per-task-verify provenance exclusion): either reaffirm Phase 263's exclusion rationale for v1.56, or schedule the change for v1.57. No code change — this is a decision only, per the v1.56 closeout handoff §7 non-goal
- verify: a decision id exists in decisions.json naming rec-20260808-007 and the chosen resolution; rec-20260808-007's status reflects it (not a silent close)
- done: AC-6

### T5: Corpus-wide content-hash verification
- files: `.cadence/phases/272-assurance-record-correctness/272-01-SUMMARY.json`, `packages/core/tests/docs/phase272-assurance-correctness.test.ts`
- action: run cadence summary verify-all corpus-wide to confirm no historical SUMMARY.json's content hash was affected by this phase's changes
- verify: cadence summary verify-all exits 0; verbatim tail pasted into this phase's SUMMARY
- done: AC-4

## Boundaries

- DO NOT change `AssuranceRecordZ` / `GateProvenanceZ` schema; no `schemaVersion` bump.
- DO NOT implement rec-20260808-007 (deep-verify/per-task-verify provenance) — record a decision only, per §7 of the v1.56 closeout handoff.
- DO NOT change the `DELTAS` gate matrix.
- DO NOT change `conduction-drift-streak`'s threshold, severity, or wording.
- DO NOT hand-edit any file under `.cadence/intelligence/`.
- DO NOT reinterpret any historical `SUMMARY.json` — additive only.
- DO NOT introduce a new refusal path anywhere in the settle pipeline.
- DO NOT let `CADENCE_HOST_CLI_BIN` default to `claude` at settle time — it must be `codex`, set explicitly on the settle invocation (no config-level persistence exists for this; confirmed via `discoverKey`'s env-or-`.env` resolution).

## Evidence

**T1 — RED run (before the fix), verbatim:**
```
 ❯ tests/gates/assurance-record-encoding.test.ts (1 test | 1 failed) 10ms
     × 272-01/AC-1: assurance-record.ts and every other packages/*/src/**/*.ts file is free of raw 0x00 bytes 10ms

AssertionError: expected [ { …(2) } ] to deeply equal []
- Expected
+ Received
- []
+ [
+   {
+     "file": ".../packages/core/src/gates/assurance-record.ts",
+     "offset": 4866,
+   },
+ ]
 Test Files  1 failed (1)
      Tests  1 failed (1)
```

**T2 — GREEN run (after the fix), verbatim:**
```
 Test Files  1 passed (1)
      Tests  1 passed (1)
```

**AC-2 — CMD-2 re-run (NUL count):**
```
NUL count=0 firstOffset=-1 line=-1 size=6882
```

**AC-3 — grep -c re-run:**
```
$ grep -c "" packages/core/src/gates/assurance-record.ts
142
$ file packages/core/src/gates/assurance-record.ts
packages/core/src/gates/assurance-record.ts: JavaScript source, Unicode text, UTF-8 text
```

**AC-4 — `cadence summary verify-all` re-run, verbatim tail:**
```
283 checked: 46 MATCH, 237 NO_HASH, 0 failed
```
(exit 0; `NO_HASH` is informational only — pre-phase-223 SUMMARY.json files with no contentHash field — not a mismatch.)

**AC-5 — rec-20260801-006 (docstring/branch fix):** re-verified 2026-08-11 that the mismatch still held (deriveAssuranceRecord's zero-ACs/zero-verifier shape hits the `'unverified'` branch first, not `'weak'` as the old docstring's last bullet claimed) before touching the docstring — corpus-before-code applied to the doc fix too. Corrected docstring + 2 new tests (`272-01/AC-5`) in `assurance-record.test.ts` covering both previously-untested branches (zero-ACs/zero-verifier → `'unverified'`; zero-ACs/real-verifier → `'mixed'`). `rec-20260801-006` promoted to `shipped`, ref `272-assurance-record-correctness (272-01)`.

**AC-6 — rec-20260808-007 decision:** re-verified 2026-08-11 that `deep-verify.ts`/`per-task-verify.ts` still don't persist provider identity into `gates[]` (unchanged since the rec's 2026-08-08 finding). `dec-20260811-002` recorded, reaffirming the Phase 263 (`dec-20260808-008`) exclusion rationale through v1.56.0 and scheduling as v1.57 input. `rec-20260808-007` promoted to `deferred`/`blocked`.

**AC-7 — D-F real-conduction proof:** cannot be evidenced before this settle — the SUMMARY this AC gates on does not exist yet (self-referential by design; the DRAFT deliberately carries no task `done:` mapping for it, confirmed compatible by `cadence draft check`). De-risked via a throwaway complex-tier draft (`999-derisk-scratch`, discarded, never committed) earlier this session: confirmed real `codex` conduction fires with no self-invocation fallback (that guard only covers the `claude` family — `verifier-factory.ts`'s `SELF_INVOCATION_ENV_VAR = { claude: 'CLAUDECODE' }`), and confirmed `providerSelection` reads `'configured'` for a `config.codeReview.provider: "host-cli"` selection whenever the underlying call succeeds without throwing `HostCliError` (`verifier-factory.ts` tags `'configured'` at the `provider === 'host-cli'` branch regardless of which bin the env var names — it only downgrades to `'fallback'` on an actual `HostCliError`). This settle's own resulting `SUMMARY.json` is the actual proof; if `code-review`'s gate entry does not read `status=ran, provider=host-cli, providerSelection=configured`, that is a real finding per this AC's own text ("fix the override, do not proceed"), not something to paper over with a bypass.
