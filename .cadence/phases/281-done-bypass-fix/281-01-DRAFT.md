---
phase: 281-done-bypass-fix
id: 281-01
tier: standard
status: PENDING
---

# 281-01 — Close the cadence done bypass hole

## Objective

Route `cadence done <id>` through `buildTaskService` so the documented shortcut for `build task <id> --status=DONE` carries identical guarantees — per-task-verify and the record-time boundary/redundancy check — to the command it claims to be a shortcut for.

## Acceptance Criteria

### AC-1: done refuses like build task when per-task-verify would refuse
Given a phase whose per-task-verify gate would refuse task T1
When `cadence done T1` runs
Then it refuses with the same exit code and message as `cadence build task T1 --status=DONE`, and PROGRESS.json is unmutated.

### AC-2: done refuses like build task under a block-mode boundary breach
Given `boundaryEnforcement` resolving to `block` and a working-tree file outside T1's declared `files:`
When `cadence done T1` runs
Then it refuses and names each stray file, matching `build task`'s refusal exactly.

### AC-3: done records identically to build task in warn mode
Given `warn` mode
When `cadence done T1` runs
Then it records the outcome and populates `touchedFiles` identically to `build task --status=DONE`.

### AC-4: existing done behavior is unchanged when none of the three inherited gates would refuse
Given a repo where none of the three gates `done` now inherits from `buildTaskService` would refuse — per-task-verify, the record-time boundary/redundancy check, and the pre-existing (phase 58) unknown-task-id guard
When `cadence done T1` runs
Then observable behavior (exit code, stdout line, PROGRESS row) is unchanged from pre-change — proven by the existing `tests/cli/done.test.ts` cases still passing, with the one case that named an undeclared task id (an incidental fixture choice, never actually valid — `build task` against the same undeclared id has refused since phase 58) corrected to use a declared id instead. (As-built amendment, D-N2/`dec-20260815-006`: this guard was not identified at DRAFT-authoring time; discovered when T4's build reproduced it.)

Also: given `cadence done` is invoked outside BUILD (a `LoopViolationError`), the emitted `anomaly-notify` `loop-violation` event's `context.source` remains `'build.done'` — distinct from `build task`'s own `'build.task'` tag — exactly as `packages/core/tests/cli/loop-violation.test.ts`'s pre-existing, unmodified Phase-23.3 case already asserts. (As-built amendment, D-N3/`dec-20260815-007`: full-delegation to `buildTaskService` initially collapsed this distinction — `buildTaskService`'s catch block hardcoded `'build.task'` — discovered by T4's independent reviewer running the full suite, not the narrow `cli/done` filter.)

### AC-5: docs state done's gate parity and the bypass path, and the repo's own roadmap/milestone records reflect this phase
Given `docs/reference/commands.md`'s `done` section
When a reader looks up what gates `done` carries and how to bypass a refusal
Then it states that `done` carries the same gates as `build task --status=DONE` — per-task-verify, the record-time boundary/redundancy check, and the unknown-task-id guard (exit 2) — and names `build task` (with `--allow-per-task-failure` / `--allow-boundary-breach`) as the path for bypass flags, per decisions D-N (dec-20260815-005) and D-N2 (dec-20260815-006); and given `tests/docs/phase271-record-integrity.test.ts`'s roadmap-currency drift check, when this phase's own directory is on disk, then `.cadence/ROADMAP.md` and `.cadence/MILESTONES.md` each carry a Phase 281 entry so the drift check passes (recording this phase's own artifact only — not a backfill of the pre-existing, out-of-scope 271–280 gap).

## Tasks

### T1: Failing regression test — per-task-verify parity
- files: `packages/core/tests/cli/done.test.ts`
- action: add a test that configures the per-task-verify gate to refuse (mirror the fixture shape used in `packages/core/tests/cli/build-task-boundary.test.ts` or the per-task-verify suite for a refusing mock verdict), then asserts `cadence done T1` exits non-zero with the same message shape as `cadence build task T1 --status=DONE`, and that PROGRESS.json is unmutated. Must fail against current `done.ts` (it calls `recordTaskOutcome` directly and never runs `runPerTaskVerifyGate`) — paste the red output.
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- cli/done` (red, for the right reason: per-task-verify never fires)
- stop: once the new per-task-verify-refusal case is red for the stated reason and other done.test.ts cases are unaffected — do not touch done.ts in this task.
- done: AC-1

### T2: Failing regression test — block-mode boundary parity
- files: `packages/core/tests/cli/done.test.ts`
- depends: T1
- action: add a test with `boundaryEnforcement` resolving to `block` and a stray working-tree file outside T1's declared `files:`; assert `cadence done T1` refuses and names the stray file, matching `build task`'s refusal. Must fail against current `done.ts` (no boundary check runs at all — the call records unconditionally). Paste the red output.
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- cli/done` (red, for the right reason: boundary check never fires)
- stop: once the new block-mode case is red for the stated reason and T1's case still passes — do not touch done.ts in this task.
- done: AC-2

### T3: Failing regression test — warn-mode touchedFiles parity
- files: `packages/core/tests/cli/done.test.ts`
- depends: T2
- action: add a test asserting that in `warn` mode, `cadence done T1`'s recorded `touchedFiles` is the git-derived delta (matching `build task`'s behavior post-DP-B), not the pre-DP-B self-report. Must fail against current `done.ts`. Paste the red output.
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- cli/done` (red, for the right reason: touchedFiles still self-reported)
- stop: once the new warn-mode case is red for the stated reason and T1/T2's cases still pass — do not touch done.ts in this task.
- done: AC-3

### T4: Route done.ts through buildTaskService
- files: `packages/core/src/cli/commands/done.ts`, `packages/core/tests/cli/done.test.ts`, `packages/core/src/services/build-task.ts`
- depends: T3
- action: replace the direct `recordTaskOutcome(...)` call with `buildTaskService(repoRoot, { taskId, status: 'DONE', notes, anomalySource: 'build.done' }, io)` per D-N (true alias — no new flags on `done` itself). Adapt to `buildTaskService`'s `CommandResult`/`CommandIO` contract so the existing `Recorded <id>: DONE` stdout line and `LoopViolationError` stderr handling are preserved for AC-4. Do not touch `recordTaskOutcome`. **As-built amendment (D-N2/dec-20260815-006):** this reveals a third inherited gate — `buildTaskService`'s unknown-task-id guard (phase 58, predates this phase) — which breaks the pre-existing case "records DONE with empty notes when --notes omitted" (it calls `done T2` against a T1-only scaffolded DRAFT; `T2` was never a declared/valid id, `build task T2` against the same fixture has always refused with exit 2). Correct that one case's task id from `T2` to `T1` (a valid declared id) — this preserves exactly what the case tests (`--notes` defaulting) while satisfying the now-correct validation. The other 3 pre-existing cases must stay untouched. **As-built amendment (D-N3/dec-20260815-007):** full delegation also collapsed `packages/core/tests/cli/loop-violation.test.ts`'s pre-existing, unmodified assertion that `done`'s `LoopViolationError` anomaly carries `context.source === 'build.done'` (distinct from `build task`'s `'build.task'`) — `buildTaskService`'s catch block hardcoded the literal `'build.task'` into its internal `emitLoopViolation` call. Add an additive optional `anomalySource?: string` to `buildTaskService`'s args (default `'build.task'` when omitted — zero behavior change for `build task` itself, the MCP surface, or any other untouched caller), thread it into that `emitLoopViolation` call in place of the hardcoded literal, and pass `anomalySource: 'build.done'` from `done.ts`. This is the ONE narrowly-scoped, additive exception to the "DO NOT change `buildTaskService`" boundary — no other change to that file.
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- cli/done` (all 7 green) AND `pnpm --filter @thomas-powers-jr/cadence-core test -- cli/loop-violation` (the `build.done` source-tag case green again) AND a full `pnpm --filter @thomas-powers-jr/cadence-core test` run (no other file regresses)
- stop: once all of done.test.ts (7 cases) and loop-violation.test.ts pass, and the full package test suite shows no failures beyond any pre-existing, unrelated known flake. If any of the OTHER 3 pre-existing done.test.ts cases, or any other test file beyond loop-violation.test.ts, now fails, stop and report — do not edit those (see Boundaries).
- done: AC-4

### T5: Update done's docs section for gate parity and the bypass path
- files: `docs/reference/commands.md`, `.cadence/ROADMAP.md`, `.cadence/MILESTONES.md`, `docs/cli.md`, `packages/core/tests/docs/phase281-done-alias-docs.test.ts`
- depends: T4
- action: in the `### done` section, replace the current "per-task verifier gate fires exactly as it does for build task" line (accurate only after T4) with prose stating `done` carries the same three gates as `build task --status=DONE`: per-task-verify, the record-time boundary/redundancy check, and the unknown-task-id guard (refuses an undeclared task id with exit 2, distinct from the exit-1 gate refusals) — and, since `done` gains no bypass flags of its own, name `build task <id> --status=DONE --allow-per-task-failure` / `--allow-boundary-breach` as the path when a bypass is needed (the unknown-task-id guard has no bypass flag on either command). **As-built addition:** this phase's own `.cadence/phases/281-done-bypass-fix/` directory pushes `roadmap-currency`'s disk-vs-backfill drift from 10 (already at the threshold before this phase existed) to 11, tripping `tests/docs/phase271-record-integrity.test.ts`'s `drift <= 10` assertion — not caused by this phase's content, but blocking regardless. Fix by recording *this phase itself* (a `### Phase 281 — …` entry in ROADMAP.md, a `- **Phase 281** …` entry in MILESTONES.md, using `(rec-20260815-002)` rather than a guessed PR number), which brings drift back to 0. Backfilling the unrelated 271–280 gap is out of scope — file it as a recommendation instead (see below).
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- docs/cli-reference docs/phase271-record-integrity docs/phase281-done-alias-docs`
- stop: once the done section states gate parity and the bypass path, the cli-reference doc test still passes, and the roadmap-currency drift check passes. **As-built addition (found by T5's independent reviewer):** `docs/cli.md`'s "Shortcut commands" section had a pre-existing `> **Carry-forward:**` callout claiming `done`, `block`, and `needs-context` all skip task-id validation — true before this phase, now false for `done` specifically (it refuses an undeclared id, exit 2, records nothing). This is directly caused by this phase's own fix, unlike the unrelated 271-280 roadmap gap — narrow the claim to `block`/`needs-context` only and note `done`'s new behavior. **As-built addition (found at settle time):** the repo's live `verification.coverageScheme` is `phase-qualified`, not `bare` — `settle run --auto` refused because every AC-N token in `done.test.ts` was bare, not qualified `281-01/AC-N`. Requalified all of T1–T4's new test titles (`AC-1`→`281-01/AC-1`, etc. — the 2 genuinely pre-existing cases' own bare `(AC-1)`/`(AC-2)` tokens, referring to an earlier phase, are untouched) and added a new doc-content test file, `packages/core/tests/docs/phase281-done-alias-docs.test.ts`, asserting AC-5's docs claims with a `281-01/AC-5` qualified token — mirroring this repo's own `phase271-record-integrity.test.ts` pattern.
- done: AC-5

### T6: Changeset
- files: `.changeset/*.md`
- depends: T5
- action: add a changeset for `@thomas-powers-jr/cadence-core` (`minor` — per the repo's semver policy of shipping breaking changes as minor pending full coupling). State plainly: `cadence done <id>` now runs through the same `buildTaskService` path as `cadence build task <id> --status=DONE`, inheriting three gates it previously bypassed entirely — the per-task-verify gate, the record-time boundary/redundancy check, and `buildTaskService`'s pre-existing unknown-task-id guard. This is a real behavior change — `done` could previously never fail; it can now refuse an id-validation or gate refusal (exit 2 for an undeclared task id, exit 1 for a gate refusal) with no bypass flag of its own. Use `build task` with `--allow-per-task-failure` / `--allow-boundary-breach` for a gate-refusal bypass (the unknown-task-id guard has no bypass on either command — the id must be declared in the active DRAFT).
- verify: new file under `.changeset/` (excluding `README.md`) with valid frontmatter
- stop: once one changeset file exists naming the behavior change and the bypass path — do not add a second changeset for this phase.
- done: AC-5

## Boundaries

- DO NOT change `buildTaskService` itself, with ONE narrow exception (D-N3/`dec-20260815-007`): adding an additive optional `anomalySource?: string` arg (default `'build.task'`) threaded into its existing internal `emitLoopViolation` call, so `done.ts` can pass `'build.done'`. No other change to that file — no new gate, no signature change to any other arg, no behavior change for any caller that omits the new field.
- DO NOT add new flags to `done` beyond the existing `--notes` — D-N chose the true-alias option, not flag passthrough.
- DO NOT touch `recordTaskOutcome` directly.
- DO NOT rewrite 3 of the 4 pre-existing `tests/cli/done.test.ts` cases — AC-4 depends on them holding unmodified. The 4th ("records DONE with empty notes when --notes omitted") may have ONLY its task id corrected from the never-valid `T2` to `T1`, per the D-N2 as-built amendment — no other change to that case.
- DO NOT touch `packages/core/tests/cli/loop-violation.test.ts` — it is pre-existing, unmodified, and correct; T4's fix must make it pass again, not change what it asserts.
- DO NOT start Phase C (coverage scanner determinism) or any dispatch-driven live exercise (Phase D) in this phase — separately scoped, separately sized.
