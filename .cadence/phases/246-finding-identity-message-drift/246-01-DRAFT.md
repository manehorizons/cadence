---
phase: 246-finding-identity-message-drift
id: 246-01
tier: quick-fix
status: PENDING
---

# 246-01 — Finding-identity dedup under real-provider message drift

## Objective

Decide, on evidence rather than assumption, whether finding-identity dedup
under real-LLM-provider message drift (rec-20260801-010) needs code now —
and since the evidence says no, record that decision durably with a
concrete, pre-registered trigger, rather than leaving it open-ended or
building something speculative to feel productive.

## Acceptance Criteria

### AC-1: The decision is recorded durably and is queryable, not just prose in this file
Given rec-20260801-010 is `needs-decision` and has been `converted` to this
phase (terminal — CADENCE has no CLI path to unconvert a recommendation)
When phase 246 settles
Then `cadence decision add --rec rec-20260801-010` has recorded a decision
whose rationale states: (a) zero code-review findings have been persisted
across any settle to date (0/257 `SUMMARY.json` files, verified 2026-08-01),
(b) the correct next step once data exists is an **offline analyzer over the
accumulated SUMMARY.json corpus** (not in-loop telemetry, not fuzzy-matching
— both rejected above with reasons), and (c) the trigger for revisiting:
**at least 3 settles under a non-mock review provider have each persisted at
least 1 code-review finding** (3 chosen to match this repo's existing
convergence-attempt magnitude, `config.convergence.maxAttempts` default;
adjustable when first revisited, not binding).

## Tasks

### T1: Record the measure-vs-build decision for rec-20260801-010
- files: `.cadence/intelligence/decisions.json`, `.cadence/intelligence/recommendations.json`
- action: Run `cadence decision add --rec rec-20260801-010 --title "..." --rationale "..."`
  recording all three rationale points from AC-1: (a) 0/257 SUMMARY.json files
  contain a persisted code-review finding, (b) the pre-committed next step
  once data exists is an offline analyzer over the SUMMARY corpus — not
  in-loop telemetry (wrong grouping key, wrong axis, no cross-settle text
  access without breaking finding-routing.ts's pure no-ledger-import
  boundary) and not fuzzy-matching (still speculative against zero data),
  and (c) the trigger: ≥3 settles under a non-mock review provider each
  persisting ≥1 code-review finding.
- verify: `cadence decision show <dec-id>` shows the rationale containing the
  0/257 evidence, the offline-analyzer next-step, and the 3-settle trigger.
- done: AC-1

## Boundaries

- Do NOT modify `packages/core/src/intelligence/finding-routing.ts`,
  `packages/core/src/verify/finding-identity.ts`, or
  `packages/core/src/services/settle.ts` — this phase ships a decision
  record, no source code.
- Do NOT attempt to fix rec-20260801-011 (refused-settle SUMMARY overwrite)
  here — filed separately as its own unit of work, out of scope for this
  phase.
- Do NOT hand-edit `.cadence/intelligence/recommendations.json` to unconvert
  rec-20260801-010 — `converted` is a deliberately terminal status with no
  CLI path back to `candidate`/`needs-evidence`; the decision record is how
  a future session finds this call, not a reverted rec status.
