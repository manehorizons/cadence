---
phase: 203-milestone-reopen-transition
id: 203-01
tier: standard
status: PENDING
---

# 203-01 — Milestone reopen: deferred -> proposed CLI transition

## Objective

`clusterMilestones()` (`packages/core/src/intelligence/milestone.ts`) treats any non-`proposed` milestone as a permanent survivor and permanently excludes its claimed `recommendationId`s from re-clustering. `applyTransition()`'s allowed-transitions table has no path out of `deferred` (accept only works from `proposed`, defer only from `proposed`/`accepted`, close only from `exported`). Once a milestone is deferred, bumping the underlying recommendation's readiness/status has no effect — the rec stays permanently claimed by the dead milestone entry. Add a CLI-driven `deferred -> proposed` transition so an operator has a real path to reopen a stuck milestone instead of hand-editing `milestones.json`.

## Acceptance Criteria

### AC-1: a deferred milestone can be reopened via CLI
Given a milestone is in `deferred` status
When the operator runs `cadence milestone reopen <id>`
Then the milestone transitions to `proposed`, the CLI prints the new status, and the change is reflected in `cadence milestone list`

### AC-2: reopen refuses from any non-deferred status
Given a milestone that is NOT in `deferred` status (`proposed`, `accepted`, `exported`, or `closed`)
When the operator runs `cadence milestone reopen <id>`
Then the command refuses loudly, names the milestone's current status in the error, and makes no state mutation

### AC-3: reopened milestone's recommendation re-enters re-clustering
Given a milestone was reopened (`deferred -> proposed`) via AC-1
When `cadence milestone propose` runs again
Then the milestone's claimed `recommendationId`s are eligible for re-clustering instead of being permanently excluded by `clusterMilestones()`

### AC-4: unknown milestone id refuses cleanly
Given an `<id>` that does not exist in `milestones.json`
When the operator runs `cadence milestone reopen <id>`
Then the command exits non-zero with a clear "no such milestone" stderr message, no state mutation

## Tasks

### T1: extend applyTransition() with a reopen (deferred -> proposed) action
- files: `packages/core/src/intelligence/milestone.ts`
- action: add `'reopen'` to the `TransitionAction` union (line ~309); add `reopen: ['deferred']` to the `allowed` transitions table (line ~357-361); map `action === 'reopen' ? 'proposed' : ...` in the `nextStatus` derivation (line ~369-370). Before allowing the transition, guard against `recommendationId` collision: if any of the target milestone's `recommendationIds` are already present in another *survivor* milestone (any status other than `deferred`/`proposed`), refuse with an error naming the colliding milestone id and recommendation id, per the flagged Open Question risk.
- verify: unit tests in `packages/core/tests/intelligence/milestone.test.ts` covering `applyTransition(..., 'reopen')` for: a deferred milestone succeeds -> status `proposed`; a `proposed`/`accepted`/`exported`/`closed` milestone refuses naming its current status (AC-2); an unknown id refuses (AC-4); a deferred milestone whose recommendationId collides with a live survivor milestone refuses naming the collision.
- done: AC-1, AC-2, AC-4

### T2: wire `cadence milestone reopen <id>` CLI subcommand
- files: `packages/core/src/cli/commands/milestone.ts`
- action: register a `reopen <id>` command (either by adding `'reopen'` to the existing `for (const action of ['accept', 'defer'])` loop at line 45 with a status-label mapping update at line 62, or as its own `.command('reopen <id>')` block mirroring `close`'s error-handling shape at line ~73). Call `runMilestoneTransition(process.cwd(), id, 'reopen')`, print `milestone <id> → proposed` on success, print `milestone reopen refused: <error>` + non-zero exit on refusal.
- verify: CLI integration test in `packages/core/tests/cli/milestone.test.ts` exercising success + all three refusal paths (wrong status, unknown id, recommendation collision).
- done: AC-1, AC-2, AC-4

### T3: verify re-clustering pickup end-to-end (no clusterMilestones() change expected)
- files: `packages/core/tests/services/milestone-propose.test.ts`
- action: add an integration test: propose -> defer a milestone -> reopen it -> run `runProposeMilestones`/`cadence milestone propose` again -> assert the milestone's `recommendationIds` are recomputed as a fresh `proposed` entry (not permanently excluded via the `claimed` set in `clusterMilestones()`, `packages/core/src/intelligence/milestone.ts` line ~244-249). This is expected to pass once T1 lands, since `clusterMilestones()` already treats any non-`proposed` status as a frozen survivor and a `proposed` status as re-poolable — confirm no source change is needed here, only test coverage.
- verify: the new test passes; if `clusterMilestones()` does need a change to make this true, note it as a deviation and fix it here rather than assuming.
- done: AC-3

### T4: document the new transition and CLI command
- files: `docs/concepts.md`, `docs/reference/commands.md`
- action: document the `deferred -> proposed` transition in the milestone lifecycle description in `docs/concepts.md`; add `cadence milestone reopen <id>` to the CLI command reference in `docs/reference/commands.md` (inside the marker block the doc-sync test diffs against the registered command set).
- verify: `pnpm --filter @manehorizons/cadence-core test -- tests/docs` green (command-reference marker-block test picks up the new subcommand automatically).
- done: AC-1

## Boundaries

- Do NOT change the `accept`/`defer`/`close` transition behavior or their allowed-status tables — scope is additive (`reopen` only).
- Do NOT modify `clusterMilestones()`'s bucketing/grouping logic — T3 is a verification task, not a rewrite, unless the test in T3 reveals it's genuinely required.
- Do NOT touch the recommendation-restart workaround (`recommendation archive`/`unarchive`) — it remains a valid escape hatch independent of this fix.
- Do NOT hand-edit `.cadence/intelligence/milestones.json` anywhere in the implementation or its tests as a shortcut — go through `applyTransition()`/`runMilestoneTransition()`.
