---
phase: 210-anthropic-docs-callout-claude-code-vs-api-key
id: 210-01
tier: quick-fix
status: PENDING
---

# 210-01 — Docs callout: anthropic provider auth is separate from Claude Code's own login

## Objective

Add a docs callout to `docs/providers.md`'s `anthropic` section that
distinguishes "logged into Claude Code" (OAuth/subscription) from
"`ANTHROPIC_API_KEY` set" (the `anthropic` provider's actual, separately
metered requirement), modeled on the existing quota-transparency notice in
the `host-cli` section.

## Acceptance Criteria

### AC-1: Anthropic section carries an explicit auth-distinction callout
Given a reader is on `docs/providers.md`'s `## anthropic — Anthropic API`
section
When they read the setup instructions before running
`cadence config set verifier.provider anthropic`
Then a clearly marked callout tells them that being logged into Claude Code
(OAuth/subscription) does not satisfy the `anthropic` provider — it calls
the Anthropic SDK directly and requires a separately API-billed
`ANTHROPIC_API_KEY` — in tone and placement consistent with the existing
`### Quota-transparency notice` callout in the `host-cli` section.

## Tasks

### T1: Add anthropic-vs-Claude-Code-login callout to docs/providers.md
- files: `docs/providers.md`
- action: In the `## anthropic — Anthropic API` section (~line 102-135),
  add a clearly marked callout (blockquote or admonition, matching the
  tone and placement of the `### Quota-transparency notice` callout in the
  `host-cli` section, ~line 352-374) stating that being logged into Claude
  Code (OAuth/subscription) does not satisfy the `anthropic` provider —
  it is a direct Anthropic SDK call requiring a separately API-billed
  `ANTHROPIC_API_KEY`, with zero visibility into Claude Code's own
  credential store.
- verify: Manual read-through — the callout appears before or alongside
  the `export ANTHROPIC_API_KEY=...` setup snippet, reads consistently
  with the `host-cli` section's callout, and does not duplicate or
  contradict the existing runtime fallback-warning text (line ~130).
- done: AC-1

## Boundaries

- Do NOT touch `packages/core/src/verify/anthropic-verifier.ts` or any
  other runtime fallback-warning source — that message already carries
  this distinction as of phase 209.
- Do NOT add or modify doc-content tests to pin this callout's exact
  wording.
- Do NOT edit any section of `docs/providers.md` other than the
  `## anthropic — Anthropic API` section.
