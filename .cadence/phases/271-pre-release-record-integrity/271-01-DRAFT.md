---
phase: 271-pre-release-record-integrity
id: 271-01
tier: standard
status: PENDING
---

# 271-01 — Pre-release record integrity (roadmap/milestone currency)

## Objective

Close the roadmap-currency doctor warning and the milestone-status desync so main's own planning records are honest before v1.56.0 cuts, per docs/handoffs/HANDOFF-v1.56-release-closeout.md's Phase Q.

## Acceptance Criteria

### AC-1: roadmap-currency drift closed
Given ROADMAP.md/MILESTONES.md are 40 phases behind disk (highest on-disk phase 270, MILESTONES.md's highest referenced phase 230)
When ROADMAP.md gains ### Phase N entries for 257-270 and MILESTONES.md gains - **Phase N** entries through 270
Then cadence doctor's roadmap-currency check reports ok, and a re-run of the handoff's CMD-1 reports drift <= 10

### AC-2: milestone/recommendation desync reconciled or honestly recorded
Given CMD-5 reports one desynced milestone (mil-rec-rec-20260808-003, status proposed, sole recommendation already shipped)
When a CLI path to close/reconcile the milestone is attempted
Then either CMD-5 reports desynced=0, or -- if no CLI path exists -- the blocker is recorded as a recommendation naming rec-20260803-001's precedent and the desync is left honestly unresolved rather than hand-edited

### AC-3: release staging surface unchanged
Given CHANGELOG.md's [Unreleased] section and 8 staged changesets are the release-notes staging surface for v1.56.0
When this phase's edits land
Then CHANGELOG.md's [Unreleased] section is still empty and CMD-6 still reports 8 changesets -- this phase writes no release notes

## Tasks

### T1: Backfill ROADMAP.md through phase 270
- files: `.cadence/ROADMAP.md`
- action: Add ### Phase N backfill entries for phases 257-270 (Objective/As-built/PR per the file's own convention), prose only, never auto-generated
- verify: CMD-1 re-run's roadmap= value reaches >= 260; cadence doctor's roadmap-currency check reports ok
- done: AC-1

### T2: Backfill MILESTONES.md through phase 270
- files: `.cadence/MILESTONES.md`
- action: Add - **Phase N** one-liner entries for every landed phase 232-270 (minus 231/237/269, which never landed under those numbers), grouped under version-tagged sections where the version can be verified (git tag timestamp + changeset-consumption evidence), otherwise under a plainly-labeled backfill section
- verify: CMD-1 re-run's milestones= value reaches >= 260; cadence doctor's roadmap-currency check reports ok
- done: AC-1

### T3: Reconcile or record the milestone desync
- files: `.cadence/intelligence/recommendations.json`
- action: Attempt cadence milestone close on mil-rec-rec-20260808-003; if refused, file a recommendation naming the gap and rec-20260803-001's precedent instead of hand-editing the ledger
- verify: CMD-5 re-run reports desynced=0, or the filed recommendation id is recorded in this DRAFT alongside CMD-5's verbatim desynced=1 output
- done: AC-2

### T4: Confirm the release staging surface is untouched
- files: `CHANGELOG.md`
- action: Read-only confirmation: CHANGELOG.md's [Unreleased] section is empty and CMD-6 still reports 8 pending changesets
- verify: grep/CMD-6 output pasted into this phase's SUMMARY shows both facts hold
- done: AC-3

## Boundaries

- DO NOT touch any file under `packages/*/src` — this is a records-integrity phase, not
  a code phase. If a source file needs editing, scope has leaked.
- DO NOT hand-edit `.cadence/intelligence/*.json` — record any gap found (e.g. the
  milestone desync) as a recommendation via the CLI instead.
- DO NOT hand-write release notes into `CHANGELOG.md` — `changeset:version` owns that at
  Phase S (v1.56.0 release).
- DO NOT backfill ROADMAP.md's separately-known 239-241 gap (phases exist on disk,
  never got a `### Phase N` entry) — out of this phase's explicit scope; filed as
  rec-20260811-005 instead of fixed inline.

## Evidence

**E4 — `cadence doctor` before-image (2026-08-10T23:XX, before any Phase Q edits).**
Full output: 36 lines, one (`worktree-phases`, an unrelated pre-existing collision
warning against the abandoned `253-dependency-override-remediation` worktree) is ~84KB
and omitted here for length — reproducible via `cadence doctor` on this checkout.
Relevant lines, verbatim:

```
! warning roadmap-currency: Roadmap currency drift is 40 (on-disk highest phase is 270, but the lowest of the referenced-highest phase numbers across ROADMAP.md/MILESTONES.md is 230) — exceeds the 10-phase threshold.
! warning release-currency: Pending changeset(s) declare a major or minor version bump not yet released: affirmative-provider-selection.md (minor), ci-test-timeout-remediation.md (minor), conduction-drift-counter.md (minor), doctor-release-currency-check.md (minor), historical-coverage-audit.md (minor), mock-abstains-review-gates.md (minor), provider-selection-provenance.md (minor, minor), rendered-label-precision.md (patch, patch).
✓ ok      conduction-drift-streak: 2 consecutive most-recent settle(s) with no non-mock verifier identity, broken by .cadence/phases/267-mock-abstains-on-review-gates/267-01-SUMMARY.json (completedAt 2026-08-10T01:56:57.831Z), which carried one.
! warning conduction-reachability: Real-provider conduction reachability — code-review: blocked by session; security-audit: blocked by profile, provider.
    → code-review: run from a real interactive terminal outside a headless Claude Code session (CLAUDECODE unset) so the self-invocation guard doesn't force a mock fallback | security-audit: override profile: to 'strict' at tier: complex — security-audit's only reachable profile×tier cell; reconfigure securityAudit.provider off 'mock' (e.g. `cadence activate`)
```

**Note on staleness vs. the source handoff.** `docs/handoffs/HANDOFF-v1.56-release-closeout.md`
measured `main` @ `2026-08-10T22:05Z`, before phase 270 landed. Every CMD in this DRAFT
was re-run against current `main` (after phase 270) per the handoff's own "re-run before
relying on it" instruction — the handoff's own §2 figures (`drift=38`, `streak=1`,
`disk=268`, 281 drafts) are superseded by the numbers below.

**CMD-1 re-run (before Phase Q edits):**
```
disk=270 roadmap=256 milestones=230 includedMin=230 drift=40 threshold=10
```

**CMD-1 re-run (after T1/T2, before this phase's own directory existed):**
```
disk=271 roadmap=270 milestones=270 includedMin=270 drift=1 threshold=10
```
(`disk` includes this phase's own `.cadence/phases/271-.../` directory once scaffolded —
expected and harmless: even after Phase R (272) and Phase S (273) land, drift stays well
under the 10-phase threshold without those phases documenting themselves circularly.)

**CMD-2 (NUL byte, unchanged, informational only — Phase R's concern, not Q's):**
```
NUL count=1 firstOffset=4866 line=87 size=6651
```

**CMD-3 re-run — the one figure that moved in a way that matters for Phase R:**
```
records=282 real=3 streak=2
```
Handoff measured `streak=1`. It is now 2 — one settle away from the 3-threshold warning.
D-F's margin is gone: this phase (Q) settling under mock alone reaches streak=3. Phase R's
real code-review conduction is what resets it before Phase S's pre-publish doctor run, not
a nice-to-have on top of an already-safe margin.

**CMD-4 re-run:**
```
{"complex":29,"standard":233,"quick-fix":20}
```
(282 drafts; handoff measured 281/232-standard — phase 270 added one `standard`-tier
draft.)

**CMD-5 re-run (T3's verbatim evidence, per AC-2's second branch):**
```
milestones total=33 desynced=1
   mil-rec-rec-20260808-003 | proposed | rec-20260808-003
```
`cadence milestone close mil-rec-rec-20260808-003` → `milestone close refused: cannot
close milestone in status proposed`. No CLI path exists from `proposed` to a reconciled
state when the milestone's recommendation shipped outside the accept→export→build flow.
Recorded as `rec-20260811-004`, explicitly naming `rec-20260803-001`'s precedent (same
class of gap, different ledger). Desync left unresolved, not hand-edited.

**CMD-6 re-run (T4):**
```
8
```
Unchanged. `CHANGELOG.md`'s `[Unreleased]` section confirmed empty (`head -5
CHANGELOG.md`).

**Additional findings filed this phase (not acted on, per the handoff's non-goals and
Q's own scope discipline):**
- `rec-20260811-001` — security-audit reachable in only 1 of 9 profile×tier cells.
  Resolved as **D-E, decision dec-20260811-001**: leave unreachable through v1.56,
  record why (operator chose option 1 of 3; option 2 explicitly deferred to v1.57).
- `rec-20260811-002` — raw NUL byte in `assurance-record.ts` (Phase R's R.1/R.2 scope).
- `rec-20260811-003` — conduction-drift-streak chronically warns for ~90% of phases by
  construction (v1.57 input per the handoff, not acted on here).
- `rec-20260811-004` — milestone status has no CLI path when its rec ships out-of-band
  (this phase's own AC-2 finding).
- `rec-20260811-005` — ROADMAP.md missing `### Phase N` entries for 239-241 (exist on
  disk, never landed under those headings; discovered while researching this phase's
  backfill, explicitly out of scope per this DRAFT's Boundaries).
