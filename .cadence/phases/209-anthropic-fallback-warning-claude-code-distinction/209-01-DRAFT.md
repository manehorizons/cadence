---
phase: 209-anthropic-fallback-warning-claude-code-distinction
id: 209-01
tier: standard
status: PENDING
---

# 209-01 — Add Claude-Code-vs-ANTHROPIC_API_KEY distinction to the anthropic-provider mock-fallback warning

## Objective

The anthropic-provider mock-fallback warning (`verifier-factory.ts`) and its
`cadence config explain` counterpart (`config-explain/build.ts`) both say only
"ANTHROPIC_API_KEY is unset" with no hint that a Claude Code login doesn't
satisfy this requirement. Add the Claude-Code-vs-API-key distinction inline to
both warning strings, preserving the exact substrings other tests depend on.

## Acceptance Criteria

### AC-1: The anthropic mock-fallback warning names Claude-Code login as insufficient
Given a verifier family is configured (or overridden) to `provider: anthropic`
but `ANTHROPIC_API_KEY` is unset, so `createVerifierFactory`'s anthropic
branch (`packages/core/src/verify/verifier-factory.ts` ~209-216) takes the
key-missing path,
When the factory selects a verifier and emits its single stderr warning via
`warn` before returning `spec.mock()`,
Then the warning still contains the literal substring `ANTHROPIC_API_KEY is
unset` (so the five sibling-family regex assertions keep matching unchanged),
AND it additionally states that a Claude Code login (OAuth/subscription
session) does NOT satisfy this requirement — `anthropic` needs a separately
API-billed key with no visibility into Claude Code's credential store. The
byte-exact assertion at `verifier-factory.test.ts:84` is updated to the new
full string.

### AC-2: `config explain`'s provider-no-key warning carries the same distinction
Given `provider: anthropic` is set on a provider block with
`anthropicKeyPresent: false`, so `deriveWarnings`
(`packages/core/src/config-explain/build.ts` ~59-68) pushes the
`provider-no-key` warning shown by `cadence config explain`,
When `buildExplanation` renders that warning,
Then the message — in addition to today's "ANTHROPIC_API_KEY is unset — it
will silently fall back to 'mock'" text — states that a Claude Code login
does not satisfy the `anthropic` provider, mirroring AC-1's wording, verified
by a test in `packages/core/tests/config-explain/warnings.test.ts`.

## Tasks

### T1: Failing regression test for the new AC-1 warning wording
- files: `packages/core/tests/verify/verifier-factory.test.ts`
- action: update the byte-exact assertion (~line 84) to expect the new full
  warning string including the Claude-Code clause; run it to confirm it fails
  against current source.
- verify: the test fails before the source change, for the right reason (the
  new clause is missing)
- done: AC-1

### T2: Implement the AC-1 warning text change
- files: `packages/core/src/verify/verifier-factory.ts`
- action: update the anthropic branch's `warn()` literal (~line 212-214) to
  append the Claude-Code-login clause while preserving the
  `ANTHROPIC_API_KEY is unset` substring exactly.
- verify: T1's test passes; the sibling regex-only tests (per-task,
  code-review, plan-review, security-audit, factory) remain green untouched
- done: AC-1
- depends: T1

### T3: Failing regression test for the new AC-2 config-explain wording
- files: `packages/core/tests/config-explain/warnings.test.ts`
- action: add/extend an assertion expecting the `provider-no-key` warning
  message to include the Claude-Code clause; confirm it fails against current
  source.
- verify: the test fails before the source change, for the right reason
- done: AC-2

### T4: Implement the AC-2 config-explain warning change
- files: `packages/core/src/config-explain/build.ts`
- action: update the `provider-no-key` warning message (~line 61-68) to add
  the same Claude-Code clause, mirroring AC-1's wording.
- verify: T3's test passes; `build.test.ts` and `render.test.ts` remain green
- done: AC-2
- depends: T3

### T5: Sync the docs sample
- files: `docs/providers.md`
- action: update the fenced warning sample (~line 130) to match the new AC-1
  warning string exactly, so the doc no longer quotes stale text.
- verify: manual read-through confirming the quoted string matches the new
  source literal verbatim (no doc-content test currently pins this sample)
- done: AC-1
- depends: T2

### T6: Full verification pass
- files: (none — verification only)
- action: run `pnpm --filter @manehorizons/cadence-core test`, `typecheck`,
  `lint`, and `build`
- verify: all four commands exit 0
- done: AC-1, AC-2
- depends: T2, T4

## Boundaries

- DO NOT touch the `local` or `host-cli` fallback warning strings, or their
  tests.
- DO NOT edit or repoint `MOCK_VERIFIER_NOTICE` or `MOCK_FALLBACK_BANNER`.
- DO NOT add the fuller `docs/providers.md` anthropic-section callout beyond
  T5's minimal sample sync — that's owned by sibling rec-20260723-002.
- DO NOT add `CLAUDECODE=1` detection or touch `cadence doctor` / `cadence
  activate` — that's owned by sibling rec-20260723-003.
- DO NOT change the `ANTHROPIC_API_KEY is unset` substring in either warning
  — the sibling regex tests depend on it verbatim.
