---
phase: 136-readme-approve-inline-note
id: 136-01
status: APPROVED
---

# 136-01 — README real-phase example: inline --no-approve pointer

## Objective

rec-20260701-006's audit evidence (R-06/F6) pointed at two places: the
copy-paste agent prompt (`renderAgentPrompt`) and the README's "drive a real
phase yourself" walkthrough, both allegedly suggesting a plain `draft
approve` that refuses headless under the `standard`/`strict` gate profile.
Investigation found the agent-prompt side already fixed (phase 130's step 3
is "STOP. Do not approve. Show me the DRAFT and wait for my review." — the
agent never runs `approve` itself; regression-locked by
`tests/agent-prompt/render.test.ts`). The one remaining real gap: the
README's own walkthrough shows a bare `cadence draft approve
01-fix-login-timeout 01` with no inline pointer to `--no-approve`; the
existing gate-profile heads-up note lives two sections further down,
separated by another code block. Add an inline comment at the point of use.

## Acceptance Criteria

### AC-1: the real-phase walkthrough's approve line has an inline --no-approve pointer
Given the README's "To drive a real phase yourself instead of the demo" fenced
shell example
When a reader reaches the `cadence draft approve ...` line
Then the same fenced code block also mentions `--no-approve`, so the
non-interactive/CI/agent case is visible without scrolling to the separate
heads-up note

### AC-2: the existing heads-up note and profile-behavior docs are unchanged
Given the README's gate-profile heads-up block (naming `auto`/`standard`/
`strict` and the ≥20-commit heuristic)
When this phase's change lands
Then that block's content is unchanged (this phase adds an inline pointer, it
does not replace the fuller explanation)

## Constraints

- Doc-only change to `README.md`; no code changes (the agent-prompt side is
  already correct and out of scope here).
- `packages/core/tests/docs/readme-profiles.test.ts`'s existing assertions
  must keep passing unmodified.

## Open Questions

- None — the agent-prompt half of rec-006 was verified already-resolved by
  reading `render.ts` + its existing test; this phase covers only the README
  half of the finding.

## Provenance

- rec-20260701-006 (scout-20260701-audit).
