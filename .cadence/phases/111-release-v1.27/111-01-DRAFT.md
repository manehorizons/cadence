---
phase: 111-release-v1.27
id: 111-01
tier: standard
status: PENDING
---

# 111-01 — Release v1.27.0 — onboarding breeze

## Objective

Release v1.27.0: document the new init front-door flags, collapse the README
quickstart to the zero-prompt / `--demo` / key-aware flow, add a changeset, and
bump all four published packages `1.26.0 → 1.27.0` in lockstep.

## Acceptance Criteria

### AC-1: README quickstart reflects the new flow
Given the README Quickstart
When it is read after this phase
Then it shows the zero-prompt init (no name/profile prompts), mentions
`init --demo` for a ready-to-run loop, and mentions `init --activate` /
auto-wire instead of the old multi-step hand-edit sequence.

### AC-2: commands.md documents the new init flags
Given `docs/reference/commands.md`
When the `init` section is read
Then `--demo`, `--activate`, `--wire-host`, and `--skip-host-wire` are each
documented.

### AC-3: changeset + lockstep version bump
Given the release
When the packages are inspected
Then a changeset exists for v1.27.0 and `core`, `types`, `host-claude-code`,
`host-codex` are all at `1.27.0`.

### AC-4: full suite green
Given the version bump + doc edits
When `pnpm --filter @manehorizons/cadence-core test` runs
Then it passes (including the doc-sync guard).

### AC-5: CLAUDE.md narrative leads with 1.27.0
Given `CLAUDE.md`
When the version narrative is read
Then the latest version is `1.27.0` describing the onboarding-breeze milestone,
satisfying the doc-sync pre-commit hook.

## Tasks

### T1: Docs — README + commands.md (+ touch-ups)
- files: `README.md`, `docs/reference/commands.md`
- action: Collapse the README Quickstart to the new flow; document the four new
  init flags in commands.md.
- verify: a doc test / grep confirms the flags appear; manual read of README.
- done: AC-1, AC-2

### T2: Changeset + lockstep version bump + CLAUDE.md narrative
- files: `.changeset/*.md` (new), `packages/{core,types,host-claude-code,host-codex}/package.json`, `CLAUDE.md`
- action: Add a changeset; bump the four published packages to `1.27.0`; lead the
  CLAUDE.md version narrative with `1.27.0` (doc-sync hook requirement).
- verify: versions all read `1.27.0`; CLAUDE.md mentions `1.27.0`.
- done: AC-3, AC-5

### T3: Verify green
- files: (none — verification)
- action: Build + run the core suite (and the doc-sync hook test).
- verify: suite green.
- done: AC-4

## Boundaries

- DO NOT run `npm publish` — publishing is the operator-triggered manual
  `Release` workflow.
- DO NOT bump `cadence-testkit` (private; not on the lockstep line).
- DO NOT change phase 108/109/110 behavior — this phase is docs + release only.
