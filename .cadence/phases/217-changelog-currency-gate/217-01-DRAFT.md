---
phase: 217-changelog-currency-gate
id: 217-01
tier: quick-fix
status: PENDING
---

# 217-01 — Gate CHANGELOG.md currency the same way CLAUDE.md's version string is gated

## Objective

`CLAUDE.md`'s version line is already gated at commit- and push-time
(`.githooks/check-doc-sync.sh`, wired into `pre-commit`/`pre-push`, backed by
`packages/core/tests/docs/doc-sync-hook.test.ts`): when a commit changes
`packages/core/package.json`'s version, `CLAUDE.md` must mention the new
version or the commit/push is refused. Root `CHANGELOG.md` has no equivalent
mechanism — it silently stopped tracking releases at `1.6.0` while
`packages/core/package.json` climbed to `1.50.0` (44 versions, closed
2026-07-24 via PR #297's manual backfill). Extend the exact same checker to
`CHANGELOG.md`'s newest `## [x.y.z]` heading, reusing
`check-doc-sync.sh` as-is (per `dec-20260724-002`, scoped to gate-only —
auto-generating changelog prose from `SUMMARY.json` is deferred).

## Acceptance Criteria

### AC-1: A version bump without a matching CHANGELOG.md entry is refused at commit- and push-time, and in CI
Given a commit changes `packages/core/package.json`'s `version` field
When the commit is staged (`pre-commit`) or being pushed to `main`
(`pre-push`), or the corresponding CI check runs
And `CHANGELOG.md`'s newest `## [x.y.z]` heading does not mention the new
version
Then the commit/push/CI run is refused with a message naming the missing
version and `CHANGELOG.md` (reusing `check-doc-sync.sh`'s existing message
shape), exactly mirroring the current `CLAUDE.md` behavior; When
`CHANGELOG.md` does mention the new version, the check passes and behavior
is unchanged from today.

## Tasks

### T1: Extend the doc-sync gate to CHANGELOG.md
- files: `packages/core/tests/docs/doc-sync-hook.test.ts`, `.githooks/pre-commit`, `.githooks/pre-push`
- action: TDD order — first, in `doc-sync-hook.test.ts`, add a new cross-platform (pure Node, no bash) test mirroring the existing "CLAUDE.md tracks the canonical package version" test but for `CHANGELOG.md`: read `packages/core/package.json`'s `version`, read `CHANGELOG.md`, extract the FIRST `## [x.y.z]` heading via regex, and assert it equals the current version exactly (not "the version appears somewhere in the file" — the actual invariant is that the NEWEST heading matches; a stale mid-file mention of the current version from a much older, already-superseded release would otherwise false-pass). Confirm this test passes right now (CHANGELOG.md is already current after PR #297's backfill) — this is the steady-state assertion, not a regression test, so there's no "red" step here the way there was for phase 216; instead, temporarily verify the test actually fails if you feed it a stale CHANGELOG.md heading (e.g. via a quick manual sed/git-stash experiment, not a permanent test double), to prove the regex extraction logic is doing real work before trusting it. Then implement: extend `.githooks/pre-commit`'s existing version-bump check block to ALSO run `check-doc-sync.sh` against `CHANGELOG.md` (reusing the exact same `check-doc-sync.sh` binary, unmodified — pass `CHANGELOG.md` as the doc-label argument, same call shape as the existing `CLAUDE.md` check, both checks must pass for the commit to proceed) and do the identical extension in `.githooks/pre-push`'s doc-sync backstop block.
- verify: `pnpm --filter @manehorizons/cadence-core test -- doc-sync-hook`
- done: AC-1

### T2: Document the new gate
- files: `.claude/skills/release-cut/SKILL.md`, `CLAUDE.md`
- action: in `release-cut/SKILL.md`'s step 2 (Version bump), add a line paralleling the existing CLAUDE.md line: CHANGELOG.md's newest `## [x.y.z]` heading must also be updated to the new version in the same commit, or the doc-sync pre-commit hook aborts it (name `doc-sync-hook.test.ts` as the CI re-assertion, matching the existing CLAUDE.md sentence's phrasing). In `CLAUDE.md`'s "Enforcement layers — what will stop you" section, extend the existing `.githooks/pre-commit` — the doc-sync gate bullet to say it now also covers `CHANGELOG.md`'s newest version heading, not just `CLAUDE.md`'s version line.
- depends: T1
- verify: manual read-through; no automated test covers CLAUDE.md/skill prose content (matches this repo's existing convention — `doc-content` tests only cover code-verifiable claims)
- done: AC-1

## Boundaries

- Do NOT modify `check-doc-sync.sh` itself — it is already generic and already correctly tested; reuse it unmodified for both docs.
- Do NOT implement any CHANGELOG-generation-from-SUMMARY.json logic — out of scope per `dec-20260724-002`.
- Do NOT change the pre-commit/pre-push hooks' existing CLAUDE.md check in any way beyond adding the parallel CHANGELOG.md check alongside it — both checks must independently pass; do not merge them into one call or change either's message shape.
- Do NOT touch `.github/workflows/ci.yml` or `release.yml` — this phase gates at commit/push time (git hooks) and via the existing doc-content test surface (`pnpm turbo run test`, already part of CI), not a new standalone CI job.
