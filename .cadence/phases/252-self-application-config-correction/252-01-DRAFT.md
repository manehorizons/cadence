---
phase: 252-self-application-config-correction
id: 252-01
tier: standard
status: PENDING
---

# 252-01 — Self-application config correction

## Objective

Raise the repo's own gates.evidenceFloor to the solo preset's assertion default and record a decision deferring the baseline profile question to v1.56 Phase P, without superseding dec-20260803-001.

## Acceptance Criteria

### AC-1: Deferred-profile decision recorded
Given the repo's own `.cadence/config.json` still sets `profile: "auto"` and `dec-20260803-001` (recorded 2026-08-03) commits conduction to a deliberate per-phase DRAFT-level override rather than an incidental baseline consequence
When this phase records a new decision via `cadence decision add` stating the baseline `profile` question is deferred, not resolved, pending v1.56 Phase P (mock-abstains-on-review-gates)
Then `cadence decision show` displays the new decision, it states explicitly that it does not supersede `dec-20260803-001` (that decision's per-phase-override commitment is unchanged; this decision answers the narrower question of whether the baseline itself should ever move), `.cadence/config.json`'s `profile` field remains `"auto"`, and an asserting test block (token `252-01/AC-1`) verifies both facts directly from `.cadence/config.json` and `.cadence/intelligence/decisions.json`

### AC-2: evidenceFloor and securityAudit invariant
Given gates.evidenceFloor is "mention" in .cadence/config.json, below the solo preset default ("assertion", packages/types/src/config.ts:608), despite the repo never having adopted a preset
When this phase raises gates.evidenceFloor to "assertion" and leaves securityAudit.provider untouched
Then a new invariant test in packages/core/tests/docs/ (asserting block, token `252-01/AC-2`) asserts gates.evidenceFloor ranks at or above "assertion" AND securityAudit.provider === "mock", both together

## Tasks

### T1: Red-first invariant tests (AC-1 and AC-2)
- files: `packages/core/tests/docs/self-application-config.test.ts`
- action: Write TWO failing asserting `it()` blocks, each carrying its own qualified token (repo-wide AC-token scans collide across phases per `rec-20260729-004`, and a qualified token outside an asserting block silently zeroes coverage per `rec-20260730-002` — so both tokens must sit inside their asserting block, not a comment): (1) token `252-01/AC-2` — asserts `gates.evidenceFloor` (read from `.cadence/config.json`) ranks at or above `"assertion"` on the evidence ladder AND `securityAudit.provider === "mock"`, both in one assertion; (2) token `252-01/AC-1` — asserts `.cadence/config.json`'s `profile` field is still `"auto"` AND `.cadence/intelligence/decisions.json` contains a decision whose text references `dec-20260803-001` and explicitly states it does not supersede it. Confirm both blocks fail against current state (evidenceFloor is `"mention"`; no such decision exists yet).
- verify: `pnpm --filter @thomas-powers-jr/cadence-core test -- self-application-config.test.ts` fails (red, both blocks) before T2/T3.
- done: AC-1, AC-2

### T2: Edit config.json evidenceFloor
- files: `.cadence/config.json`
- action: Edit .cadence/config.json directly: change gates.evidenceFloor from "mention" to "assertion" only. Do not touch profile (stays "auto") or securityAudit.provider (stays "mock").
- verify: T1 test now passes (green); git diff .cadence/config.json shows only the evidenceFloor line changed.
- done: AC-2
- depends: T1

### T3: Record deferral decision + capture doctor snapshot
- files: `.cadence/intelligence/decisions.json`, `.cadence/intelligence/DECISIONS.md`, `.cadence/phases/252-self-application-config-correction/252-01-DRAFT.md`
- action: Run `cadence decision add --title "..." --rationale "..."` (confirmed CLI shape: only `--rec`/`--title`/`--rationale`, no `--rec` here since no rec sources this decision) to record the deferred-profile decision. Rationale text must state explicitly: does not supersede `dec-20260803-001` (that decision's per-phase-override commitment is unchanged; this answers the narrower question of whether the baseline itself should ever move); moving the baseline today would newly fire `draft-read` plus `code-review`/`deep-verify` at complex tier, gate-cost this release does not need before the Aug 12 deadline; note `softCap` as a distinct `auto`-specific mechanic worth understanding later, not resolved here; note `cadence init`'s maturity heuristic would give a repo this age `standard` fresh, as context only; trigger to revisit is v1.56 Phase P landing. Then capture `cadence doctor` output (the `conduction-reachability` line, confirming it stays `warning`/unchanged) into this DRAFT as the decision's baseline snapshot.
- verify: `cadence decision show <new-id>` displays the decision text with the non-supersede statement; T1's AC-1 test block now passes; DRAFT.md contains the captured doctor snapshot.
- done: AC-1
- depends: T1

### T4: Full verification suite
- files: `packages/core/tests/docs/self-application-config.test.ts`, `.cadence/config.json`
- action: Run the full verification bar: pnpm build && pnpm typecheck && pnpm lint && pnpm test. This task verifies, it does not itself satisfy any AC — leave `done:` empty.
- verify: All four commands exit 0; T1's two invariant blocks (AC-1 and AC-2 tokens) are both green under the new config and decision.
- done:
- depends: T1, T2, T3

## Decision baseline snapshot (T3)

`dec-20260804-001` recorded (2026-08-04) — defers the baseline `profile`
question to v1.56 Phase P; explicitly does not supersede `dec-20260803-001`.

`cadence doctor`'s `conduction-reachability` line, captured immediately after
recording the decision, confirming it is unchanged from baseline (`profile`
never moved in this phase — the correct outcome per AC-1):

```
! warning conduction-reachability: Real-provider conduction reachability — code-review: blocked by profile, session; security-audit: blocked by profile, provider.
    → code-review: override profile: in a DRAFT's frontmatter to 'standard' (tier: complex) or 'strict' (tier: standard or complex) to include code-review in the gate set; run from a real interactive terminal outside a headless Claude Code session (CLAUDECODE unset) so the self-invocation guard doesn't force a mock fallback | security-audit: override profile: to 'strict' at tier: complex — security-audit's only reachable profile×tier cell; reconfigure securityAudit.provider off 'mock' (e.g. `cadence activate`)
```

## Boundaries

- Do NOT change `.cadence/config.json`'s `profile` field — it stays `"auto"`. The baseline `profile` question is explicitly deferred to v1.56 Phase P, not resolved here (see AC-1 rationale); this decision does not supersede `dec-20260803-001`.
- Do NOT change `securityAudit.provider` — it stays `"mock"`. Per `dec-20260803-001` clause (c) that field is an independently-changeable config decision, but changing it is out of scope for this phase.
- Do NOT touch `isSelfInvocation`, `SELF_INVOCATION_ENV_VAR`, or `DELTAS` (phase 251's changeset commits to leaving these alone; unrelated to this phase's scope).
- Do NOT reopen or refile `rec-20260801-010` (archived shipped despite `dec-20260801-003` saying "ship no code this phase") or `rec-20260801-012` (already shipped via phase 251, carries `dec-20260803-001`).
- Do NOT touch the sibling worktrees (`kernel-arc-docs-review`, `phase249-refused-settle-post-gate`) — tracked separately as `rec-20260804-004`.
