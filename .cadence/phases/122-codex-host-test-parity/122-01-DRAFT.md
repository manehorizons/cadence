---
phase: 122-codex-host-test-parity
id: 122-01
tier: standard
status: PENDING
---

# 122-01 — Close Codex host adapter test parity

## Objective

Close the remaining Codex host-adapter parity gap with durable tests for local hook execution and prompt catalog parity.

## Acceptance Criteria

### AC-1: Codex local hook roundtrip
Given `installHooks(..., { local: true })` writes project `.codex/hooks.json`
When the generated `SessionStart` hook command is executed with a Codex hook payload
Then it invokes the local host shim and core CLI end-to-end and prints CADENCE session context.

### AC-2: Codex prompt catalog parity
Given Codex prompts are installed into a temporary `CODEX_HOME`
When generated prompt filenames are compared to the Claude command golden catalog
Then Codex exposes the same `cadence-*` command names, without missing or extra prompts.

### AC-3: Codex prompt format remains Codex-native
Given Codex prompts are installed
When prompt files are inspected
Then they use frontmatter plus terminal command blocks, include the managed marker, and never emit Claude-only `!` autorun or `allowed-tools` fields.

### AC-4: Existing Codex host tests remain green
Given the new parity tests are added
When `@manehorizons/cadence-host-codex` tests run
Then install, shim, adapter-conformance, CLI, and integration coverage all pass together.

## Tasks

### T1: Add Codex local hook roundtrip test
- files: `packages/host-codex/tests/local-roundtrip.test.ts`
- action: Mirror the Claude local roundtrip for Codex `.codex/hooks.json`.
- verify: `pnpm.cmd --filter @manehorizons/cadence-host-codex test -- tests/local-roundtrip.test.ts`
- done: AC-1

### T2: Add Codex prompt catalog parity test
- files: `packages/host-codex/tests/install-commands-parity.test.ts`
- action: Compare generated Codex prompt names against the Claude golden command catalog.
- verify: `pnpm.cmd --filter @manehorizons/cadence-host-codex test -- tests/install-commands-parity.test.ts`
- done: AC-2

### T3: Lock Codex-native prompt format
- files: `packages/host-codex/tests/install-commands-parity.test.ts`
- action: Assert frontmatter, managed marker, terminal command block, and absence of Claude-only fields across all prompts.
- verify: focused parity test
- done: AC-3

### T4: Run host and workspace gates
- files: `packages/host-codex/tests/*`
- action: Run Codex host tests plus full workspace gate after adding parity coverage.
- verify: `pnpm.cmd --filter @manehorizons/cadence-host-codex test`, `pnpm.cmd -w lint`, `pnpm.cmd -w typecheck`, `pnpm.cmd -w build`, `pnpm.cmd -w test`
- done: AC-4

## Boundaries

- Do not write to real `$CODEX_HOME`; use temp dirs.
- Do not change production host behavior unless a new parity test proves a bug.
- Do not alter Claude adapter fixtures.
