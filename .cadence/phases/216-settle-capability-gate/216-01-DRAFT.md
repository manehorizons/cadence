---
phase: 216-settle-capability-gate
id: 216-01
tier: quick-fix
status: PENDING
---

# 216-01 — Close the trust envelope: gate the SETTLE capability class in MCP serve

## Objective

`packages/core/src/mcp/tools.ts` classifies `cadence_settle` as capability
class `SETTLE` but its `run()` is never wrapped with `gatedRun` — unlike
`cadence_draft_approve`/`cadence_spec_approve` (`APPROVAL_BYPASS`), an MCP
call to `cadence_settle` executes immediately with no trust-envelope check.
`enforceApprovalBypassGrant` (phase 181) is already tool-agnostic and the
`cadence mcp trust grant/revoke/list` CLI already accepts `SETTLE` as a
gated class (`mcp-trust-grant.ts`'s `GATED_CLASSES`) — the remaining step is
wiring `cadence_settle`'s `run` through the existing `gatedRun` wrapper, the
same pattern already proven for the two `APPROVAL_BYPASS` tools.

## Acceptance Criteria

### AC-1: cadence_settle is gated by the trust envelope
Given an MCP client calls `cadence_settle`
When no valid trust grant exists for `cadence_settle` (missing, def-hash
mismatch, version mismatch, or expired — the same four checks
`enforceApprovalBypassGrant` already applies to the `APPROVAL_BYPASS` tools)
Then the call is refused with a reason naming which envelope check failed,
`settleService` never runs, and no `state.json`/`SUMMARY.{json,md}` write
occurs and the loop position is unchanged; When a valid, matching,
unexpired trust grant for `cadence_settle` exists (issued via
`cadence mcp trust grant --tool cadence_settle` on a real terminal)
Then the call proceeds to `settleService` exactly as it does today (no
change to a granted caller's behavior).

## Tasks

### T1: TDD the trust-envelope gate onto cadence_settle
- files: `packages/core/src/mcp/tools.ts`, `packages/core/src/mcp/trust/enforce.ts`, `packages/core/tests/mcp/trust/enforce.test.ts`, `packages/core/tests/mcp/tools-capability-class.test.ts`
- action: TDD order — first, in `enforce.test.ts`, add a `settleTool()` helper and failing unit tests mirroring the existing `APPROVAL_BYPASS` coverage (no grant refuses; valid grant passes; stale def-hash/version-mismatch/expired grant each refuse, naming the failing check), plus MCP-integration tests via the real server (`connect`/`call` helpers) proving a no-grant `cadence_settle` call is refused before `settleService` runs (loop position and `state.json` unchanged) and a valid grant lets it proceed to the real service — confirm these fail against current (ungated) behavior; then implement: wrap `cadence_settle`'s `run` in `tools.ts` with the existing `gatedRun('cadence_settle', ...)` wrapper (same call shape as `cadence_draft_approve`/`cadence_spec_approve`); rename `enforceApprovalBypassGrant` to `enforceGatedToolGrant` (it is already generic over any `ToolDef` and now gates three tools, not two — keeping the old name would be exactly the doc-drift `CLAUDE.md` warns against) and update its call site in `gatedRun` and the test import; update `cadence_settle`'s `description` string to document the grant requirement the way the two `APPROVAL_BYPASS` tools already do; update the stale `CapabilityClass` doc comment block (tools.ts, currently says SETTLE is "classified but deliberately left ungated this phase") and `enforce.ts`'s header doc comment (currently says "the two `APPROVAL_BYPASS` MCP tools") to reflect that `cadence_settle` is now gated too; finally, in `tools-capability-class.test.ts`, fix the `SETTLE class contains exactly cadence_settle` test's stale "left ungated this phase" comment (count/classification assertions themselves are unaffected — `cadence_settle` stays the sole `SETTLE`-class tool); confirm all new and existing tests pass (green).
- verify: `pnpm --filter @manehorizons/cadence-core typecheck`, `pnpm --filter @manehorizons/cadence-core build`, and `pnpm --filter @manehorizons/cadence-core test -- trust/enforce tools-capability-class` all pass
- done: AC-1

## Boundaries

- Do NOT change `cadence_settle`'s `capabilityClass` value (stays `SETTLE`) or its `inputSchema`.
- Do NOT touch the CLI `cadence settle` command path (`cli/commands/settle.ts` or `settleService`'s own logic) — this phase gates the MCP tool surface only, exactly as phase 181 scoped `APPROVAL_BYPASS`.
- Do NOT edit the phase-181 DRAFT/SUMMARY/RETRO artifacts under `.cadence/phases/181-mcp-tool-trust-envelope/` — they are historical record of a past phase's boundary (`CLAUDE.md`'s Freshen Reflex / .keel Cleanup principle); the "deferred to a follow-up recommendation" line there stays as written.
- Do NOT add a new runtime dependency — this is wiring of already-implemented, already-tested machinery.
- Do NOT change `TOOL_NAMES`, rename `cadence_settle` itself, or alter the `CallToolResult` shape of a passing (granted) call — only the refusal path for an ungated/invalid-grant call is new surface, matching phase 181's own boundary.
