---
phase: 76-mcp-tool-parity
id: 76-01
status: APPROVED
---

# 76-01 — MCP tool parity (handoff/resume/recommendation/doctor)

## Objective

Widen the `cadence mcp serve` tool set with the proven-out excluded commands —
`handoff`, `resume`, `recommendation add`, `recommendation promote`, `doctor` — so a host can
run session continuity and the scout→rec→promote path over MCP.

## Acceptance Criteria

### AC-1: The five new tools are advertised
Given a `cadence mcp serve` server in an initialized repo
When the client calls `tools/list`
Then it advertises the original phase-58 tools plus `cadence_handoff`, `cadence_resume`,
`cadence_recommendation_add`, `cadence_recommendation_promote`, and `cadence_doctor`.

### AC-2: handoff writes a SESSION doc
Given the connected server
When the client calls `cadence_handoff` (optionally with a `label`)
Then a `.cadence/handoff/SESSION-*.md` is written and the tool result's `structuredContent`
reports the written path; calling it again for the same path without `force` returns a clean MCP
error result (not a crash).

### AC-3: resume replays the freshest handoff (read-only)
Given a repo that has a handoff doc
When the client calls `cadence_resume`
Then the result returns the replayed narrative and `structuredContent` carries `found: true`, the
`handoffPath`, and the resolved `mode`; the loop state is unchanged.

### AC-4: recommendation add then promote round-trips
Given the connected server
When the client calls `cadence_recommendation_add` with a title/readiness and then
`cadence_recommendation_promote` on the returned id
Then the recommendation is appended to the ledger and its status/readiness advance — observable via
the `cadence://recommendations` resource and the recommend report.

### AC-5: doctor returns a structured health report
Given the connected server
When the client calls `cadence_doctor`
Then the result's `structuredContent` is the doctor report (`checks[]` + `ok`), and the report is
read-only (no state mutation).

### AC-6: graceful failure
Given a tool invocation that fails (e.g. promoting an unknown recommendation id)
When the client calls it
Then the server returns an MCP error result carrying the message and keeps serving subsequent
requests.

## Constraints

- **Reuse existing core logic** — the new tools call the already-factored `runHandoff`,
  `runResume`, `runDoctor`, `addRecommendation`, `runRecommendationPromotion` via thin
  `*Service(repoRoot, args, io)` adapters in `packages/core/src/services/`. No duplicated logic.
- **Do not refactor the existing CLI commands** — they keep their own rendering; the services are
  the MCP adapters over the shared core functions. (No CLI-output regression.)
- **No new tool/service may import the MCP SDK** (the hot path never loads it).
- **TDD, house style:** tests via the SDK in-memory transport against a `cadence-testkit`
  ephemeral repo — deterministic, offline, mock verifier, no subprocess/network. Each AC referenced
  by its `AC-N` token. Tests under `packages/core/tests/mcp/`.
- Tools added to `packages/core/src/mcp/tools.ts`; services under
  `packages/core/src/services/`. No new package.

## Open Questions

- None — scope fixed by the v1.16 design doc
  (`docs/superpowers/specs/2026-06-07-mcp-surface-deepening-design.md` §2).
