---
phase: 115-release-v1.28
id: 115-01
tier: standard
status: PENDING
---

# 115-01 — Release v1.28.0 — coverage depth + onboarding completion

## Objective

Cut v1.28.0: consume the three pending changesets (phases 112/113/114), bump all
four published packages `1.27.0 → 1.28.0` in lockstep, and lead the CLAUDE.md +
MILESTONES.md narrative with `1.28.0`. npm publish + the `v1.28.0` tag are the
operator-triggered manual `Release` workflow (not part of this phase).

## Acceptance Criteria

### AC-1: lockstep version bump + changesets consumed
Given the release
When the packages are inspected
Then `cadence-core`, `cadence-types`, `cadence-host-claude-code`, and
`cadence-host-codex` are all at `1.28.0`, the three changesets are consumed, and
each package CHANGELOG carries the v1.28.0 entry.

### AC-2: CLAUDE.md leads with 1.28.0 (doc-sync gate passes)
Given `CLAUDE.md`
When read after this phase
Then its latest-version narrative leads with `1.28.0` and summarizes phases
112/113/114, so the doc-sync gate (canonical version vs CLAUDE.md) passes.

### AC-3: MILESTONES records v1.28.0
Given `.cadence/MILESTONES.md`
When read after this phase
Then it has a `v1.28.0` section listing the three feature phases + the release
and noting the onboarding arc is complete.

### AC-4: full suite green
Given the version bump + narrative edits
When `pnpm turbo run lint typecheck test build` runs
Then it passes, including the doc-sync guard test.

## Tasks

### T1: version bump + changeset consume
- files: `packages/*/package.json`, `packages/*/CHANGELOG.md`, `.changeset/*`
- action: `pnpm changeset version` (done) — lockstep `1.27.0 → 1.28.0`, 3 changesets consumed.
- verify: `node -p "require('./packages/core/package.json').version"` is `1.28.0`
- done: AC-1

### T2: release narrative
- files: `CLAUDE.md`, `.cadence/MILESTONES.md`
- action: lead CLAUDE.md with the `1.28.0` entry (phases 112/113/114); add the v1.28.0 MILESTONES section.
- verify: `grep -c "1.28.0" CLAUDE.md` ≥ 1
- done: AC-2, AC-3

### T3: green gate
- files: (none — verification only)
- action: run the full pipeline; confirm the doc-sync guard passes with the bumped version.
- verify: `pnpm turbo run lint typecheck test build`
- done: AC-4

## Boundaries

- DO NOT trigger the npm `Release` workflow or push tags — publish is operator-only.
- DO NOT change any feature code — this phase is version + narrative only.
- DO NOT re-freshen ROADMAP or other `.cadence/` docs cosmetically; MILESTONES + CLAUDE.md carry the release record.
