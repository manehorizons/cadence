# Retro

## Code review findings

- packages/core/src/gates/engine.ts: MEDIUM — Exported mutable gate tables can be changed by any importer, altering later `gatesFor()` results. Expose frozen/read-only data or copies instead. (line 22)
