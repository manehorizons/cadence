---
phase: 219-recommendation-id-cross-check
id: 219-01
tier: quick-fix
status: PENDING
---

# 219-01 — Cross-check evidence.json in recommendation id-minting

## Objective

Make recommendation id-minting immune to a dangling `evidence.json` row silently colliding with a newly minted recommendation id, and add a `cadence doctor` check that surfaces such orphaned evidence rows so future drift is caught immediately instead of surviving unnoticed.

## Acceptance Criteria

### AC-1: id-minting cross-checks evidence.json
Given an evidence ledger entry references a `recommendationId` with no matching entry in `recommendations.json` (an orphaned/dangling row)
When `cadence recommendation add` mints a new recommendation id for the same date prefix
Then the minted id is strictly greater than both the max existing `recommendations.json` id and the max `recommendationId` referenced by `evidence.json` for that date prefix, so it can never collide with the orphaned row

### AC-2: doctor flags orphaned evidence rows
Given the evidence ledger contains a row whose `recommendationId` has no matching entry in `recommendations.json` (active or archived)
When `cadence doctor` runs
Then it reports the orphaned evidence row as a diagnostic finding, naming the evidence id and the missing recommendation id

## Tasks

### T1: Reproduce the id-minting collision with a regression test
- files: `packages/core/tests/intelligence/store/ids.test.ts`
- action: add a failing regression test proving `nextRecommendationId` mints an id that collides with a dangling `evidence.json` row's `recommendationId` (an evidence entry referencing an id with no matching `recommendations.json` entry, for the same date prefix)
- verify: the new test fails before the implementation change, for the expected reason (collision reproduced)
- done: AC-1

### T2: Fix `nextRecommendationId` to cross-check evidence.json
- files: `packages/core/src/intelligence/store/ids.ts`, `packages/core/src/intelligence/store/recommendations.ts`
- action: change `nextRecommendationId`'s signature (and its one call site in `addRecommendation`) to also take the `EvidenceLedger` (already loaded in scope at the call site) and take the max across both `recommendations.json` ids (+ archived) AND `evidence.json`'s referenced `recommendationId`s for the matching date prefix
- verify: T1's regression test passes; existing `ids.test.ts` / `recommendations.test.ts` coverage stays green
- done: AC-1
- depends: T1

### T3: Add a doctor check for orphaned evidence rows, with its own test
- files: `packages/core/src/doctor/*.ts`, `packages/core/tests/doctor/*.test.ts`
- action: add a best-effort `cadence doctor` check that reads `recommendations.json` + `evidence.json` and reports any evidence row whose `recommendationId` has no matching entry (active or archived) in `recommendations.json`, naming the evidence id and the missing recommendation id; degrade to no finding on any read error, never throw (match existing doctor check conventions)
- verify: a focused test proves the check fires on a synthetic orphaned-evidence fixture and stays silent on a clean ledger
- done: AC-2

### T4: Run full verification
- files: `packages/core` (test/typecheck/lint/build)
- action: run `pnpm --filter @manehorizons/cadence-core test`, `pnpm --filter @manehorizons/cadence-core typecheck`, `pnpm --filter @manehorizons/cadence-core lint`, `pnpm --filter @manehorizons/cadence-core build`
- verify: all four commands exit successfully with no failures
- done: AC-1
- depends: T2, T3

## Boundaries

- DO NOT touch `nextEvidenceId`, `nextAssumptionId`, or `nextIntelligenceDecisionId` — only `nextRecommendationId`'s evidence cross-check is in scope.
- DO NOT change the recommendation/evidence id format (`rec-YYYYMMDD-NNN` / `ev-YYYYMMDD-NNN`).
- DO NOT attempt to repair or backfill any currently-orphaned evidence rows in this repo's own `.cadence/intelligence/evidence.json` — this phase fixes the mechanism, not historical data.
- DO NOT add a general-purpose ledger-integrity framework — scope is the one doctor check described in T3.
