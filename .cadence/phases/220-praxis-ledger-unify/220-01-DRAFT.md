---
phase: 220-praxis-ledger-unify
id: 220-01
tier: complex
profile: standard
status: PENDING
---

# 220-01 — Deepen the Praxis ledger into one module

## Objective

Replace the five subject ledgers' (recommendations, evidence, assumptions, decisions, milestones) independently hand-rolled read/write and id-minting logic with one shared module, so a fix or safeguard added for one subject (e.g. the Phase 219 cross-ledger id-collision check) applies to all five instead of needing to be re-patched per subject — while genuinely bespoke per-subject transition logic (recommendation promotion, archive/unarchive, decision supersession) stays subject-specific on top of the shared primitives rather than being forced into one generic shape it doesn't fit.

## Acceptance Criteria

### AC-1: One shared module backs read/write/id-minting for all five subjects
Given the five subject ledgers (recommendations, evidence, assumptions, decisions, milestones)
When any subject reads its ledger file, writes it back, or mints a new id
Then that path runs through one shared module rather than five independent hand-rolled implementations — each subject's existing read/write/mint functions keep their current names and signatures as thin wrappers over it, so the ~13 existing callers outside `store/` and `cli/commands/` (`services/settle.ts`, `draft-new.ts`, `milestone-propose.ts`, `progress.ts`, `spec-new.ts`, `doctor/run.ts`, `intelligence/context.ts`, `inspect.ts`, `recommend.ts`, `cli/commands/next.ts`, `retro.ts`, `intelligence.ts`) continue to work unchanged. Bespoke subject-specific transition logic (recommendation promotion, archive/unarchive, decision supersession) is explicitly NOT required to collapse into one generic transition function — see Constraints.

### AC-2: Cross-ledger id-collision safeguard applies to all four id-minting subjects
Given a dangling cross-reference in a related ledger — a recommendation id inside `evidence[].recommendationId`, an assumption id inside `recommendation.assumptionIds[]`, a decision id inside `recommendation.decisionIds[]`/`supersededBy`/`supersedes`, or an evidence id inside `recommendation.evidenceIds[]` — whose number is higher than the owning ledger's own current max
When a new recommendation, evidence, assumption, or decision record is minted
Then the new id is computed with the same cross-ledger-max safeguard Phase 219 added for recommendations, using the per-subject cross-reference source named above — milestones mint no sequence id at all (their ids are cluster-derived, `mil-grp-<slug>`/`mil-rec-<recId>`) and are out of this AC's scope entirely

### AC-3: Milestones ledger reaches parity with the other four in reconcile/audit/stats
Given a milestone whose `recommendationIds[]` includes an id that exists in neither the recommendations ledger's live nor archived array (a milestone referencing a merely-archived, still-recoverable recommendation is NOT a broken reference — archived is recoverable via `unarchive`)
When `cadence intelligence audit` runs
Then it reports the broken reference as a new `orphan-milestone` finding kind alongside the existing eight (`AUDIT_KINDS`, `--filter-kind` validation, and `render-intelligence-audit.ts`'s section order all include it); `cadence intelligence reconcile`'s result (including its `--json` shape) and `computeIntelligenceStats`/`render-intelligence-stats.ts` include milestones alongside the other four subjects; and `docs/reference/commands.md` is updated in the same commit — both the "4 intelligence ledgers" count (3 occurrences) and the "eight finding kinds" list (which needs a ninth) currently go stale otherwise

### AC-4: One shared list/filter pipeline serves recommendation, decision, and assumption CLI commands
Given the `--sort-by`, `--filter-regex`, and `--filter-regex-flags` options exposed by `cadence recommendation list`, `cadence decision list`, and `cadence assumption list`
When a user passes any of them, or an invalid combination
Then all three commands validate through one shared pipeline parameterized by each subject's own sort-key set and command name — regex/flag validation and error wording are identical modulo the `<subject> list failed:` prefix and each subject's own allowed sort-key list, rather than three independently maintained copies of the validation logic

### AC-5: Existing on-disk ledgers keep reading correctly
Given the current `.cadence/intelligence/*.json` ledger files in this repo (and in any project already using CADENCE)
When the deepened module reads them
Then every existing record parses to a deep-equal value under the current schema — additive-only, no migration step required

### AC-6: Ledger writes stay byte-stable and mode-parity holds
Given a ledger file with no logical change to make
When the deepened module writes it back
Then the output is byte-identical to the pre-refactor writer (property order and formatting unchanged, so `cadence intelligence reconcile` remains byte-idempotent as documented), and `milestones.json` is written with the same `{ mode: 0o600 }` the other four ledger files already receive (currently missing — `store/milestones.ts`'s writer omits it)

## Tasks

### T1: Build the shared ledger core module
- files: `packages/core/src/intelligence/store/ledger.ts`, `packages/core/tests/intelligence/store/ledger.test.ts`
- action: create the `SubjectLedgerSpec<TRec, TLedger, TStatus, TAction, TPayload>` descriptor type and the generic `readLedger`/`writeLedger`/`mintId` functions (data + pure functions only; I/O stays in this module — pure core / impure shell). `records`/`withRecords` operate on a `{ live, archived }` shape.
- verify: unit tests exercise the generic functions against a fake in-memory subject spec (no real subject wired yet)
- done: AC-1

### T2: Migrate assumptions and decisions onto the shared module
- files: `packages/core/src/intelligence/store/assumptions.ts`, `packages/core/src/intelligence/store/decisions.ts`, `packages/core/tests/intelligence/store/assumptions.test.ts`, `packages/core/tests/intelligence/store/decisions.test.ts`
- action: define each subject's `SubjectLedgerSpec` (decision's supersession cycle-walk becomes its `guard`); existing exported function names/signatures stay thin wrappers over `readLedger`/`writeLedger`
- verify: existing assumption/decision store tests pass unchanged; all ~13 downstream callers (grep confirms none import removed)
- done: AC-1
- depends: T1

### T3: Migrate milestones onto the shared module for read/write and fix the write-mode gap
- files: `packages/core/src/intelligence/store/milestones.ts`, `packages/core/tests/intelligence/store/milestones.test.ts`, `packages/core/tests/intelligence/store/io.test.ts`
- action: wire milestones' read/write through the shared module (no `idMint` — milestone ids stay cluster-derived in `intelligence/milestone.ts`); keep `applyTransition`'s bespoke reopen-reclaim guard and `warning`-carrying result on top; add the missing `{ mode: 0o600 }` to the write path
- verify: `store/io.test.ts`'s existing platform-guarded mode assertions gain a milestones case and pass
- done: AC-1, AC-6
- depends: T1

### T4: Migrate recommendations and evidence onto the shared read/write primitives
- files: `packages/core/src/intelligence/store/io.ts`, `packages/core/src/intelligence/store/recommendations.ts`, `packages/core/tests/intelligence/store/recommendations.test.ts`, `packages/core/tests/intelligence/store/io.test.ts`
- action: keep `writeIntelligenceLedgers`'s existing paired atomic writer (recommendations + evidence + `RECOMMENDATIONS.md` render from sibling ledgers) on top of the shared primitives; recommendation promotion and archive/unarchive stay bespoke functions, not forced into the generic `transitions` shape
- verify: existing recommendation/evidence store tests pass unchanged; `buildRecommendationNotFoundMessage`'s nearest-id suggestion still fires
- done: AC-1
- depends: T1

### T5: Generalize the cross-ledger id-collision safeguard to all four minting subjects
- files: `packages/core/src/intelligence/store/ids.ts`, `packages/core/tests/intelligence/store/ids.test.ts`
- action: extend the Phase 219 `crossRefIds` safeguard from recommendations-only to evidence/assumptions/decisions, using each subject's cross-reference source named in AC-2
- verify: a regression test per subject reproduces the Phase 219 collision scenario (a dangling cross-ref higher than the owning ledger's own max) and confirms no collision
- done: AC-2
- depends: T2, T4

### T6: Bring milestones into intelligence reconcile/audit/stats
- files: `packages/core/src/intelligence/audit.ts`, `packages/core/src/intelligence/reconcile.ts`, `packages/core/src/intelligence/stats.ts`, `packages/core/src/render/render-intelligence-audit.ts`, `packages/core/src/render/render-intelligence-stats.ts`, `packages/core/src/cli/commands/intelligence.ts`, corresponding test files
- action: add an `orphan-milestone` finding kind to `AUDIT_KINDS` + `--filter-kind` validation + audit render section order; include milestones in `computeIntelligenceStats`, its renderer, and `cadence intelligence reconcile`'s result (including the `--json` shape) — archived (not deleted) recommendation references are NOT flagged as broken, per AC-3
- verify: `cadence intelligence audit` reports an `orphan-milestone` finding for a milestone referencing a fully-deleted rec id, and does not for one referencing an archived rec id
- done: AC-3
- depends: T3

### T7: Extract the shared CLI list/filter pipeline
- files: `packages/core/src/cli/commands/recommendation.ts`, `packages/core/src/cli/commands/decision.ts`, `packages/core/src/cli/commands/assumption.ts`, a new shared helper module under `packages/core/src/cli/`, corresponding test files
- action: factor `--sort-by`/`--filter-regex`/`--filter-regex-flags` parsing, validation, and error messages into one pipeline parameterized by each subject's sort-key set and command name
- verify: existing recommendation/decision/assumption list tests pass unchanged; a shared-pipeline test confirms identical validation behavior across all three modulo the subject-prefixed error message
- done: AC-4
- depends: T1

### T8: Read-compatibility regression coverage and doc sync
- files: `packages/core/tests/intelligence/store/io.test.ts`, `packages/core/tests/intelligence/reconcile.test.ts`, `docs/reference/commands.md`
- action: add a regression test reading this repo's own current `.cadence/intelligence/*.json` files through the deepened module (AC-5) and a read→write round-trip byte-stability test (AC-6); update `docs/reference/commands.md`'s `stats`/`audit`/`reconcile` sections (ledger count 4→5, finding-kinds list 8→9)
- verify: both regression tests pass; `grep -c` for "4 intelligence ledgers" / the old 8-kind list in `docs/reference/commands.md` returns zero
- done: AC-3, AC-5, AC-6
- depends: T4, T6

## Boundaries

- DO NOT touch `decayState` derivation logic (fresh/aging/stale/superseded/contradicted/needs-revalidation) — separate, out of scope.
- DO NOT bundle MCP/CLI service parity (rec-20260725-004) into this phase.
- DO NOT refactor `packages/core/src/services/settle.ts`'s own internal structure (rec-20260725-007, a separate phase) — it is a read-only consumer here and must keep working unchanged via preserved thin-wrapper signatures.
- DO NOT change milestone id format — milestones keep cluster-derived ids (`mil-grp-<slug>`/`mil-rec-<recId>`) and get no `idMint`.
- DO NOT make a breaking or migration-requiring change to any `.cadence/intelligence/*.json` file's on-disk shape — additive only.
