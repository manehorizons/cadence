---
phase: 239-coverage-phase-scoping
id: 239-01
tier: complex
status: PENDING
---

# 239-01 — Phase-attributable AC coverage: close the collision and the false drift

## Objective

Nothing in a settled phase's artifacts records which phase a test belongs to, and CADENCE's two coverage consumers each paper over that with an opposite guess. The `test-coverage` gate searches every `packages/**/*.test.ts` for the bare token `AC-N`; since AC ids restart at `AC-1` each phase, any past phase's `AC-3` satisfies every future phase's `AC-3` — measured here, `AC-1` is satisfied by 189 unrelated files, so a phase can pass the gate having written no tests. `replayPhaseCoverage` instead scopes to the files the DRAFT declared, which under-declares constantly — replaying phase 233 reports 5/5 ACs drifting against a SUMMARY recording all five as pass/executed, a false positive, since 233's tests exist and assert correctly. The gate under-refuses and the replay over-refuses, from one shared cause. This phase fixes the cause: a phase-qualified prefix token (`239-01/AC-3`) makes each test self-identify, which lets the gate demand this phase's own evidence and lets the replay drop file-scoping entirely and match repo-wide on a globally unique token. Phases settled before the scheme existed have no phase-attributable evidence and their true coverage is unrecoverable, so the replay reports them `indeterminate` rather than asserting a verdict it cannot support. Gated behind `verification.coverageScheme` so consumer projects on `@manehorizons/cadence-core@1.51.1` are untouched until they opt in.

## Acceptance Criteria

### AC-1: the bare scheme is unchanged
Given a config with `verification.coverageScheme: "bare"` or with the field absent entirely
When the `test-coverage` gate runs
Then coverage resolution is byte-for-byte identical to current behavior, including `mention`/`assertion` mode and the existing weak-link, skip-only, and refusal message text

### AC-2: the qualified scheme refuses an unqualified or foreign-phase reference
Given `coverageScheme: "phase-qualified"` and a draft whose id is `239-01`
When an AC's only references are a bare `AC-3` token, or another phase's `211-01/AC-3` token, inside an asserting test block
Then the gate refuses that AC as uncovered rather than passing it

### AC-3: the qualified scheme passes on this phase's own token
Given `coverageScheme: "phase-qualified"` and a draft whose id is `239-01`
When an asserting test block references `239-01/AC-3`
Then the gate counts AC-3 as covered, and that same test satisfies no other phase's AC-3

### AC-4: the refusal names the exact expected token
Given a refusal raised under `coverageScheme: "phase-qualified"`
When the gate writes its refusal to stderr
Then the message contains the literal token string the test must carry, so the fix requires no source reading

### AC-5: back-compat holds through the real load path, and the field is operator-editable
Given a `.cadence/config.json` that predates this field, or no config file at all, versus one written by a fresh `cadence init`
When each is resolved through `loadConfig` — the actual path the gate uses, not the Zod schema in isolation
Then the pre-existing and absent cases both resolve to `"bare"` while the fresh init resolves to `"phase-qualified"`, and the field is editable through `cadence config edit`

As built: this AC originally asserted only the Zod schema layer, and that layer was correct while the shipped behavior was not. `loadConfig` (`config/loader.ts:20-28`) merges the user's `config.json` *over* `defaultConfig`, so a `defaultConfig` carrying `"phase-qualified"` is injected into every pre-existing config before Zod parses and the field-level `.default("bare")` never fires — flipping every consumer of published 1.51.1 to the strict scheme on upgrade. The phase-139 `coverageMode` precedent does not transfer, because `cadence init` writes `coverageMode` explicitly into every config.json so the user's own value wins the merge, whereas no config on earth contains `coverageScheme`. The AC is therefore restated to bind the end-to-end load path, and the mechanism moves: `defaultConfig` holds `"bare"` and `cadence init` writes `"phase-qualified"` explicitly in its existing verification overlay (`init.ts:372-378`, alongside `testGlobs`/`coverageMode`).

### AC-6: the prefix form keeps the bare token lexable
Given a qualified token in the prefix form `239-01/AC-3`
When any existing consumer of `AC_TOKEN_RE` scans it
Then the embedded `AC-3` still lexes as a bare token, and `cadence verify coverage --explain` additionally reports whether each occurrence satisfies the configured scheme

### AC-7: the settle records which scheme produced the evidence
Given a phase settles under either scheme
When `SUMMARY.json` is written
Then it records the `coverageScheme` and `coverageMode` in force at settle, as additive optional fields that leave every pre-existing SUMMARY parsing unchanged

### AC-8: replaying a qualified-scheme phase matches repo-wide with no file scoping
Given a settled phase whose SUMMARY records `coverageScheme: "phase-qualified"`
When `cadence verify phase` replays it
Then it matches that phase's qualified tokens across the whole repo, never scopes to the DRAFT's declared files, and never returns `no-scoped-files` — so a phase that under-declared its test files replays correctly

As built: "across the whole repo" overstates both what was built and what is correct. The qualified branch scopes the scan to `verification.testGlobs` (or the engine's `DEFAULT_GLOBS` when absent) — the same file set the `test-coverage` gate itself scans — matched by `<id>/AC-N`. A literal whole-repo scan was **rejected as a defect, not accepted as a shortfall**: measured on this branch, scanning `**/*` under `mention` mode (the default when a consumer configures no `coverageMode`) credits `239-01/AC-8` from `.cadence/intelligence/RECOMMENDATIONS.md`, `.cadence/intelligence/evidence.json`, and a `.cadence/handoff/SESSION-*.md` — letting a phase's own planning prose satisfy its own coverage replay, which is CLAUDE.md's "The Token Drop" promoted from a gaming risk to specified behavior, and would make `verify phase` structurally incapable of detecting drift for any phase whose artifacts mention its own tokens. (`assertion` mode survives it incidentally — `.md`/`.json` yield zero spans — but `mention` is the default.) Scoping to the gate's own file set is also what keeps gate and replay from disagreeing. The contrast this AC actually draws is its trailing clause — no scoping to the DRAFT's declared files, no `no-scoped-files` refusal, an under-declared phase replays correctly — and that is satisfied exactly.

### AC-9: replaying a pre-scheme phase reports indeterminate, never drift
Given a settled phase whose SUMMARY records no coverage scheme, such as phase 233
When `cadence verify phase` replays it
Then each AC is reported `indeterminate` with `drift: false`, the output states that pre-scheme coverage is not phase-attributable and therefore unverifiable, and the command does not claim drift it cannot substantiate

### AC-10: docs are paired and this repo dogfoods the scheme
Given the gate, replay, and config schema have changed
When the doc-content test surface runs
Then `docs/concepts.md`'s `test-coverage` gate row and `docs/reference/config.md`'s field table both describe `coverageScheme`, a doc test asserts the pairing, and this repo's `.cadence/config.json` is set to `"phase-qualified"` so this phase settles under its own new rule

## Tasks

### T1: Add the coverageScheme config field with a two-layer default
- files: `packages/types/src/config.ts`, `packages/types/tests/config-coverage-scheme.test.ts`, `packages/core/src/tutorial/fixtures.ts`, `packages/core/src/cli/commands/init.ts`, `packages/core/tests/config/coverage-scheme-loader.test.ts`
- action: add `verification.coverageScheme: z.enum(['bare','phase-qualified']).default('bare')` alongside `coverageMode`, hold `'bare'` in `defaultConfig` and in the object-level `.default({...})` literal, and write `'phase-qualified'` explicitly in `init.ts`'s verification overlay so only a fresh init opts in. As built (two amendments): (1) a Zod `.default()` makes the field required in the emitted output type, so every hand-built `CadenceConfig` literal must supply it — `tutorial/fixtures.ts`'s `SANDBOX_CONFIG` is the one such literal and pins `'bare'`, deliberately, for the same reason it pins `coverageMode: 'mention'`. (2) `defaultConfig` must hold `'bare'`, NOT `'phase-qualified'`: `loadConfig` merges user config over `defaultConfig`, so a strict default there is injected into every pre-existing config before Zod runs and silently flips every consumer on upgrade. The opt-in belongs at the init layer, which is the only writer that can distinguish a fresh project from an upgraded one.
- verify: through `loadConfig`, a config.json with a `verification` block lacking the key resolves to `'bare'`, a repo with no config.json resolves to `'bare'`, and a config written by `cadence init` resolves to `'phase-qualified'`
- done: AC-5

### T2: Teach scanTestCoverage the expected qualifier
- files: `packages/core/src/verify/coverage.ts`, `packages/core/tests/verify/coverage-scheme.test.ts`
- action: add an optional `expectedQualifier` to `CoverageScanOptions`; when set, a `TestRef` counts only if the matched token is immediately preceded by that qualifier prefix. Leave `AC_TOKEN_RE` and the bare path untouched
- verify: with no option the scan is unchanged against existing fixtures; with `expectedQualifier: '239-01'` only prefixed occurrences are returned
- done: AC-1, AC-6

### T3: Wire the gate to the active draft id and name the expected token on refusal
- files: `packages/core/src/gates/coverage.ts`, `packages/core/tests/gates/coverage-qualified.test.ts`
- action: resolve the scheme from config, pass the active draft id as the qualifier under `phase-qualified`, and extend each refusal to print the literal expected token. As built: the qualifier could NOT be threaded through the shared `ctx.coverage()` thunk — it is memoized with bare options in `services/settle.ts`, outside this task's files — so the qualified path runs its own `scanTestCoverage` and the bare path keeps consuming the memoized thunk. This was the pre-identified boundary risk, and the alternative (declaring `services/settle.ts` here) was rejected because the tests written for T3 already specify the gate-local scan and because the real fix belongs where the second consumer lives, not in the gate. Consequence, recorded rather than silently absorbed: a *passing* qualified settle now scans the repo twice, and `deriveEvidenceAndCheckFloor` still derives per-AC evidence from the bare, unqualified map. For an AC listed as `explicit` — filtered out of `required` and so never qualifier-checked — that evidence comes entirely from a bare scan any past phase's identically-numbered AC can satisfy, which is the collision this phase exists to close. **T6 must rewire evidence derivation to the resolved scheme before T10 flips this repo to `phase-qualified`, or the SUMMARY will contradict its own gate.**
- verify: bare-only and foreign-phase references both refuse; a correctly-qualified asserting block passes; the refusal contains the exact token
- done: AC-2, AC-3, AC-4
- depends: T1, T2

### T4: Make verify coverage --explain scheme-aware
- files: `packages/core/src/services/verify.ts`, `packages/core/src/verify/coverage.ts`, `packages/core/tests/verify/coverage-explain-qualified.test.ts`
- action: report per-occurrence whether it satisfies the configured scheme, so a qualified refusal is debuggable without reading engine source. As built: `packages/core/src/verify/coverage.ts` is added to this task's set — the third boundary under-declaration of the same class the handoff predicted. `explainAcCoverage` already *accepts* `expectedQualifier` (T1/T2 widened `CoverageScanOptions`) but its body ignores it, and the per-occurrence `satisfies`/`reason` verdict it computes is the thing AC-6 requires to become scheme-aware. Doing it from `services/verify.ts` instead was rejected: the returned occurrences carry a trimmed `snippet` whose offsets no longer align with the file text, so the service would have to re-read every matched file and re-implement the qualifier check — duplicated I/O plus a second copy of the rule that would drift from `scanTestCoverage`. Declared rather than silently absorbed
- verify: `--explain` on an AC satisfied only by bare tokens reports them non-qualifying under `phase-qualified`
- done: AC-6
- depends: T2

### T5: Expose coverageScheme through cadence config edit
- files: `packages/core/src/config-edit/fields.ts`, `packages/core/tests/config-edit/coverage-scheme-field.test.ts`, `packages/core/tests/config-edit/fields.test.ts`, `packages/core/tests/cli/init.test.ts`
- action: register the field with its allowed values so it is editable exactly as `coverageMode` is, and prove AC-5's init clause end-to-end
- verify: `cadence config edit coverageScheme` accepts both values and rejects anything else; a real `cadence init` writes `coverageScheme: 'phase-qualified'` into `config.json`
- done: AC-5
- depends: T1

As built (second amendment): `packages/core/tests/cli/init.test.ts` is added to this task's set to close AC-5's clause (b) — "a fresh `cadence init` resolves to `phase-qualified`" — which was implemented in T1's `init.ts` overlay but had **zero** asserting coverage anywhere in the repo. The existing loader test only looked like it covered this: it builds repos with testkit's `tempRepo({ initialized: true })`, which writes `defaultConfig`, never init's overlay. The gap mattered disproportionately because that overlay is the sole opt-in point for the whole scheme — `defaultConfig` deliberately holds `bare` for back-compat, so if the overlay silently stopped writing `phase-qualified`, every fresh init would quietly get the bare scheme, the phase would become a no-op for new projects, and every existing test would stay green. It also illustrates the gate's own blind spot, which is worth recording in the phase that exists to sharpen it: the `AC-5` token already appeared in asserting blocks in three other files, so the coverage gate was fully satisfied while this clause went unproven.

As built: `fields.test.ts` pins the registry's exact field-name array (it was last updated for `coverageMode` in phase 108), so registering an eighth field necessarily fails it — the file is added to this task's set. This is the second ripple of the same class as T1's `tutorial/fixtures.ts`: adding a config field touches every place that enumerates or hand-builds the config, and the DRAFT's `files:` lines under-declared both. Recorded rather than silently absorbed.

### T6: Record the coverage scheme and mode in SUMMARY.json
- files: `packages/types/src/summary.ts`, `packages/core/src/services/settle.ts`, `packages/core/src/gates/ac-evidence.ts`, `packages/core/tests/summary-coverage-scheme.test.ts`
- action: add optional `coverageScheme`/`coverageMode` fields and populate them at settle. Do not touch `schemaVersion`; both fields are additive and optional. Amended after T3 (see T3's As built): ALSO rewire `deriveEvidenceAndCheckFloor` so per-AC evidence is derived under the resolved scheme rather than always from the bare memoized `ctx.coverage()` map. Recording the scheme in SUMMARY while deriving that SUMMARY's own evidence by bare matching would make the artifact self-contradictory, so the two halves ship together. `ac-evidence.ts` is declared here because the rewiring may need `deriveAcEvidence`'s inputs to change; `gates/coverage.ts` stays out of scope (T3 owns it)
- verify: a pre-existing SUMMARY with neither field still parses; a fresh settle records both; under `phase-qualified`, an AC whose ONLY reference is a foreign-phase bare token derives `unverified` evidence — including when that AC is listed as `explicit`
- done: AC-7
- depends: T1

### T7: Replay a qualified-scheme phase repo-wide by token
- files: `packages/core/src/verify/phase-replay.ts`, `packages/core/tests/verify/phase-replay-qualified.test.ts`, `packages/core/src/services/settle.ts`, `packages/core/src/services/verify.ts`
- action: when the SUMMARY records `phase-qualified`, scan repo-wide with that phase's qualifier instead of scoping to `draft.tasks[].files`, and skip the `no-scoped-files` refusal entirely — the qualifier makes scoping unnecessary
- verify: a qualified phase that declared no test files in its DRAFT still replays as covered
- done: AC-8
- depends: T2, T6

As built (fifth amendment, from T7's independent review — verdict FAIL, one Critical). Two changes to this task's scope, both recorded rather than silently absorbed.

(1) **"Repo-wide" was not repo-wide.** The first implementation passed no `globs` to `scanTestCoverage` under the qualified branch, which falls back to the engine's hardcoded `DEFAULT_GLOBS` (`packages/**/*.test.ts{,x}`) — so it ignored the DRAFT's files (intended) *and* `verification.testGlobs` (not intended). The bare branch never had that dependency: `globs: taskFiles` are literal declared paths that work for any repo layout. A consumer project that is not a pnpm monorepo, with `testGlobs: ["src/**/*.spec.ts"]`, would settle green through the gate (which scans the configured globs) and then have `cadence verify phase` match zero files and report every AC drifted — CI red on a healthy phase, with no recovery flag, since `PhaseReplayConfig` had no globs field. That is the false-drift failure this phase exists to remove, reintroduced in a new form, and it made AC-8's "across the whole repo" clause literally false. Fix: `PhaseReplayConfig` gains an optional `testGlobs`, threaded into the qualified branch only, defaulting to the engine defaults when absent (so nothing regresses before `services/verify.ts` wires it at T8). A test whose fixture test file lives **outside** `packages/` is required — none of the original five covered that, which is why a Critical shipped under a fully green suite.

(2) **`packages/core/src/services/settle.ts` is added to this task's set** for a one-line comment correction. T6's comment there states the field is "Deliberately NOT read back out of a parsed SUMMARY anywhere: it is provenance, not an input." T7 branches control flow on `summary.coverageScheme`, so it is now precisely an input, and a future agent could read that line, conclude the field is write-only, and safely "simplify" the emission — silently breaking every qualified replay. The correction belongs with the task that falsified it. This is the fifth boundary under-declaration of the same class this phase has produced: a change ripples into every place that describes or enumerates the thing being changed.

(3) **`packages/core/src/services/verify.ts` is added to this task's set, comment-only**, for a single `// phase 239 T8:` marker at the `replayPhaseCoverage` call site recording that `config.verification.testGlobs` must be threaded there. T7 cannot do the wiring — that file's logic belongs to T8 — but the second review established that nothing in T8's original action line would have forced it, and a T8 implementer editing that exact call site would have had no signal. The marker makes the code itself carry the obligation rather than only the DRAFT. Same precedent as (2): comment-only, declared rather than absorbed.

Deliberately NOT fixed here, and filed instead: the replay still takes `coverageMode` from the live config rather than `summary.coverageMode` (`rec-20260730-001`). A correct fix changes the **bare** path for any post-239 SUMMARY, which this task's boundary forbids, and fixing it only under the qualified branch would leave two schemes silently resolving the same question differently. The dedup hazard that cost this task a full review round-trip is filed as `rec-20260730-002`.

### T8: Report pre-scheme phases as indeterminate instead of drifting
- files: `packages/core/src/verify/phase-replay.ts`, `packages/core/src/services/verify.ts`, `packages/core/tests/verify/phase-replay-indeterminate.test.ts`
- action: add an `indeterminate` state to the per-AC replay result; when the SUMMARY records no scheme, mark every AC indeterminate with `drift: false` and state in the output that pre-scheme coverage is not phase-attributable. **ALSO MANDATORY (carried over from T7, see As built): thread `config.verification.testGlobs` into `replayPhaseCoverage`'s `testGlobs` at the `services/verify.ts` call site.** Without it T7's Critical fix has no production effect — `verify phase` still takes the `DEFAULT_GLOBS` branch for every real consumer
- verify: replaying phase 233 reports 5 indeterminate and 0 drift, where it currently reports 5 false drifts; AND a service-level test builds a repo whose tests live outside `packages/**` with `verification.testGlobs` matching them, and asserts `cadence verify phase` reports **zero** drift where it currently reports every AC drifted
- done: AC-8, AC-9
- depends: T7

As built (routed here from T7's second review, which made this a FAIL condition): T7 added the `testGlobs` parameter to `PhaseReplayConfig` and proved it works at the function level, but **could not wire it** — `services/verify.ts` is this task's file, not T7's. The consequence, measured end-to-end on a synthetic non-monorepo consumer (`testGlobs: ["src/**/*.spec.ts"]`, `coverageScheme: "phase-qualified"`, a genuinely-qualified asserting test at `src/foo.spec.ts`): `cadence verify coverage --explain AC-1` reports `SATISFIED` / exit 0 while `cadence verify phase` reports `drift: true`, `driftCount: 1` / exit 1 — **the gate green and the replay red, same repo, same commit.** That is verbatim the Critical from T7's first review, still live on this branch.

`done:` gains AC-8 because this wiring is what finally makes AC-8 true of the shipped command rather than only of the exported function. Two forcing devices exist so this cannot be quietly skipped: the `verify:` clause above requires a service-level test that fails today, and a `// phase 239 T8:` marker comment sits at the `services/verify.ts` call site. **This repo is itself a monorepo, so its own T10 dogfooding can never surface this failure** — the only thing standing between it and a shipped regression is this task actually doing the wiring.

### T9: Pair the docs and add the changeset
- files: `docs/concepts.md`, `docs/reference/config.md`, `docs/reference/commands.md`, `packages/core/tests/docs/coverage-scheme-docs.test.ts`, `.changeset/coverage-phase-scoping.md`
- action: update the `test-coverage` gate row and the config field table, add a doc test pinning the pairing, and write a changeset covering core + types
- verify: the doc-content test surface passes
- done: AC-10
- depends: T3, T8

As built: `docs/reference/commands.md` is added to this task's set, routed here from T7's review because this task already owns the doc pairing. T7 changed shipped `verify phase` behavior and left three now-false unconditional statements behind: `:2309-2311` ("Coverage re-scanning is deliberately scoped to the file paths the phase's own DRAFT declared … never a whole-repo scan"), `:2315-2316` ("A DRAFT that declares no task files refuses outright rather than silently widening the scan"), and the exit-code-2 list at `:2338-2341`, which presents `no-scoped-files` as a hard error. All three are now true only under the bare scheme. The operator-visible failure is concrete: someone on a `phase-qualified` project reads the exit-code table and designs CI around a refusal that can never fire. Note for whoever picks this up — the review could not find an executable doc-content test covering this prose, and the full suite is green *with* the drift present, so the doc test added by this task should extend to cover it rather than assuming the existing surface does.

### T10: Flip this repo onto the qualified scheme and prove it at settle
- files: `.cadence/config.json`
- action: set this repo's `verification.coverageScheme` to `phase-qualified` so this phase's own settle enforces the new rule; every test written in T1–T9 must carry the `239-01/AC-N` form
- verify: `settle run --auto` passes with all ten ACs covered under the qualified scheme, run via `node packages/core/bin/cadence.cjs`
- done: AC-10
- depends: T1, T2, T3, T4, T5, T6, T7, T8, T9

## Boundaries

- DO NOT change `AC_TOKEN_RE` or the bare-token lexing path — AC-6 depends on the embedded token staying lexable, and every existing scanner consumes it.
- DO NOT migrate historical tests to the qualified form. The gate only evaluates the phase being settled, and pre-scheme phases are reported indeterminate by design.
- DO NOT attempt to compute a real coverage verdict for a pre-scheme phase. The evidence is not phase-attributable; asserting a verdict is the bug being fixed.
- DO NOT change the Zod-level default away from `bare` — that is the back-compat contract for every consumer project on upgrade.
- DO NOT bump `SUMMARY.schemaVersion`; the kernel-assurance arc owns that bump and a second one here would collide at merge.
- DO NOT touch the criteria-anchoring surface (`verify/anchor.ts`, `verify/criteria-gap.ts`, `gates/code-review.ts`) — it lives on the arc branch, not here.
- DO NOT add a runtime dependency.
