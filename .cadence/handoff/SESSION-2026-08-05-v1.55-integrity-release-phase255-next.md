---
cadence_handoff: 1
generated_at: 2026-08-05T04:29:40.413Z
label: v1.55-integrity-release-phase255-next
loop_position: IDLE
active_phase: 252-self-application-config-correction
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 6a63a61a
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-08-05 (v1.55-integrity-release-phase255-next)

## TL;DR for the next session
- **Phase 254 (security advisory remediation) shipped**: PR #374 merged (squash commit `c66fd27c`). Both ACs pass, CI fully green (10/10 including the 3-OS matrix and `ci-success`).
- Retired the dead `GHSA-mh99-v99m-4gvg` (brace-expansion) exception row; re-justified the 3 exceptions expiring 2026-08-13 (vitest/vite/postcss) with fresh reachability analysis, expiry extended to 2026-11-02; recorded the vitest 2→4 major upgrade as a named deferred blocker rather than reviving stale PR #235 (closed 2026-08-04, premised on the pnpm-overrides-broken narrative phase 253 disproved); captured the ip-address moderate-advisory evidence.
- This primary checkout's own `.cadence/state.json` still shows `activePhase: 252-...` / `loopPosition: IDLE` — stale/uninformative for phase 254's status; phase 254's entire DRAFT→BUILD→SETTLE lifecycle ran in a **separate worktree's private `.cadence/`** (`.claude/worktrees/254-security-advisory-remediation`, now fully removed post-merge). Trust git history (PR #374 / commit `c66fd27c`) over local state for phase 254.
- **Next action: scope and scaffold Phase 255 (make security merge-blocking)**, sourced from `rec-20260804-006` (still open, `candidate/ready-for-cadence-spec`), per the v1.55 integrity-release plan file. Must land after 254 (now done). No hard per-phase deadline beyond the overall v1.55 release cadence.
- One low-priority note: `rec-20260803-003` (3 undocumented high-severity advisories) is now moot as a side effect of phase 253's override refresh — evidence recorded on the rec this session, status deliberately left `candidate/needs-evidence` for the operator to close (it wasn't in either phase 253 or 254's own DRAFT scope).
- The `253-dependency-override-remediation` worktree is still present on disk (unresolved from the prior handoff, deferred by the operator, not touched this session) — see gotchas before touching it.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `6a63a61a`
- Recent commits:
```
6a63a61a chore(cadence): stamp session handoff — v1.55-integrity-release-phase254-next
ea20600c chore(cadence): stamp session handoff — v1.55-integrity-release-phase252-next
c66fd27c fix: security advisory remediation (phase 254) (#374)
0b371063 fix: dependency-override remediation — refresh stale pnpm.overrides targets + add drift detector (phase 253) (#373)
cf7f4a80 fix: raise self-application evidenceFloor to assertion + defer baseline profile decision (phase 252) (#372)
d8673700 chore(cadence): file scout-20260804-integrity-release recs + track v1.55/v1.56 handoffs (#371)
e218206e chore(cadence): file rec-20260803-003 -- undocumented audit advisories on Security workflow (#369)
d19251b7 chore(cadence): sync local main handoff stamps into origin (#368)
```
- Uncommitted (diff --stat):
```
.claude/scheduled_tasks.lock | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```
- Loop: IDLE · phase 252-self-application-config-correction · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260727-012 — cadence doctor check: roadmap-currency (anti-recurrence for ROADMAP/MILESTONES drift) (candidate/ready-for-cadence-spec)
  - rec-20260804-006 — ci-success does not aggregate Security/CodeQL, and they are not required checks by any other route (candidate/ready-for-cadence-spec)
  - rec-20260731-001 — cadence doctor: release-currency check (local package.json vs published npm) (candidate/ready-for-milestone)
  - rec-20260801-001 — docs/reference/commands.md config edit section lists only 5 fields; EDITABLE_FIELDS has 8 (candidate/ready-for-cadence-spec)
  - rec-20260729-004 — test-coverage gate's repo-wide AC-N token scan collides across phases, so any AC can be satisfied by an unrelated phase's tests (candidate/needs-decision)
- Open assumptions:
  - (none)
- Active decisions:
  - dec-20260711-001 — Multi-language assertion-coverage: fast diagnose-fix now, shared-lexer engine as a later phase
  - dec-20260721-001 — cadence next extends nextAction(), does not subsume quickstart or reimplement
  - dec-20260721-002 — Shared legal-moves computation also powers empty-state footers (rec-20260721-001)
  - dec-20260721-003 — cadence next --json includes schemaVersion: 1
  - dec-20260721-004 — Ship /cadence-next slash command alongside the CLI command
  - dec-20260724-001 — Enforce ledger-diff at audit close, not a standing rule
  - dec-20260724-002 — Scope rec-20260724-003 to a CHANGELOG-currency gate only, defer auto-generation
  - dec-20260726-001 — Split SUMMARY.json attestation: content-hash now, full signing deferred to threat model
  - dec-20260730-001 — Coverage phase-scoping uses a phase-qualified test token, not file-ownership scoping
  - dec-20260728-001 — Phase 233 AC-3 tripwire cleared: assurance-record derivation is gate-agnostic
  - dec-20260729-001 — Phase 234 AC-1 narrowed: contracts/ is the type-naming surface, not the resolution surface
  - dec-20260729-002 — Uniform opts? on VerifierPort is what makes zero-special-cases true
  - dec-20260729-003 — Phase 235 scope: criteria-anchoring is code-review only, not spec-review/ui-spec-review/plan-review
  - dec-20260729-004 — Anchor executable tier: non-empty verify + build-test-must-pass ran, no prose heuristic
  - dec-20260729-005 — Criteria-gap refusal reuses code-review's existing HIGH-severity refuse path, not gates.evidenceFloor
  - dec-20260729-006 — D3 unconditional declaration binds the floor outcome, not the empty-gap case
  - dec-20260731-001 — Findings-to-ledger routing merges same-identity findings by design; the identity hash itself is not changed
  - dec-20260801-001 — Add a settle-time guard for global-CLI-shadowing-branch-build; interim rule is settle via the local build
  - dec-20260801-002 — Finding identity narrowed to (file, normalized message); anchor/severity dropped as identity inputs
  - dec-20260801-003 — Defer finding-identity message-drift dedup: wait for real-provider data, offline analyzer first
  - dec-20260802-001 — Refused gate-loop settles thread acc's findings into the SUMMARY, with a conditional contentHash
  - dec-20260802-002 — Attempt preservation via timestamp-slugged sibling artifact, invisible to all current SUMMARY consumers by construction
  - dec-20260802-003 — Ledger routing stays finalize-only on refusal; Slice 3's revisit trigger amended to name its precondition
  - dec-20260803-001 — Conduction stays operator-initiated: guard and gate set retained; mock-provider default is a separate ordinary config decision
  - dec-20260804-001 — Defer baseline profile change to v1.56 Phase P
- Files in play:
  - `.cadence/ROADMAP.md` — affected by rec-20260727-012 cadence doctor check: roadmap-currency (anti-recurrence for ROADMAP/MILESTONES drift)
  - `packages/core/src/cli/commands/doctor/checks/roadmap-currency.ts` — affected by rec-20260727-012 cadence doctor check: roadmap-currency (anti-recurrence for ROADMAP/MILESTONES drift)
  - `packages/core/src/cli/commands/doctor/registry.ts` — affected by rec-20260727-012 cadence doctor check: roadmap-currency (anti-recurrence for ROADMAP/MILESTONES drift)
  - `.github/workflows/ci.yml` — affected by rec-20260804-006 ci-success does not aggregate Security/CodeQL, and they are not required checks by any other route
  - `.github/workflows/security.yml` — affected by rec-20260804-006 ci-success does not aggregate Security/CodeQL, and they are not required checks by any other route
  - `.github/workflows/codeql.yml` — affected by rec-20260804-006 ci-success does not aggregate Security/CodeQL, and they are not required checks by any other route
  - `packages/core/src/doctor/run.ts` — affected by rec-20260731-001 cadence doctor: release-currency check (local package.json vs published npm)
  - `.githooks/pre-push` — affected by rec-20260731-001 cadence doctor: release-currency check (local package.json vs published npm)
  - `docs/reference/commands.md` — affected by rec-20260801-001 docs/reference/commands.md config edit section lists only 5 fields; EDITABLE_FIELDS has 8
  - `packages/core/src/config-edit/fields.ts` — affected by rec-20260801-001 docs/reference/commands.md config edit section lists only 5 fields; EDITABLE_FIELDS has 8
  - `packages/core/src/verify/coverage.ts` — affected by rec-20260729-004 test-coverage gate's repo-wide AC-N token scan collides across phases, so any AC can be satisfied by an unrelated phase's tests
  - `packages/core/src/gates/coverage.ts` — affected by rec-20260729-004 test-coverage gate's repo-wide AC-N token scan collides across phases, so any AC can be satisfied by an unrelated phase's tests

## What landed this session
- Phase 254 (security advisory remediation) fully built, reviewed, settled, and merged as PR #374 (`c66fd27c`) — 3 tasks (T1–T3), one implementer + one independent adversarial reviewer per task, then a whole-branch review that caught and required fixing 2 coverage-gate defects before settle: AC-2 had zero test coverage, and AC-1's own header comment accidentally shadowed its real assertion via the coverage scanner's first-qualified-occurrence dedup (fixed with properly spaced vs. hard-adjacent qualifier tokens).
- Corrected a factual overclaim inherited from the pre-254 doc: the vitest advisory itself patches at `>=3.2.6`, but PR #235's own (unmerged) investigation found that floor still resolves a vulnerable vite — the real target for the full vitest/vite/postcss chain is `>=4.1.10`.
- Added evidence to `rec-20260803-003` noting it's now moot (phase 253 side effect); left its status alone for the operator.
- Post-merge sync completed: local `main` rebased cleanly onto the new `origin/main` tip (used `git rebase --autostash` to work around the live `.claude/scheduled_tasks.lock` file, which this session's harness keeps rewriting); phase 254 worktree and its local branch fully removed; remote branch explicitly deleted after `gh pr merge --delete-branch`'s local post-merge checkout step failed on the known "main already checked out in another worktree" issue (the remote squash-merge itself succeeded regardless — verified via `gh pr view`).

## Carry-forward gotchas
- **`.claude/worktrees/253-dependency-override-remediation` is still present on disk, unresolved.** Same status as the prior handoff: its branch was squash-merged and the remote deleted, so it has no further purpose, but the operator was asked whether to force-clean it up and deferred — not touched again this session. Re-check liveness before removing (process CPU time not advancing across a short sleep, no file writes for several minutes), or wait for the operator to say so explicitly.
- The other two sibling worktrees (`kernel-arc-docs-review` on stale branch `feat/kernel-assurance-v2`, `phase249-refused-settle-post-gate`) remain confirmed dead/unlocked, untouched, tracked under `rec-20260804-004` — no new information this session.
- **Settling from within a worktree via the global `cadence` binary on PATH still resolves to the MAIN checkout's dist, not the worktree's own local build** — even after `pnpm build` was run inside the worktree. This printed a loud `SETTLING VIA A FOREIGN CADENCE BINARY` warning and, unlike the historical phases 233/234 incident, did **not** silently downgrade: `254-01-SUMMARY.json` still came out `schemaVersion: 2` with full `assurance` data, plus a new `foreignBinaryMismatch` provenance field recording the binary-path mismatch. This failure mode appears to have been hardened since dec-20260801-001, but still prefer the explicit local build (`node packages/core/bin/cadence.cjs settle run --auto`) to avoid the warning and any residual risk.
- `.claude/scheduled_tasks.lock` shows as locally modified in `git status` and **cannot be `git stash`ed durably** — this session's harness rewrites it live on every `ScheduleWakeup` call, so a plain `git stash push` gets silently re-dirtied seconds later. Use `git rebase --autostash` (or equivalent) for any operation that needs a clean tree; never stage or commit this file.
- Local `main` again has two unpushed handoff-stamp commits (this one plus the prior `v1.55-integrity-release-phase252-next` one, both rebased forward onto phase 254's merge) — kept local by design ("push only when switching machines"), don't push without asking first.
- `cadence recommendation evidence add` still cannot target a `rejected`/`shipped` rec (same gap noted in the prior handoff) — not hit this session since `rec-20260803-003` is still `candidate`, but still logged as a known gap on `rec-20260804-003`.

## Next action
**Action:** Read the v1.55 integrity-release plan file's "Phase 255 — make security merge-blocking" section in full
(`/home/thomas/.claude/plans/read-docs-handoffs-handoff-v1-55-integri-tidy-kernighan.md`, around line 331). It's
sourced from **Rec G** (`rec-20260804-006`, still open, `candidate/ready-for-cadence-spec`) and must land after
phase 254 (now done). Template: `.github/workflows/ci.yml:47-58`'s `ci-success` job — the only `if: always()` +
explicit-result-check aggregation pattern in the repo. String-compare each job's `.result` to `"success"`; do not
rely on `needs` short-circuiting (this is also what makes a *skipped* job correctly fail). Decide between extending
`ci-success`'s own `needs` to include Security/CodeQL (no branch-protection change needed, couples workflows) vs. a
separate `security-success` required context (cleaner, but needs an **operator** action on GitHub branch protection
settings — name it explicitly as a blocker in the DRAFT if chosen). Test precedent: extend
`packages/core/tests/docs/security-ci.test.ts` rather than inventing a new file. Show both a deliberately-failing
and a deliberately-skipped security job failing the required check (the plan's own stated bar). Then scaffold:
`cadence draft new 255-<title-slug> --tier <per scope>`, isolated in a fresh worktree branched from `origin/main`.
**Verify:** `cadence draft check` passes; the DRAFT reflects Rec G's scope; if the "separate required context" path
is chosen, the DRAFT explicitly names the GitHub branch-protection-settings operator action as a named blocker/task
rather than silently assuming it will happen.
**If it fails:** if the two design options (extend `ci-success`'s `needs` vs. a new required context) both seem
viable with no clear mechanical tiebreaker, surface the tradeoff to the operator rather than picking unilaterally —
per CLAUDE.md's model-selection guidance this is exactly the "two architectural approaches, tiebreaker isn't
mechanical" case that should go through the Opus advisor before committing to the DRAFT's task breakdown.
