---
cadence_handoff: 1
generated_at: 2026-07-24T20:32:45.688Z
label: next-rec-20260724-005
loop_position: IDLE
active_phase: 
active_draft: 
tier: 
git_branch: main
git_dirty: false
git_head: d59410a3
git_ahead: 4
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-07-24 (next-rec-20260724-005)

## TL;DR for the next session
- This session synced local `main` with origin, ran `cadence recommend`, and shipped **phase 215-p0-escape-retro-ledger-diff** (rec-20260724-002 — "The Unlogged Audit Finding" entry in `CLAUDE.md` + a doc-content test), landed as **PR #295**, squash-merged, all CI green.
- Loop is IDLE, nothing in flight. Local `main` is 4 ahead / 0 behind origin — all handoff/chore bookkeeping commits, intentionally left unpushed (push only when switching machines).
- **Next session should start `rec-20260724-005`** — "Close the trust envelope: gate the SETTLE capability class in MCP serve." It's top-ranked (score 68), `ready-for-milestone`, priority high, and security-relevant: `mcp-trust.ts` classifies SETTLE as a capability class but a source comment says it's left ungated; the def-hash-bound grant/revoke-on-version-change/expiry machinery already exists and gates `APPROVAL_BYPASS`, so extending it to SETTLE is the remaining step.
- No blockers. `rec-20260724-005` is already `ready-for-milestone` (no `needs-decision` gate to resolve first, unlike rec-20260724-002 was) — should be able to go straight to `cadence milestone propose` after `recommendation promote rec-20260724-005 --status accepted`.
- Two real cross-session hazards were hit and written up in cross-tool memory this session (see gotchas below) — worth a skim before the next `git rebase origin/main`.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (clean), 4 ahead / 0 behind origin
- HEAD `d59410a3`
- Recent commits:
```
d59410a3 chore(cadence): stamp session handoff — 2026-07-24 (phase 215 shipped)
fef5764d chore(cadence): stamp session handoff — 2026-07-24 (phase 213 shipped)
62aa5a33 chore(cadence): stamp session handoff — 2026-07-24
b0791bc3 chore(cadence): stamp session handoff — 2026-07-24
df621ef9 docs: audit sessions ledger-diff findings before closing (phase 215-p0-escape-retro-ledger-diff) (rec-20260724-002) (#295)
1cf84ce5 chore(security): document postcss audit exception (GHSA-r28c-9q8g-f849) (#294)
a24506d9 feat: minimum-evidence floor gate for settle (phase 214-evidence-floor-gate) (rec-20260724-001) (#293)
54511099 chore(cadence): mark rec-20260712-014 shipped (PR #291 / phase 213) (#292)
```
- Loop: IDLE · phase (none) · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260724-005 — Close the trust envelope: gate the SETTLE capability class in MCP serve (candidate/ready-for-milestone)
  - rec-20260724-003 — Generate CHANGELOG entries from settle artifacts and gate releases on changelog currency (candidate/needs-decision)
  - rec-20260724-004 — Refresh .cadence/ROADMAP.md or formally deprecate it in favor of milestones plus ledger (candidate/needs-decision)
  - rec-20260724-006 — Signed or tamper-evident SUMMARY attestations (candidate/needs-decision)
  - rec-20260724-007 — Define and document multi-contributor concurrency semantics for .cadence state (candidate/needs-evidence)
- Open assumptions:
  - (none)
- Active decisions:
  - dec-20260711-001 — Multi-language assertion-coverage: fast diagnose-fix now, shared-lexer engine as a later phase
  - dec-20260721-001 — cadence next extends nextAction(), does not subsume quickstart or reimplement
  - dec-20260721-002 — Shared legal-moves computation also powers empty-state footers (rec-20260721-001)
  - dec-20260721-003 — cadence next --json includes schemaVersion: 1
  - dec-20260721-004 — Ship /cadence-next slash command alongside the CLI command
  - dec-20260724-001 — Enforce ledger-diff at audit close, not a standing rule
- Files in play:
  - `packages/types/src/mcp-trust.ts` — affected by rec-20260724-005 Close the trust envelope: gate the SETTLE capability class in MCP serve
  - `packages/core/src/mcp/tools.ts` — affected by rec-20260724-005 Close the trust envelope: gate the SETTLE capability class in MCP serve
  - `CHANGELOG.md` — affected by rec-20260724-003 Generate CHANGELOG entries from settle artifacts and gate releases on changelog currency
  - `.github/workflows/release.yml` — affected by rec-20260724-003 Generate CHANGELOG entries from settle artifacts and gate releases on changelog currency
  - `.cadence/ROADMAP.md` — affected by rec-20260724-004 Refresh .cadence/ROADMAP.md or formally deprecate it in favor of milestones plus ledger
  - `packages/types/src/summary.ts` — affected by rec-20260724-006 Signed or tamper-evident SUMMARY attestations
  - `packages/core/src/services/settle.ts` — affected by rec-20260724-006 Signed or tamper-evident SUMMARY attestations
  - `docs/team-rollout.md` — affected by rec-20260724-007 Define and document multi-contributor concurrency semantics for .cadence state

## What landed this session
- **PR #295** (merged, squash, `df621ef`): `CLAUDE.md` gains "The Unlogged Audit Finding" — a named-failure-mode entry requiring audit sessions to ledger-diff critical/P0 findings against `recommendations.json` before closing. Backed by `packages/core/tests/docs/audit-ledger-diff.test.ts`.
- `rec-20260724-002` shipped (ref: `PR #295`); `dec-20260724-001` recorded the mechanism decision.
- Local `main` rebase-synced with origin twice (once pre-phase, once post-merge), resolving two real `rec-`/`ev-` id collisions along the way (see gotchas).

## Carry-forward gotchas
- **A rebase conflict in `.cadence/intelligence/{recommendations,evidence}.json` can be a genuine same-day id collision, not cosmetic JSON noise** — two sessions can independently mint the identical `rec-`/`ev-` id before either pushes. Don't hand-splice conflict markers: diff `ours`/`theirs` against the merge-base for each side's new ids; if disjoint, take the fuller side wholesale (`git checkout --ours` during a rebase = the branch being rebased *onto*) and re-file the dropped side fresh via `cadence recommendation add` so the CLI assigns a genuinely free id. Written up as `cadence-rec-id-collision-on-rebase` in cross-tool memory.
- **A fresh worktree/clone has no `state.json`** (gitignored, never checked out) — every `cadence` command refuses "not initialized" even though `.cadence/` is fully real. Run `cadence onboard` first (safe on an existing `.cadence/` dir, unlike `cadence init` which refuses).
- **`milestone` eligibility requires `status=accepted` AND `readiness` in `{ready-for-milestone, ready-for-cadence-spec}`** — promoting only readiness isn't enough; `cadence milestone propose` names the exact missing status when it refuses.
- **`milestone propose/accept/export` and `recommendation promote`/`decision add` genuinely mutate the ledger even on the primary checkout** — a fresh worktree branches from `origin/main` and won't see them. Run these inside the worktree, not before `EnterWorktree`.
- GitHub had a real ~15min platform-wide incident on Pull Requests (2026-07-24, 19:37-19:56 UTC, confirmed via githubstatus.com) — both `gh pr create` and raw REST `POST /pulls` returned bare HTTP 500s. If `gh pr create` mysteriously fails again, check githubstatus.com before debugging the request.
- `gh pr merge --squash --delete-branch` again hit the known local-checkout-failure pattern (`'main' is already used by worktree...`); remote merge succeeds regardless — verify via `gh pr view --json state,mergedAt,mergeCommit`. The remote branch also didn't auto-delete this time; clean up manually with `git push origin --delete <branch>` if it recurs.

## Next action
**Action:** `cadence recommendation promote rec-20260724-005 --status accepted` (readiness is already `ready-for-milestone`), then `cadence milestone propose` inside a fresh worktree, and continue the loop (SPEC → DRAFT → BUILD → settle) same as phase 215. The work itself: extend the existing def-hash-bound grant/revoke-on-version-change/expiry envelope machinery in `packages/types/src/mcp-trust.ts` / `packages/core/src/mcp/tools.ts` (which already gates `APPROVAL_BYPASS`) to also cover the `SETTLE` capability class.
**Verify:** `rec-20260724-005` should show as `shipped`, not in the ranked/candidate list, once landed.
**If it fails:** if `milestone propose` still refuses after the promote, re-run `cadence recommendation show rec-20260724-005` to confirm both `status` and `readiness` cleared the eligibility bar named in the refusal message.
