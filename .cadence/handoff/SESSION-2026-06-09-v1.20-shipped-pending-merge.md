---
cadence_handoff: 1
generated_at: 2026-06-09T04:59:48.978Z
label: v1.20-shipped-pending-merge
loop_position: IDLE
active_phase: 90-release-v1.20.0
active_draft: 
tier: 
git_branch: milestone/v1
git_dirty: true
git_head: db37ae8
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-06-09 (v1.20-shipped-pending-merge)

## TL;DR for the next session
- v1.20 (handoff-retention) was built end-to-end this session — scope → phases 88/89/90 → release prep — and is **pushed**. PR **#62 → main is fully green** (ci-success + all 6 test legs + build). Loop IDLE.
- **Single next action: merge PR #62, then trigger the Release workflow** (`release.yml`) to publish `1.20.0` + tag `v1.20.0`.
- **Blocker for the agent only:** `gh pr merge 62` was **denied by Claude Code's auto-approval classifier** (merge-to-`main` isn't auto-approvable). The user must run the merge + release (or grant permission). Nothing is wrong with the PR.
- Branch `milestone/v1.20-handoff-retention` (7 commits); all four published packages already at `1.20.0` on the branch.
- After publish: update the launch-positioning memory to npm latest = `1.20.0` — **deliberately not done yet** (held until actually live, per honest-claims preference).

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `milestone/v1.20-handoff-retention` (dirty), 0 ahead / 0 behind origin _(cadence truncated this to `milestone/v1` at the dot)_
- HEAD `db37ae8`
- Recent commits:
```
db37ae8 chore: settle 90-01 — release v1.20.0
2852c55 chore(release): version 1.20.0
30090fe chore: settle 89-01 — doctor handoff-retention check
b179e95 feat(doctor): handoff-retention check — visibility for SESSION-doc growth (phase 89)
44bd18e chore: settle 88-01 — handoff retention core + wiring
808b058 feat(handoff): opt-in count-based SESSION retention (phase 88)
79f8c59 docs(planning): scope v1.20 — handoff retention milestone
44f7d75 Merge pull request #61 from manehorizons/chore/v1.19-housekeeping
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```
- Loop: IDLE · phase 90-release-v1.20.0 · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - (none)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - (none)

## What landed this session
- Pruned a stale phase-85 SESSION handoff doc; confirmed v1.19.0 shipped + clean.
- Scoped v1.20 (handoff-retention) in `MILESTONES.md` (`79f8c59`), from rec-20260608-001.
- **Phase 88** — `handoff.retain` config (cadence-types) + pure `selectPrunable` + best-effort prune-on-write in `runHandoff` + `pruned` reporting on CLI/service. TDD.
- **Phase 89** — read-only `cadence doctor` `handoff-retention` check. TDD.
- **Phase 90** — release prep: `config.md` `handoff` section, changeset, lockstep `1.19.0 → 1.20.0` across all four published packages, CLAUDE.md version note.
- Pushed branch, opened PR #62, watched CI to green. Full gate green throughout (20/20 turbo, 1556 core tests).

## Carry-forward gotchas
- **Merge is agent-gated:** Claude Code's auto-mode classifier denied `gh pr merge 62`. User merges via GitHub UI or `! gh pr merge 62 --merge`, then `! gh workflow run release.yml`.
- The **Release workflow publishes whatever versions are on `main`** and tags from `core/package.json` — it does **not** run `changeset version` (already done locally; changeset consumed). So merge-then-dispatch is correct — do **not** re-bump versions.
- `release.yml` `dry_run` defaults **false** = real publish. Use `-f dry_run=true` for a pack-only validation first. Publish + tag steps are idempotent (skip already-published versions / existing tag).
- Phase-90 settle used `--allow-missing-coverage` (release phase has no test-backed ACs) → emitted a `coverage-bypassed` anomaly. Expected, not a failure.
- Uncommitted `.cadence/STATE.md` + `state.json` are **ephemeral telemetry** (a `subagentSpawns` counter bump) — not real work; safe to leave or discard. Do not `git restore state.json` mid-loop as a habit, but here (IDLE) it's harmless.
- The repo's own `.cadence/config.json` has **no `handoff.retain`** set, so this very handoff did not self-prune — opt-in, by design (eat-your-own-dogfood would be to set it, but that's a separate choice).

## Next action
**Action:** Merge PR #62 into `main`, then trigger the Release workflow:
```bash
gh pr merge 62 --merge
gh workflow run release.yml          # dry_run defaults false → real publish + v1.20.0 tag
```
**Verify:** `gh run list --workflow=release.yml` shows a successful run; `npm view @manehorizons/cadence-core version` → `1.20.0`; `git ls-remote --tags origin v1.20.0` shows the tag.
**If it fails:** `gh run view --log` on the release run. A transient npm/CI blip is safe to re-run (idempotent). If CI on `main` is red, fix forward via a branch + PR (ci-success is admin-enforced). Then update the launch-positioning memory to npm latest = `1.20.0`.
