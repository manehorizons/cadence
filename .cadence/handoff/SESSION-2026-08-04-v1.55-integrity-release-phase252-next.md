---
cadence_handoff: 1
generated_at: 2026-08-04T21:58:25.239Z
label: v1.55-integrity-release-phase252-next
loop_position: IDLE
active_phase: 246-finding-identity-message-drift
active_draft: 
tier: 
git_branch: main
git_dirty: false
git_head: d8673700
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-08-04 (v1.55-integrity-release-phase252-next)

## TL;DR for the next session
- v1.55.0 "integrity release" is planned and approved: full execution plan lives at `/home/thomas/.claude/plans/read-docs-handoffs-handoff-v1-55-integri-tidy-kernighan.md` — **read it in full before continuing**, it has the corrected AC/task breakdowns for every phase.
- Verification overturned the source handoff's Phase A premise: pnpm's `pnpm.overrides` are NOT broken (git history proves they apply); the real gap was stale override targets + no drift detector. Phase A's scope is corrected accordingly (now Phase 253).
- Step 0 housekeeping is done and merged (PR #371): 6 recs filed (`rec-20260804-001`..`006`), 4 previously-untracked handoff docs now tracked, one real CI regression found+fixed along the way (see gotchas).
- Phase 252's scope was narrowed after a decision-consistency check: it will NOT change the baseline `profile` (that's deferred to v1.56 Phase P per a decision still to be recorded) — only bumps `gates.evidenceFloor` and records the deferral decision.
- **Single hard deadline: Phase 254 (security advisories) must merge by 2026-08-12** — 3 exceptions (vitest/vite/postcss) expire 2026-08-13.
- Next action: scaffold Phase 252 (`cadence draft new`), per the plan file's Phase 252 section.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (clean), 0 ahead / 0 behind origin
- HEAD `d8673700`
- Recent commits:
```
d8673700 chore(cadence): file scout-20260804-integrity-release recs + track v1.55/v1.56 handoffs (#371)
e218206e chore(cadence): file rec-20260803-003 -- undocumented audit advisories on Security workflow (#369)
d19251b7 chore(cadence): sync local main handoff stamps into origin (#368)
1c18b6f6 chore: sync session handoffs + merge phase 251 into main (#367)
c8333f8c feat: conduction reachability check + finding-durability arc close-out (phase 251) (#366)
2926944b chore(cadence): file rec-20260802-006 -- website/ has no security audit CI coverage (#365)
c74155b1 fix(security): sync website lockfile off vulnerable brace-expansion/js-yaml (#364)
c71c12d0 chore(release): v1.54.0 -- npm scope rename + post-gate refusal durability fixes (#363)
```
- Loop: IDLE · phase 246-finding-identity-message-drift · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260727-012 — cadence doctor check: roadmap-currency (anti-recurrence for ROADMAP/MILESTONES drift) (candidate/ready-for-cadence-spec)
  - rec-20260804-001 — Nothing detects a pnpm override whose target has gone stale or whose key no longer matches (candidate/ready-for-cadence-spec)
  - rec-20260804-006 — ci-success does not aggregate Security/CodeQL, and they are not required checks by any other route (candidate/ready-for-cadence-spec)
  - rec-20260731-001 — cadence doctor: release-currency check (local package.json vs published npm) (candidate/ready-for-milestone)
  - rec-20260801-001 — docs/reference/commands.md config edit section lists only 5 fields; EDITABLE_FIELDS has 8 (candidate/ready-for-cadence-spec)
- Open assumptions:
  - (none)
- Active decisions:
  - dec-20260711-001 — Multi-language assertion-coverage: fast diagnose-fix now, shared-lexer engine as a later phase
  - dec-20260721-001 — cadence next extends nextAction(), does not subsume quickstart or reimplement
  - dec-20260721-002 — Shared legal-moves computation also powers empty-state footers (rec-20260721-001)
  - dec-20260721-003 — cadence next --json includes schemaVersion: 1
  - dec-20260721-004 — Ship /cadence-next slash command alongside the CLI command
  - dec-20260724-001 — Enforce ledger-diff at audit close, not a standing rule
  - dec-20260724-002 — Scope rec-20260724-003 to a CHANGELOG-currency gate only, defer auto-generation
  - dec-20260726-001 — Split SUMMARY.json attestation: content-hash now, full signing deferred to threat model
  - dec-20260730-001 — Coverage phase-scoping uses a phase-qualified test token, not file-ownership scoping
  - dec-20260728-001 — Phase 233 AC-3 tripwire cleared: assurance-record derivation is gate-agnostic
  - dec-20260729-001 — Phase 234 AC-1 narrowed: contracts/ is the type-naming surface, not the resolution surface
  - dec-20260729-002 — Uniform opts? on VerifierPort is what makes zero-special-cases true
  - dec-20260729-003 — Phase 235 scope: criteria-anchoring is code-review only, not spec-review/ui-spec-review/plan-review
  - dec-20260729-004 — Anchor executable tier: non-empty verify + build-test-must-pass ran, no prose heuristic
  - dec-20260729-005 — Criteria-gap refusal reuses code-review's existing HIGH-severity refuse path, not gates.evidenceFloor
  - dec-20260729-006 — D3 unconditional declaration binds the floor outcome, not the empty-gap case
  - dec-20260731-001 — Findings-to-ledger routing merges same-identity findings by design; the identity hash itself is not changed
  - dec-20260801-001 — Add a settle-time guard for global-CLI-shadowing-branch-build; interim rule is settle via the local build
  - dec-20260801-002 — Finding identity narrowed to (file, normalized message); anchor/severity dropped as identity inputs
  - dec-20260801-003 — Defer finding-identity message-drift dedup: wait for real-provider data, offline analyzer first
  - dec-20260802-001 — Refused gate-loop settles thread acc's findings into the SUMMARY, with a conditional contentHash
  - dec-20260802-002 — Attempt preservation via timestamp-slugged sibling artifact, invisible to all current SUMMARY consumers by construction
  - dec-20260802-003 — Ledger routing stays finalize-only on refusal; Slice 3's revisit trigger amended to name its precondition
  - dec-20260803-001 — Conduction stays operator-initiated: guard and gate set retained; mock-provider default is a separate ordinary config decision
- Files in play:
  - `.cadence/ROADMAP.md` — affected by rec-20260727-012 cadence doctor check: roadmap-currency (anti-recurrence for ROADMAP/MILESTONES drift)
  - `packages/core/src/cli/commands/doctor/checks/roadmap-currency.ts` — affected by rec-20260727-012 cadence doctor check: roadmap-currency (anti-recurrence for ROADMAP/MILESTONES drift)
  - `packages/core/src/cli/commands/doctor/registry.ts` — affected by rec-20260727-012 cadence doctor check: roadmap-currency (anti-recurrence for ROADMAP/MILESTONES drift)
  - `package.json` — affected by rec-20260804-001 Nothing detects a pnpm override whose target has gone stale or whose key no longer matches
  - `pnpm-lock.yaml` — affected by rec-20260804-001 Nothing detects a pnpm override whose target has gone stale or whose key no longer matches
  - `scripts/` — affected by rec-20260804-001 Nothing detects a pnpm override whose target has gone stale or whose key no longer matches
  - `packages/core/tests/docs/` — affected by rec-20260804-001 Nothing detects a pnpm override whose target has gone stale or whose key no longer matches
  - `.github/workflows/ci.yml` — affected by rec-20260804-006 ci-success does not aggregate Security/CodeQL, and they are not required checks by any other route
  - `.github/workflows/security.yml` — affected by rec-20260804-006 ci-success does not aggregate Security/CodeQL, and they are not required checks by any other route
  - `.github/workflows/codeql.yml` — affected by rec-20260804-006 ci-success does not aggregate Security/CodeQL, and they are not required checks by any other route
  - `packages/core/src/doctor/run.ts` — affected by rec-20260731-001 cadence doctor: release-currency check (local package.json vs published npm)
  - `.githooks/pre-push` — affected by rec-20260731-001 cadence doctor: release-currency check (local package.json vs published npm)
  - `docs/reference/commands.md` — affected by rec-20260801-001 docs/reference/commands.md config edit section lists only 5 fields; EDITABLE_FIELDS has 8
  - `packages/core/src/config-edit/fields.ts` — affected by rec-20260801-001 docs/reference/commands.md config edit section lists only 5 fields; EDITABLE_FIELDS has 8

## What landed this session
- Read `docs/handoffs/HANDOFF-v1.55-integrity-release.md`; dispatched 3 parallel Opus research agents to independently re-verify every load-bearing claim against live repo state (per the handoff's own "re-verify before implementing" instruction).
- Found the handoff's §4 claim ("pnpm 9.12.0 ignores `pnpm.overrides`") is **false** — overrides apply and are proven by git history (commits `5e8f2894`/`a427a939` show lockfile resolutions moving in lockstep with override declarations); CI always uses the real pinned 9.12.0 binary. The misleading warning came from the globally-installed pnpm v11.2.2 launcher on PATH, not the pinned version. Corrected Phase A's scope from "replace a broken mechanism" to "refresh stale targets + add a drift detector" (do NOT attempt a pnpm major upgrade — actively harmful).
- Read the other 2 untracked handoffs for context: `HANDOFF-v1.56-verifier-honesty.md` (a separate, already-scoped v1.56 release — phases L–P — gated on v1.55 shipping; does not resolve the "ship G–J as v1.56?" question, which the v1.55 handoff itself still defers) and `cadence-handoff-finding-durability-remainder.md`/`cadence-phase251-draft-input.md` (historical, already executed via phases 247–251).
- Produced and got user approval for a full execution plan sequencing phases 252–257 (saved at the plan-file path above), via an Opus Plan-agent synthesis + advisor review pass.
- **Decision-consistency correction, post-approval:** found `rec-20260801-012` (shipped, phase 251) carries `dec-20260803-001` (recorded the day before this session), which commits to conduction staying a deliberate per-phase DRAFT-level `profile:` override — not a change to the repo's baseline config. This conflicted with the plan's original Phase 252 (permanently move baseline `profile` to `standard`). User's final call: do not supersede `dec-20260803-001`; drop the baseline profile change entirely; defer it to v1.56 Phase P; keep the `evidenceFloor` bump. Plan file updated to match.
- Executed Step 0 housekeeping: restored `.cadence/intelligence/` files dirtied by an earlier orientation command; deduped against active+archived recommendation lists (advisor caught that a planned "Rec E" would have duplicated the already-shipped `rec-20260801-012` — dropped it); filed `rec-20260804-001` through `rec-20260804-006`; opened PR #371 tracking the 4 handoff docs + the rec filings.
- CI on PR #371 caught a real regression: tracking the v1.55 handoff tripped `packages/core/tests/docs/npm-scope-sweep.test.ts` (false positive — the handoff's own release-checklist line names the deprecated `@manehorizons` scope to describe a check about it, not a leftover reference). Fixed by adding it to the test's existing per-file allowlist (matching the `docs/migration-npm-scope.md` precedent); verified locally (all 23 doc-test files + full local suite pass); pushed.
- CI went green (`ci-success` + CodeQL + build + sbom + secret-scan + all 3 OS test legs; `audit` stayed red, expected/unrelated — Phase 254's job); merged PR #371 (squash) into `main` on explicit user confirmation.

## Carry-forward gotchas
- **Read the plan file in full** (`/home/thomas/.claude/plans/read-docs-handoffs-handoff-v1-55-integri-tidy-kernighan.md`) before drafting any phase — it has the corrected AC/task breakdowns, the rec-ID mapping, and the reasoning behind every scope change. This handoff is a pointer to it, not a substitute.
- `cadence recommend` dirties tracked files (`.cadence/intelligence/{RECOMMEND.md,recommend.json}`) as a read side-effect — use `cadence recommendation list` (+ `--archived`) for dedup instead, per `~/.claude/projects/.../memory/feedback-cadence-recommend-dirties-tracked-files.md`.
- Phase 252 must **not** change the baseline `profile` — that question is explicitly deferred to v1.56 Phase P via a decision to be recorded *inside* Phase 252 itself (not yet written). Do not reopen this without re-reading `dec-20260803-001` and `rec-20260801-012` first.
- Rec IDs filed this session: `rec-20260804-001` (pnpm override drift detector, sources Phase 253) · `002` (audit-exceptions parser directional fragility) · `003` (archived-shipped rec invisible to default dedup) · `004` (stale worktree phase collision) · `005` (evidenceFloor below solo preset, sources Phase 252) · `006` (ci-success doesn't aggregate security, sources Phase 255).
- `rec-20260801-010` is archived `shipped` despite `dec-20260801-003` saying "ship no code this phase" — do not refile it; "unblocking" it (Phase 256) means progressing `dec-20260801-003`'s 3-settle revisit trigger, not reopening the rec.
- GitHub's Dependabot summary (36 vulnerabilities: 7 critical/5 high/19 moderate/5 low) does not match `pnpm audit --json`'s count (27: 1/9/15/2) from earlier verification — different scope/source, unreconciled. Reconcile in Phase 254, don't assume either number without re-deriving.
- Two sibling worktrees (`.claude/worktrees/kernel-arc-docs-review` on a stale `feat/kernel-assurance-v2` branch, `.claude/worktrees/phase249-refused-settle-post-gate`) still trip `cadence doctor`'s `worktree-phases` check permanently — tracked as `rec-20260804-004`. Do not touch either without first confirming neither is a live session (CLAUDE.md's "Zombie Session" rule).
- The `kernel-arc-docs-review` worktree's branch convention (one long-lived feature branch for the whole kernel/assurance arc) is superseded — phases 246-251 all landed directly on `main`. Don't resurrect that pattern for new phases.

## Next action
**Action:** Read the plan file's "Phase 252" section in full, then scaffold it:
`cadence draft new 252-self-application-config-correction --tier standard`
(sourced from `rec-20260804-005`). Author AC-1 (record the deferred-profile
decision, not superseding `dec-20260803-001` — see the plan file's exact
rationale bullets) and AC-2 (invariant test: `gates.evidenceFloor` ≥
`assertion` AND `securityAudit.provider === "mock"`), then work T1–T4 as
specified in the plan.
**Verify:** `cadence draft show` reflects the two ACs above; the DRAFT names
`dec-20260803-001` explicitly and states it is not being superseded.
**If it fails:** if `draft new` reports a phase-number collision, re-run
`cadence doctor`'s `worktree-phases` check and `ls .cadence/phases/ | sort -V | tail`
to confirm 252 is still free (the two stale worktrees in the gotchas above
can make this check noisy) before picking a different number — don't assume,
verify against `main`'s own `.cadence/phases/`.
