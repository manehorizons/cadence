# Session Handoff — 2026-05-15 (v1.1 planning + Phase 29.1 shakedown)

Continues `SESSION-2026-05-15-v1.0.0.md` (v0.6.0→v1.0.0 + post-1.0 hardening).
This session: planned the **v1.1 milestone**, ran **Phase 29.1** (foreign-repo
shakedown) end-to-end via the dogfood loop. **Next session: Phase 29.4 —
remediate the shakedown findings (F2 is the publish-blocker).**

## TL;DR for the next session

- v1.1 milestone is phased in `.cadence/ROADMAP.md`: **29.1 → 29.4 (shakedown) → 30.1 (publish)**; 29.4 is a hard gate before 30.1.
- **Phase 29.1 DONE + settled + committed.** Full tagged finding log: `.cadence/shakedown/29-01-FOREIGN.md`.
- **Next = Phase 29.2** (expensive-gate live exercise, strict×complex + `anthropic` providers) per ROADMAP — *unless* you choose to pull F2 remediation forward. Recommended: do **29.4 F2 fix first** (it's a publish-blocker and tiny), or at minimum keep it top of mind; ROADMAP ordering is 29.1→29.2→29.3→29.4→30.1 but F2 is the headline risk.
- Loop **IDLE**, cadence `main` clean, both repos committed + pushed (see "State on handoff").
- Process decision recorded in ROADMAP: **stay on the CADENCE loop, do NOT migrate meta-process to GSD** (re-litigation-guarded). Escape hatch: borrow `brainstorming`/`grill-me` for 30.1, `grill-with-docs` for 29.x — `.cadence/` stays system of record.

## What landed this session

### v1.1 milestone planned (`.cadence/ROADMAP.md`)

Replaced the "Post-1.0 planning anchor" with phased v1.1:

| Phase | Scope |
|---|---|
| 29.1 | Foreign-repo shakedown on `continuity-runtime` — **DONE** |
| 29.2 | Expensive-gate live exercise (strict×complex, `anthropic` providers) |
| 29.3 | Interactive/approve TTY exercise |
| 29.4 | Shakedown remediation — **hard gate before publish** |
| 30.1 | Publish pipeline (changesets/release/provenance) — **depends on 29.4** |

Open questions 29.1/29.2/30.1 added. Server-side CI enforcement, backlog parking lot, deferred OQs (23.1/23.4/24.3/26.2), test-infra serialized lane → **v1.2+**.

### Phase 29.1 — foreign-repo shakedown (continuity-runtime)

Target: `~/Documents/Projects/continuity-runtime` (npm single-package, 75 commits, TS+vitest+biome, tests in `tests/`). Locked as 29.1 target in ROADMAP OQ 29.1.

Ran the full dogfood loop on cadence (auto×standard): DRAFT→approve→BUILD (T1–T4)→settle, `--allow-missing-coverage` (observation phase, documented), `coverage-bypassed` anomaly fired as expected. Inside it, drove **two real phases full-loop on continuity-runtime** (biome format-fixes to `src/index.ts` + `src/health/index.ts`).

**Findings (full detail + verbatim output in `.cadence/shakedown/29-01-FOREIGN.md`):**

| ID | Tag | Sev | What | 29.4 action |
|----|-----|-----|------|-------------|
| **F2** | `bug` | **HIGH** | `init` hard-codes `testGlobs=packages/**` with **no layout detection** → test-coverage gate **unsatisfiable on any single-package repo**. Confirmed at runtime (settle refused verbatim: `searched: packages/**/*.test.ts, packages/**/*.test.tsx`). **THE publish-blocker — cadence's most over-fit-to-itself assumption.** | Detect layout (`packages/` vs `src/`+`tests/`) at init, or prompt, or smarter default |
| F1 | `docs` | MED | `--local` `settings.local.json` bakes absolute machine paths; no gitignore/warn → committing ships unusable paths | docs + install warning or auto-gitignore |
| F6 | `docs` | MED | init-suggested `standard` profile → first `draft approve` needs TTY/`--no-approve`; quickstart silent (cadence dogfood never hits this, uses `auto`) | one line in quickstart + post-init summary |
| F4 | `ux` | LOW | config preset (`team`) vs gate profile (`standard`) — overlapping "profile" vocabulary adjacent | triage, clarify in summary |
| F3,F5 | works-as-designed | — | gate-profile suggestion, non-TTY init, `.cadence`/`.planning` coexistence — all clean | none |

**Out of scope (per DRAFT boundary):** language-tuning of gates (continuity-runtime is TS+vitest; "are gates JS-tuned" stays a documented boundary / later phase).

**Positive:** every other loop mechanic worked on a foreign single-package repo. Loop core is portable; only real defect is F2.

## State on handoff

- **cadence repo:** `main`, clean tree, loop IDLE, active phase null (last settled `29-shakedown/29-01`). Commits this session (pushed): `c9c2084` v1.1 milestone, `5801244` OQ 29.1 resolution, `7c895d4` 29.1 settle, `faf536b` loop-state sync, plus this handoff commit.
- **continuity-runtime repo:** received shakedown residue — `.cadence/` (init + 2 settled phases `01-lint-format`/`02-lint-format`), `.claude/` (host install: `settings.local.json` + 9 commands), and 2 biome-formatted src files (`src/index.ts`, `src/health/index.ts`). Committed + pushed so nothing is lost on the machine move. **It is residue from cadence testing, not real continuity-runtime work** — safe to `git revert` / delete `.cadence` + `.claude` later if undesired. Its `.claude/settings.local.json` carries absolute paths to *this* machine's cadence workspace (see F1) — on the new machine those paths are wrong; re-run the host install there if you want cadence active in continuity-runtime again.

## Carry-forward gotchas (additions to prior handoff)

1. **F1 in practice:** continuity-runtime's committed `.claude/settings.local.json` has machine-absolute paths from the OLD computer. On the new machine they won't resolve. Either ignore (it's test residue) or re-run `node packages/host-claude-code/bin/cadence-host-claude-code.cjs install --cwd <continuity-runtime> --local --settings .claude/settings.local.json` from a fresh cadence build.
2. **F2 will bite any single-package dogfood**, not just continuity-runtime. cadence itself is a monorepo so its own `testGlobs=packages/**` is correct — the bug only manifests on foreign single-package repos. Keep this in mind if 29.2/29.3 pick a different target.
3. Prior gotchas still hold: machine path `C:\Users\Thomas\…`; run CLI as `node packages/core/dist/cli/index.js <cmd>` (managed `cadence-progress` skill has hardcoded `digit` path); fresh checkout needs `pnpm install --config.confirmModulesPurge=false` then `pnpm turbo run build`; Bash cwd resets — prefix with `cd C:/Users/Thomas/Documents/Projects/cadence &&`; `core.hooksPath=.githooks` is untracked local config, re-run on fresh clone; pre-push hook runs full turbo gate on `main`.

## Conventions reaffirmed this session

- Observation-only phase with all-`.cadence/` artifacts → **single settle commit** (no separable impl diff); `--allow-missing-coverage` documented in DRAFT boundary + settle commit; `coverage-bypassed` anomaly expected.
- Loop-state files (`.cadence/state.json`, `STATE.md`) committed in a follow-up `chore: sync loop state` commit (not amended — new-commit rule).
- Push user-gated; this session user explicitly authorized commit-all + push for the machine move.

## Quick resume commands (new machine)

```bash
cd <repo>/cadence
git config core.hooksPath .githooks            # fresh clone
git pull
pnpm install --config.confirmModulesPurge=false && pnpm turbo run build
git log --oneline -6
node packages/core/dist/cli/index.js progress
cat .cadence/shakedown/29-01-FOREIGN.md        # 29.1 findings
sed -n '/## v1.1.0/,/## Deferred/p' .cadence/ROADMAP.md
# Next: Phase 29.2 (per ROADMAP) — or pull F2 (29.4) forward as the publish-blocker fix
```
