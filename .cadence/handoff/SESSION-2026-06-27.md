---
cadence_handoff: 1
generated_at: 2026-06-27T00:21:19.096Z
label: 
loop_position: IDLE
active_phase: 131-doctor-fix-for-safe-onboarding-repairs
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 6a4899d
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-06-27

## TL;DR for the next session
- **v1.34.0 is SHIPPED to npm** (`cadence doctor --fix`, phase 131). All 4 published packages live at 1.34.0 (verified `dist-tags.latest`), tag `v1.34.0` → `6a4899d`, GitHub Release published. Loop is **IDLE**, no active draft — ready for a fresh phase.
- This session = pure delivery: resumed the phase-131 handoff, fixed a `main` CI flake, then merged phase 131 (PR #113) and cut + merged + published the v1.34.0 release (PR #114).
- **⚠ The Release workflow run showed a RED ✗ but the release fully succeeded** — its integrity step raced npm CDN propagation. Do NOT re-run it. See Carry-forward gotchas + [[cadence-release-workflow-propagation-flake]].
- **No blockers, no pending PRs.** `main` is at `6a4899d`, CI green, only the live `.cadence/` telemetry + untracked SESSION docs are uncommitted (intentional).
- **Single next action:** start the next loop. Backlog is raw-idea only — rec-20260619-005 (`init --dry-run` fit check) and rec-20260619-008 (team rollout kit); pick one (likely rec-005, smaller/onboarding-lane) or scout fresh.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `6a4899d`
- Recent commits:
```
6a4899d chore(release): v1.34.0 — doctor-fix (#114)
e8101b8 feat(doctor): cadence doctor --fix for safe onboarding repairs (phase 131) (#113)
fbd456b chore(release): v1.33.0 — agent-prompt
689249b feat: cadence agent-prompt — hand the first real phase to your AI agent (phase 130)
0a53646 chore: release v1.32.0 (#110)
fae3d3e feat(tutorial): rebuild around the catch (refuse→fix→pass) (#109)
ab542eb feat(release): verify GitHub Release integrity (#108)
39d22b6 chore: release v1.31.0
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```
- Loop: IDLE · phase 131-doctor-fix-for-safe-onboarding-repairs · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260619-005 — init dry-run fit check (candidate/raw-idea)
  - rec-20260619-008 — Team rollout kit (candidate/raw-idea)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - `packages/core/src/cli/commands/init.ts` — affected by rec-20260619-005 init dry-run fit check
  - `docs/quickstart.md` — affected by rec-20260619-005 init dry-run fit check
  - `README.md` — affected by rec-20260619-008 Team rollout kit
  - `docs/README.md` — affected by rec-20260619-008 Team rollout kit
  - `.github` — affected by rec-20260619-008 Team rollout kit

## What landed this session
1. **Fixed the flaky `main` CI red.** Run `28210850351` (post-merge CI for the v1.33.0 commit `fbd456b`) had failed only on the **macos-latest / Node 20** leg — two subprocess-heavy CLI settle tests (`settle-code-review.test.ts` AC-6, `settle-codereview-convergence.test.ts` AC-4) hit the exact 20s vitest timeout. The other 5 OS×Node legs were green. Re-ran just the failed leg (`gh run rerun --failed`) → passed in 3m45s, `ci-success` green. No code changed (house rule: re-run the OS flake, don't add per-test timeout band-aids).
2. **Merged phase 131 — `cadence doctor --fix`** (rec-20260619-004). Pushed `feat/doctor-fix` (was local-only), opened **PR #113**, CI green all 6 legs, squash-merged to `main` as `e8101b8`.
3. **Cut + merged + shipped v1.34.0.** On `release/v1.34.0`: `pnpm changeset version` consumed `.changeset/doctor-fix.md` → all 4 pkgs `1.33.0 → 1.34.0` lockstep, updated CHANGELOGs, added the CLAUDE.md v1.34 narrative (doc-sync gate green). **PR #114**, CI green all 6 legs, squash-merged as `6a4899d`. Operator-authorized the manual `Release` workflow (run `28272203839`) → published all 4 pkgs to npm with provenance, pushed tag `v1.34.0`, created the GitHub Release.
4. **Cleanup:** deleted merged branches `feat/doctor-fix` + `release/v1.34.0` (local + remote). Updated memory (`cadence-launch-positioning` → v1.34.0; new `cadence-release-workflow-propagation-flake`).

## Carry-forward gotchas
- **The `Release` workflow run `28272203839` is RED but v1.34.0 shipped correctly.** Its final `Create GitHub Release and verify registry` step failed: the release-integrity check ran `npm view` right after publish and npm's CDN still served `1.33.0` for `host-claude-code` through all 3 retries (`is 1.33.0 on npm, expected 1.34.0`), then propagated minutes later. **Verify reality before reacting** (`npm view … dist-tags.latest`, `git ls-remote --tags origin v1.34.0`, `gh release view v1.34.0`) — all agree on 1.34.0. **Do NOT `gh run rerun --failed`** — re-running the `release` job would re-run `pnpm -r publish` and fail on already-published versions. Durable fix worth filing: more retries/longer backoff in the integrity verifier. See [[cadence-release-workflow-propagation-flake]].
- **macos-20 CI flake recurs on release/heavy commits** — subprocess-spawning CLI settle tests hit the 20s timeout on slow runners (the v1.32.0 commit flaked identically). Re-run the leg; never add per-test timeout band-aids (`vitest.shared.ts` is the single source of truth).
- **CLI tests spawn `dist/cli/index.js`** — rebuild core (`pnpm --filter @manehorizons/cadence-core build`) before running CLI-affecting tests locally, or a regression hides until CI.
- **Uncommitted `.cadence/STATE.md` + `state.json` are live telemetry** — CADENCE rewrites them during the session; don't sweep into a commit, don't `git restore` mid-loop. The 3 untracked `.cadence/handoff/SESSION-*.md` docs (phase-130, phase-131, today) are intentional scratch — leave them.
- **`main` is admin-enforced branch-protected** — land via PR + green `ci-success`. Self-merging a PR you opened needs explicit user authorization (the auto-mode classifier blocks it otherwise).
- **Release recipe:** `pnpm changeset version` FIRST (the Release workflow publishes package.json versions verbatim) + a `CLAUDE.md` <version> mention (doc-sync gate) before the operator fires the manual `Release` workflow.

## Next action
**Action:** Nothing is in flight — the loop is IDLE and v1.34.0 is fully shipped. Start the next phase. The backlog holds two raw-idea recs only: `rec-20260619-005` (`cadence init --dry-run` fit check) and `rec-20260619-008` (team rollout kit). Most likely next: scaffold a phase for **rec-20260619-005** (smaller, onboarding lane) — `cadence draft new --title "..."` (auto-derives the next phase id) — or run `cadence scout` to refresh the backlog first. Confirm direction with Thomas before committing to a phase.

**Verify:** `cadence progress` shows IDLE with the next-loop suggestion; `npm view @manehorizons/cadence-core version` returns `1.34.0`.

**If it fails:** If `cadence progress` reports drift or a corrupt state, run `cadence doctor` (and now `cadence doctor --fix` — the feature just shipped) to inspect/repair. There is no half-finished work to recover.
