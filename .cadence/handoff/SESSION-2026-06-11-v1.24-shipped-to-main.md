---
cadence_handoff: 1
generated_at: 2026-06-11T22:51:08.415Z
label: v1.24-shipped-to-main
loop_position: IDLE
active_phase: 103-rec-retention-release
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 0a48237
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-06-11 (v1.24-shipped-to-main)

## TL;DR for the next session
- **Loop is IDLE; everything is merged to `main` (`0a48237`).** Only uncommitted changes are ephemeral `.cadence/STATE.md`/`state.json` telemetry — safe to discard. No WIP, no stash.
- **The entire v1.24 `recommendation-retention` milestone shipped to `main` this session** (phases 101→102→103, PRs #77/#78/#79), plus a roadmap-status update (#80). All 4 published packages are at **1.24.0** on `main`, changeset consumed.
- **v1.24 is NOT yet on npm.** `npm view @manehorizons/cadence-core version` is still `1.23.0`. The version bump is on `main`, so the only remaining step is the **operator-triggered manual Release workflow** (Actions → Release, `dry_run` unchecked) → publishes 1.24.0 + tag `v1.24.0` w/ provenance. An assistant cannot trigger it.
- **Also shipped earlier this session:** the v1.23.0 release (phase 100 `shipped` status) to npm — see gotchas; that one IS live on npm.
- **No forced next action.** Optional menu: (1) confirm v1.24 reached npm after you trigger Release; (2) file the `draft new` `NNN-NNN` hint bug as a rec (still unfiled); (3) start a new phase/milestone.
- **Blocker:** none.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `0a48237`
- Recent commits:
```
0a48237 docs(roadmap): mark v1.24.0 recommendation-retention DELIVERED (#80)
f3fc315 feat: v1.24.0 — recommendation retention (phase 103 / release) (#79)
cef5c06 feat(intelligence): auto-archive recommendations (phase 102, v1.24) (#78)
bcd66a7 feat(intelligence): soft-archive recommendations (phase 101, v1.24) (#77)
acdd184 docs(roadmap): plan v1.24.0 — recommendation retention (soft-archival) (#76)
788a75d chore: release v1.23.0 (shipped terminal status, phase 100) (#75)
2e13e18 Merge pull request #74 from manehorizons/chore/dogfood-shipped-status
fc76a4f chore(intelligence): mark 20 shipped recs as shipped (dogfood phase 100)
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```
- Loop: IDLE · phase 103-rec-retention-release · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - (none)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - (none)

## What landed this session
- **v1.23.0 published to npm** (start of session): cut the release for phase 100's `shipped` terminal rec status — `pnpm changeset version` (1.22.1→1.23.0), CLAUDE.md bump, PR #75, operator-triggered Release run succeeded; all 4 pkgs live on npm at 1.23.0, tag `v1.23.0` (`a3433ad`).
- **Brainstormed + scaffolded the v1.24 `recommendation-retention` milestone** (PR #76): manual + automatic soft-archival of recs. Design spec local-only at `docs/superpowers/specs/2026-06-11-recommendation-retention-design.md` (gitignored).
- **Phase 101** (PR #77, `bcd66a7`) — storage + manual surface: `RecommendationLedger.archived` array (`.default([])`, backward-compat) + optional `archivedAt`/`archiveReason` on recs; pure `archive/unarchiveRecommendation` + `runRecommendation{Archive,Unarchive}`; CLI `recommendation archive`/`unarchive`/`list --archived`. TDD.
- **Phase 102** (PR #78, `cef5c06`) — auto-archive: `recommendations.autoArchive` config (default ON, recoverable); promote→shipped/rejected archives in the same atomic write; best-effort settle→rec hook (`runAutoArchiveConvertedForPhase`, reason `converted-settled`); `autoArchive` in `config edit` catalog; `recommendation show` made archive-aware. TDD.
- **Phase 103** (PR #79, `f3fc315`) — release: docs (commands.md + config.md), changeset, lockstep `1.23.0 → 1.24.0` bump; CLAUDE.md leads with 1.24.0.
- **Roadmap status** (PR #80, `0a48237`) — flipped MILESTONES.md §v1.24.0 + ROADMAP to DELIVERED/SHIPPED.
- Memory updated: launch-positioning (v1.23/v1.24 progress) + new `cadence-cli-tests-need-dist-rebuild` feedback memory.

## Carry-forward gotchas
- **CLI tests spawn the BUILT binary (`packages/core/dist/cli/index.js`), not src.** Always `pnpm --filter @manehorizons/cadence-core build` BEFORE running the suite when you've changed CLI-affecting code — else a regression hides locally and only CI (build-then-test) catches it. This bit phase 102: a green local run masked a real `recommendation show` break because `dist/` was stale. (Saved as memory `cadence-cli-tests-need-dist-rebuild`.)
- **Cutting a release needs `pnpm changeset version` FIRST** — the Release workflow publishes `package.json` versions verbatim, it does NOT run changeset version. Then update `CLAUDE.md` to mention the new version (pre-commit doc-sync gate aborts otherwise), commit + PR + green, then the operator triggers Actions → Release. For 1.24.0 this is **already done** — the bump is on `main`; only the workflow trigger remains.
- **`cadence draft new`'s proactive hint puts the phase NUMBER in the task-NUMBER slot** for phases ≥ 100 (`draft new 103-foo 103` → id `103-103`). Workaround used all session: pass task-num `1` (`draft new 103-foo 1`). Candidate rec, still **unfiled**.
- **Uncommitted `.cadence/STATE.md` + `state.json` are ephemeral telemetry** (subagentSpawns counter) — re-touched by `cadence progress`/handoff. Safe to discard; no stash taken.
- **`recommendations.autoArchive` now defaults ON** — promoting a rec to shipped/rejected (or settling a phase a rec was converted into) now auto-archives it out of the active `recommend`/`list` surface. Use `recommendation list --archived` or `recommendation show <id>` (archive-aware) to find it. The repo's own 20 existing shipped recs were NOT retro-archived (only future terminal events trigger it).

## Next action
**Action:** No forced next step — the loop is IDLE and all this session's work is merged. The one outstanding item is operator-only: **trigger Actions → Release** (`dry_run` unchecked) to publish v1.24.0 to npm (an assistant cannot do this). Otherwise pick from the optional menu: (1) after the operator triggers Release, confirm v1.24.0 reached npm; (2) `cadence recommendation add` the `draft new` `NNN-NNN` hint bug; (3) start a new phase/milestone.

**Verify:** `git -C /home/thomas/projects/cadence log -1 --oneline` is `0a48237` (roadmap-delivered merge is latest); `for p in core types host-claude-code host-codex; do node -e "console.log(require('./packages/$p/package.json').version)"; done` all print `1.24.0` (bump is on main); `npm view @manehorizons/cadence-core version` is still `1.23.0` until the operator triggers Release (then `1.24.0`).

**If it fails:** if `git log` ≠ `0a48237`, a merge didn't land — check `gh pr list --state merged --limit 6`. If package.json versions aren't `1.24.0`, the release-cut commit (`e4e5ffe`, folded into PR #79) didn't land — re-check `gh pr view 79`. If after a Release run npm still shows `1.23.0`, check the run: `gh run list --workflow=release.yml --limit 3`.
