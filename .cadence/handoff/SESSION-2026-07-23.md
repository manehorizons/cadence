---
cadence_handoff: 1
generated_at: 2026-07-23T04:34:58.942Z
label: 
loop_position: IDLE
active_phase: 209-anthropic-fallback-warning-claude-code-distinction
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 43099e3
git_ahead: 1
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-07-23

## TL;DR for the next session
- Resumed a prior session (phase 209/`rec-20260723-001` already shipped), then picked up the next sibling recommendation, `rec-20260723-002` (docs callout distinguishing Claude Code login from the `anthropic` provider's `ANTHROPIC_API_KEY` requirement), and took it end-to-end: promote → milestone propose/accept/export → SPEC → DRAFT → BUILD → SETTLE as **phase 210**, fully landed.
- Ran the whole loop inside an isolated worktree (`EnterWorktree`) from the start this time — no repeat of the prior session's mid-loop worktree-order mistake.
- Single-file docs change (`docs/providers.md`, anthropic section): added a `> **Label.**`-style blockquote callout matching the existing docs convention, modeled on the `host-cli` section's quota-transparency notice, cross-linking to `host-cli`. No runtime code touched — the phase 209 fallback-warning text was left alone per the DRAFT's boundaries.
- Landed as **PR #285** (the docs change) + a separate follow-up **PR #286** (`chore(cadence): mark rec-20260723-002 shipped`) — following the same two-PR pattern actually used historically for phases 203/204/209 (settle commit, then a distinct shipped-promotion commit once the real PR number is known), not the single-commit-with-guessed-PR-number literal reading of CLAUDE.md's convention text.
- Both PRs' local post-merge steps hit the known `gh pr merge --delete-branch` local-checkout failure (main checked out elsewhere / diverged) — remote merges both succeeded regardless (verified via `gh pr view --json state,mergedAt,mergeCommit` each time); resynced local `main` manually via stash + rebase against `origin/main` after each merge.
- Loop is IDLE, nothing in flight, nothing blocking. `rec-20260723-003` (CLAUDECODE-aware doctor/activate messaging) is the natural next candidate, still `raw-idea`/`candidate`.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 1 ahead / 0 behind origin
- HEAD `43099e3`
- Recent commits:
```
43099e3 chore(cadence): stamp session handoff — 2026-07-23
6db04d6 chore(cadence): mark rec-20260723-002 shipped (PR #285 / phase 210) (#286)
fc36e77 docs: anthropic provider auth is separate from Claude Code login (phase 210) (rec-20260723-002) (#285)
ba9cc69 chore(cadence): mark rec-20260723-001 shipped (PR #282 / phase 209) (#284)
fc14fd6 fix: clarify anthropic provider auth is separate from Claude Code login (phase 209) (rec-20260723-001) (#283)
11bda6b fix: clarify anthropic provider auth is separate from Claude Code login (phase 209) (rec-20260723-001) (#282)
64d91a9 chore(cadence): record scout-20260723-0108 recs + mil-rec-rec-20260723-001 export (#281)
35379fe feat: doctor phase-freshness check + concurrent-session guidance (phase 208) (#279)
```
- Uncommitted (diff --stat):
```
.claude/scheduled_tasks.lock | 1 -
 1 file changed, 1 deletion(-)
```
- Loop: IDLE · phase 209-anthropic-fallback-warning-claude-code-distinction · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260710-001 — Clarify Claude Code auth vs ANTHROPIC_API_KEY confusion in provider docs + fallback warning (candidate/raw-idea)
  - rec-20260712-003 — Retro friction feeds back into Praxis recommendation scoring (candidate/raw-idea)
  - rec-20260712-009 — Record a gate lifecycle-state taxonomy (requested/started/passed/refused/failed/timed-out) in SUMMARY (candidate/raw-idea)
  - rec-20260712-014 — Add test-coverage reporting with enforced minimum thresholds to CI (candidate/raw-idea)
  - rec-20260718-003 — Frame dispatched task boundaries as stop-conditions, not file-scope lists (candidate/raw-idea)
- Open assumptions:
  - (none)
- Active decisions:
  - dec-20260711-001 — Multi-language assertion-coverage: fast diagnose-fix now, shared-lexer engine as a later phase
  - dec-20260721-001 — cadence next extends nextAction(), does not subsume quickstart or reimplement
  - dec-20260721-002 — Shared legal-moves computation also powers empty-state footers (rec-20260721-001)
  - dec-20260721-003 — cadence next --json includes schemaVersion: 1
  - dec-20260721-004 — Ship /cadence-next slash command alongside the CLI command
- Files in play:
  - `packages/core/src/services/settle.ts` — affected by rec-20260712-009 Record a gate lifecycle-state taxonomy (requested/started/passed/refused/failed/timed-out) in SUMMARY
  - `packages/types/src/summary.ts` — affected by rec-20260712-009 Record a gate lifecycle-state taxonomy (requested/started/passed/refused/failed/timed-out) in SUMMARY
  - `.github/workflows/ci.yml` — affected by rec-20260712-014 Add test-coverage reporting with enforced minimum thresholds to CI
  - `vitest.config.ts` — affected by rec-20260712-014 Add test-coverage reporting with enforced minimum thresholds to CI

## What landed this session
- `rec-20260723-002` promoted `accepted`/`ready-for-milestone`, clustered into `mil-rec-rec-20260723-002`, accepted, exported to a staged SPEC.
- Phase 210 (`210-anthropic-docs-callout-claude-code-vs-api-key`) SPEC → DRAFT → BUILD (1 task) → SETTLE, all inside a dedicated worktree (`.claude/worktrees/rec-20260723-002`, branch `worktree-rec-20260723-002`), full `pnpm turbo run lint typecheck test build` green (20/20 tasks, 352 files / 3069 tests) before committing.
- PR #285 — `docs: anthropic provider auth is separate from Claude Code login (phase 210) (rec-20260723-002)` — merged, CI green across the full OS/Node matrix, remote branch cleaned up (had to be deleted manually — `--delete-branch` skipped it on the first PR).
- PR #286 — `chore(cadence): mark rec-20260723-002 shipped (PR #285 / phase 210)` — merged, CI green, remote branch auto-deleted this time.
- Worktree removed post-merge, after verifying the squash-merge content diff was empty against the local commit.
- Local `main` resynced twice (once per PR) via `git stash push -u -m "..."` (unique tag) → `git rebase origin/main` → `git stash apply <sha>` → `git stash drop` — chosen over `git pull` because local `main` kept picking up a fresh 1-commit-ahead handoff-stamp divergence each time.

## Carry-forward gotchas
- **The "single-commit settle" convention (CLAUDE.md item 6) does not literally hold in practice for the shipped-promotion step.** Real history (phases 203, 204, 209, and now 210) always does the recommendation `--status=shipped --ref "PR #NNN"` promotion as a **separate** small follow-up commit/PR, because the PR number genuinely isn't known until the settle-commit's PR is created. Don't try to guess/predict a PR number to cram the promotion into the original settle commit — follow the two-PR pattern that's actually used.
- **`gh pr merge --delete-branch` keeps failing its local checkout step in this repo** whenever `main` is checked out elsewhere (a worktree) or has diverged from origin (an unpushed local commit, e.g. a handoff stamp) — this happened on *both* PRs #285 and #286 this session. The remote merge always succeeds regardless; verify with `gh pr view <n> --json state,mergedAt,mergeCommit` rather than trusting the CLI's own error output. Remote branch deletion is inconsistent too — check with `git ls-remote --heads origin` after every merge and delete manually if it's still there (needed for #285, not for #286).
- A **fresh local `main` divergence appears almost every time you resync** in this repo right now, because the standing "push only when switching machines" convention means the handoff-stamp commit stays local-only across many session boundaries — expect `git rebase origin/main` (with a stash round-trip for the always-present `.claude/scheduled_tasks.lock` deletion) to be a routine step after every PR merge, not a one-off.
- `docs/providers.md` is **not** covered by any doc-content test that pins prose — confirmed before writing the callout (grepped `packages/*/tests/docs/*.test.ts` for `providers.md`, no hits) and deliberately did not add one (matches the phase 210 DRAFT's boundaries).

## Next action
**Action:** Run `cadence recommend` to see the current ranked list and decide whether to pick up `rec-20260723-003` (CLAUDECODE-aware doctor/activate messaging + host-cli nudge) next, or something else.
**Verify:** `rec-20260723-002` should no longer appear as a `candidate` (it's `shipped`); `rec-20260723-003` should still show as `candidate`/`raw-idea`.
**If it fails:** if the ranked list looks stale or wrong, run `cadence doctor` first to check ledger integrity before investigating further.
