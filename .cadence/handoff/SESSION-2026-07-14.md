---
cadence_handoff: 1
generated_at: 2026-07-14T04:39:53.717Z
label: 
loop_position: BUILD
active_phase: 181-mcp-tool-trust-envelope
active_draft: 181-01
tier: complex
git_branch: feat/mcp-tool-trust-envelope
git_dirty: true
git_head: c8b197a
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-07-14

## TL;DR for the next session
- Phase 181 (MCP tool-trust envelope) is mid-BUILD in worktree `.claude/worktrees/mcp-tool-trust-envelope` (branch `feat/mcp-tool-trust-envelope`) — T1–T5 of 6 tasks are DONE and independently re-verified; only T6 (docs) remains before the whole-branch review + settle.
- Nothing is committed yet in this worktree — source, tests, and the whole Praxis milestone→spec→draft trail are all uncommitted, per the two-commit settle convention; commit only after T6 + whole-branch review.
- Security-critical result already landed: an MCP client can never self-grant approval-bypass trust — grants are CLI-only (`cadence mcp trust grant/revoke/list`), confirmed unreachable from any MCP tool call, and `cadence_draft_approve`/`cadence_spec_approve` now refuse without a valid, hash-matched, version-matched, unexpired grant.
- Next action: implement T6 (docs), then whole-branch review → feature commit + changeset → `cadence settle run --auto` → settle commit → land via `pr-land`.
- Two unrelated Praxis CLI gaps were filed live as recommendations (rec-20260714-001, rec-20260714-002) — not part of this phase, just discovered along the way.
- Gotcha: this worktree's `.cadence/` was manually seeded from the primary checkout's uncommitted state (a fresh worktree branches from origin and drops uncommitted dirt) — see Carry-forward gotchas before touching either checkout's `.cadence/`.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `feat/mcp-tool-trust-envelope` (dirty), 0 ahead / 0 behind origin
- HEAD `c8b197a`
- Recent commits:
```
c8b197a feat: redact secrets from evidence quotes and security-audit findings (phase 180) (#197)
b2a6a08 chore(cadence): mark rec-20260703-001 shipped (PR #195) (#196)
424aa8c feat: milestone fan-in worktree status/reconciliation command (phase 179) (#195)
462f239 feat: guardrails for headless-CLI verifier (phase 178) (#193)
9690536 chore(cadence): mark rec-20260712-007 shipped, stamp release handoff — v1.44.1 (#194)
7430b28 chore(release): v1.44.1 -- gate-throw audit, installer refusal, optimistic concurrency, retro artifact (#192)
9b773fb fix(gates): normalize gate-impl throws into an audited refuse outcome (#191)
ac8bdd3 docs: README embeds the animated test-gutting demo (phase 177) (#190)
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md                          |  16 +++--
 .cadence/intelligence/MILESTONES.md        |  31 +--------
 .cadence/intelligence/RECOMMEND.md         |  25 ++-----
 .cadence/intelligence/RECOMMENDATIONS.md   |  34 ++++++++-
 .cadence/intelligence/evidence.json        |  14 ++++
 .cadence/intelligence/milestones.json      |  30 +++-----
 .cadence/intelligence/recommend.json       | 107 ++++-------------------------
 .cadence/intelligence/recommendations.json |  61 +++++++++++++++-
 .cadence/state.json                        |  27 +++++---
 packages/core/bin/cadence.cjs              |   0
 packages/core/src/cli/commands/mcp.ts      |  79 +++++++++++++++++----
 packages/core/src/mcp/tools.ts             |  66 +++++++++++++++++-
 packages/core/tests/mcp/mcp-server.test.ts |  37 ++++++++++
 packages/types/src/index.ts                |   1 +
 14 files changed, 329 insertions(+), 199 deletions(-)
```
- Loop: BUILD · phase 181-mcp-tool-trust-envelope · tier complex

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260712-010 — Thread AbortSignal + deadline + trace id through gates, verifiers, and the headless-CLI verifier (candidate/needs-evidence)
  - rec-20260712-012 — Generate the command/config/exit-code reference from source and fail CI on drift (candidate/needs-evidence)
  - rec-20260712-013 — Add the missing CI security automation: CodeQL, secret scanning, npm-audit policy, SBOM, scheduled run (candidate/needs-evidence)
  - rec-20260712-015 — Smoke-test the packed npm tarball (clean install -> init -> settle), not just in-repo dist (candidate/needs-evidence)
  - rec-20260619-008 — Team rollout kit (candidate/raw-idea)
- Open assumptions:
  - (none)
- Active decisions:
  - dec-20260711-001 — Multi-language assertion-coverage: fast diagnose-fix now, shared-lexer engine as a later phase
- Files in play:
  - `packages/core/src/gates` — affected by rec-20260712-010 Thread AbortSignal + deadline + trace id through gates, verifiers, and the headless-CLI verifier
  - `packages/core/src/verify/security-audit.ts` — affected by rec-20260712-010 Thread AbortSignal + deadline + trace id through gates, verifiers, and the headless-CLI verifier
  - `docs/reference/commands.md` — affected by rec-20260712-012 Generate the command/config/exit-code reference from source and fail CI on drift
  - `docs/reference/config.md` — affected by rec-20260712-012 Generate the command/config/exit-code reference from source and fail CI on drift
  - `scripts` — affected by rec-20260712-012 Generate the command/config/exit-code reference from source and fail CI on drift
  - `.github/workflows` — affected by rec-20260712-013 Add the missing CI security automation: CodeQL, secret scanning, npm-audit policy, SBOM, scheduled run
  - `.github/dependabot.yml` — affected by rec-20260712-013 Add the missing CI security automation: CodeQL, secret scanning, npm-audit policy, SBOM, scheduled run
  - `scripts/publish-proof.mjs` — affected by rec-20260712-015 Smoke-test the packed npm tarball (clean install -> init -> settle), not just in-repo dist
  - `scripts/release-integrity.mjs` — affected by rec-20260712-015 Smoke-test the packed npm tarball (clean install -> init -> settle), not just in-repo dist
  - `.github/workflows/release.yml` — affected by rec-20260712-015 Smoke-test the packed npm tarball (clean install -> init -> settle), not just in-repo dist
  - `README.md` — affected by rec-20260619-008 Team rollout kit
  - `docs/README.md` — affected by rec-20260619-008 Team rollout kit
  - `.github` — affected by rec-20260619-008 Team rollout kit

## What landed this session
- Ran the full Praxis pipeline: `cadence recommend` → `milestone propose` → `premortem` → `accept` → `export`, then promoted the staged export via `cadence spec new`/`spec approve` (phase 181), fleshing out AC-1/Constraints/Open Questions grounded in real code research rather than the auto-generated placeholder text.
- `cadence draft new`/`draft approve` (with `--allow-auto-complex` to clear the DESIGN.md §4 M2 auto×complex soft cap) into BUILD, with tasks T1–T6 and hand-added `depends:` chains.
- Filed rec-20260714-001 (milestone premortem's `outOfScope` field has no CLI writer) and rec-20260714-002 (`draft add-task` missing a `--name` flag, unlike `add-ac`) — both discovered live, not part of this phase.
- Entered worktree `.claude/worktrees/mcp-tool-trust-envelope` on branch `feat/mcp-tool-trust-envelope`; manually transplanted the primary checkout's uncommitted `.cadence/` Praxis+phase state into it (see gotchas).
- T1 DONE: capability-classified all 18 MCP tools (`READ_ONLY`/`LEDGER_WRITE`/`LOOP_WRITE`/`APPROVAL_BYPASS`/`SETTLE`) via a new `ToolDef.capabilityClass` field in `packages/core/src/mcp/tools.ts`.
- T2 DONE: `computeToolDefHash` structural fingerprint (sha256 over name+description+sorted-inputSchema-shape) in `packages/core/src/mcp/trust/def-hash.ts` — correctly targets Zod v4's real `_def` internals (the DRAFT's prose loosely implied Zod v3 shape).
- T3 DONE: `McpTrustGrant`/`McpTrustLedger` Zod schemas (`packages/types/src/mcp-trust.ts`) plus a fail-closed file-backed store at `.cadence/mcp-trust.json` (`packages/core/src/mcp/trust/store.ts`) — deliberately diverges from `state.json`/`config.json`'s hard-throw-on-corrupt convention, on purpose, documented inline.
- T4 DONE: `cadence mcp trust grant/revoke/list` CLI (`packages/core/src/services/mcp-trust-grant.ts` + `packages/core/src/cli/commands/mcp.ts`) — independently confirmed CLI-only, never reachable as an MCP tool (the core security invariant of this phase).
- T5 DONE: enforcement wrapper (`packages/core/src/mcp/trust/enforce.ts` + a `gatedRun` helper in `tools.ts`) gating exactly `cadence_draft_approve`/`cadence_spec_approve`; relocated `readPackageVersion` out of the CLI layer into `packages/core/src/version.ts` to fix a layering smell; fixed the one regression this caused in `packages/core/tests/mcp/mcp-server.test.ts` (2 pre-existing tests needed grant setup added, no assertions weakened).
- Every task (T1–T5): implemented by a dedicated subagent, independently adversarially reviewed by a second subagent, then re-verified by me in the main thread with a full `pnpm turbo run lint typecheck test build` before recording `cadence build task T<n> --status=DONE`. Currently green: 317 test files / 2683 tests.

## Carry-forward gotchas
- This worktree's `.cadence/` was seeded by **manually copying** the primary checkout's uncommitted files (`state.json`, `STATE.md`, `intelligence/*`, `phases/181-*`, `intelligence/exports/mil-rec-rec-20260712-011`) — it was never derived from a commit. The primary checkout at `/home/thomas/projects/cadence` still has that same uncommitted dirt sitting in its working tree. Do not `git restore`/`reset` either checkout's `.cadence/` — it's live telemetry — and do not assume the two are kept in sync beyond this one manual copy.
- The "State on handoff" `git diff --stat` block above only shows tracked-modified files — it misses most of the actual work, which is new/untracked: `packages/core/src/mcp/trust/` (`def-hash.ts`, `store.ts`, `enforce.ts`), `packages/core/src/services/mcp-trust-grant.ts`, `packages/core/src/version.ts`, `packages/types/src/mcp-trust.ts`, and their tests under `packages/core/tests/mcp/trust/`, `packages/core/tests/cli/mcp-trust.test.ts`, `packages/core/tests/mcp/tools-capability-class.test.ts`. Run `git status --short` for the real picture.
- T6 must update `cadence_draft_approve`/`cadence_spec_approve`'s description strings in `tools.ts` to mention the grant requirement — keep docs in sync with T5's actual enforced behavior, not the pre-phase "the tool call IS the approval" framing.
- The DRAFT's Boundaries explicitly defer gating `cadence_settle` (classified `SETTLE`, left ungated) to a follow-up recommendation — do not fold that into this phase.
- The draft was approved under `--allow-auto-complex` (auto×complex soft cap, DESIGN.md §4 M2) — a recorded gate bypass. It should surface in the eventual SUMMARY's `gateBypasses`; don't let settle silently omit it.
- `181-01-DRAFT.md`'s tasks have hand-added `- depends:` lines (no CLI flag exists for this yet — see rec-20260714-002-adjacent gap) — respect them if resuming task ordering manually; `cadence dispatch plan --json` already accounts for them (T4←T1-T3, T5←T1-T4, T6←T5).

## Next action
**Action:** Implement T6 ("Document the trust envelope" — `docs/reference/commands.md`, `docs/concepts.md`, `packages/core/src/mcp/tools.ts` description strings) the same way T1–T5 were done: dispatch an implementer subagent scoped to T6's `files:`/`action:`/`verify:` lines in `181-01-DRAFT.md`, then an independent adversarial reviewer, then re-verify with a full `pnpm turbo run lint typecheck test build` in the main thread yourself before `cadence build task T6 --status=DONE`. Then: whole-branch review (fresh subagent vs. the DRAFT's Objective + AC-1, must return zero Critical/Important findings), the feature commit (source+tests+docs+`.changeset/*.md`), `cadence settle run --auto`, the settle commit, and land via the `pr-land` skill.
**Verify:** `pnpm turbo run lint typecheck test build` from the worktree root shows all tasks green; `cadence status` shows BUILD phase 181 with T6 DONE before settling.
**If it fails:** If T6's doc changes fail a doc-content test (`packages/core/tests/docs/*.test.ts`), fix the doc, not the test. If `cadence settle run --auto` refuses, read the reason first — it's likely about coverage or the recorded `--allow-auto-complex` bypass, not a real regression — but don't reach for a bypass flag without confirming the cause.
