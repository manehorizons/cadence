---
cadence_handoff: 1
generated_at: 2026-06-18T03:14:51.550Z
label: v1.29-shipped
loop_position: IDLE
active_phase: 117-release-v1.29
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 01419f2
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-06-18 (v1.29-shipped)

## TL;DR for the next session
- **v1.29.0 is fully SHIPPED to npm** — all 4 published pkgs live at 1.29.0 (verified `npm view`), tag `v1.29.0` (`5a17cc2`) on origin w/ provenance, Release run 27733972247 success (4m29s). `main` at `01419f2`, 0 ahead/behind.
- **v1.29 = non-TTY gate auto-bypass** (rec-20260617-005, the top backlog item): the `approve` + `interactive-verdict` gates auto-bypass in a non-TTY instead of hard-failing. Phase 116 (feature, PR #94) + phase 117 (release, PR #95), both squash-merged, CI green all 6 legs.
- Loop is **IDLE**, working tree clean except the telemetry counter (see gotchas). Next free phase **118**. Nothing in flight, no blockers.
- **No single mandated next action** — milestone done. Strongest remaining backlog: **rec-20260617-003** (auto-derive phase id + collapse `--ac` syntax, `accepted/ready-for-milestone`). Others: rec-611-005 (gate-bypass audit trail), rec-611-007 (COMPETITIVE.md), rec-611-006 (codex test-parity).

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `01419f2`
- Recent commits:
```
01419f2 chore: release v1.29.0 (non-TTY gate auto-bypass, phase 116) (#95)
588c99a feat(gates): non-TTY auto-bypass for the approve + interactive-verdict gates (phase 116) (#94)
eade8d4 chore: release v1.28.0 (coverage assertion mode + onboarding front door/papercuts) (#93)
f6182c0 feat(onboarding): init auto-flip warning + handoff CADENCE_NOW seam (phase 114) (#92)
3fae956 feat(onboarding): one front door + guided Next: rail (phase 113) (#91)
401d86c feat(verify): coverage-gate assertion mode (phase 108) (#90)
b3cfcfa feat: v1.27.0 — onboarding breeze (init front door) (#89)
3c488dc feat: v1.26.0 — guided onboarding (cadence start) (#88)
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```
- Loop: IDLE · phase 117-release-v1.29 · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260617-003 — Auto-derive phase id and collapse --ac syntax (accepted/ready-for-milestone)
  - rec-20260611-005 — Loud audit trail when settle gates are bypassed (--force / --allow-verifier-failure) (candidate/needs-decision)
  - rec-20260611-007 — Author a sourced COMPETITIVE.md / objection-FAQ capturing the in-loop-enforcement wedge (candidate/needs-decision)
  - rec-20260611-006 — Close the Codex host-adapter test-parity gap to prevent silent bit-rot (candidate/needs-evidence)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - `packages/core/src/cli/commands/draft-new.ts` — affected by rec-20260617-003 Auto-derive phase id and collapse --ac syntax
  - `packages/core/src/cli/commands/settle.ts` — affected by rec-20260617-003 Auto-derive phase id and collapse --ac syntax
  - `packages/core/src/cli/commands/progress.ts` — affected by rec-20260617-003 Auto-derive phase id and collapse --ac syntax
  - `packages/core/src/gates/deep-verify.ts` — affected by rec-20260611-005 Loud audit trail when settle gates are bypassed (--force / --allow-verifier-failure)
  - `packages/core/src/gates/coverage.ts` — affected by rec-20260611-005 Loud audit trail when settle gates are bypassed (--force / --allow-verifier-failure)
  - `docs` — affected by rec-20260611-007 Author a sourced COMPETITIVE.md / objection-FAQ capturing the in-loop-enforcement wedge
  - `README.md` — affected by rec-20260611-007 Author a sourced COMPETITIVE.md / objection-FAQ capturing the in-loop-enforcement wedge
  - `packages/host-codex/src` — affected by rec-20260611-006 Close the Codex host-adapter test-parity gap to prevent silent bit-rot
  - `packages/host-claude-code/tests` — affected by rec-20260611-006 Close the Codex host-adapter test-parity gap to prevent silent bit-rot

## What landed this session
- **Phase 116** — non-TTY auto-bypass for the `approve` + `interactive-verdict` gates. Pure `resolveInteractivity(env, isTTY) → 'interactive' | 'bypass' | 'require-tty'` (`packages/core/src/gates/interactivity.ts`); approve auto-passes loudly (stderr audit trail), settle skips its walker + records `interactiveVerifySkipped: "non-tty"` in the SUMMARY (no fabricated verdicts). Env controls: `CADENCE_REQUIRE_TTY=1` (strict restore, wins), `CADENCE_NONINTERACTIVE=1` (force bypass under pseudo-TTY), `CADENCE_PROMPTER_SCRIPT` (always honored). Env-only, no config knob. TDD (5 ACs), 2 adversarial reviews PASS. PR #94 → squash `588c99a`.
- **Phase 117** — release v1.29.0: changeset + lockstep `1.28.0 → 1.29.0` across all 4 published pkgs; CLAUDE.md leads with 1.29.0; MILESTONES.md v1.29.0 section. PR #95 → squash `01419f2`.
- **Published**: operator fired the manual Release workflow → npm + tag `v1.29.0` live.
- Corrected a stale memory note that claimed v1.28 was unpublished (npm confirmed 1.28.0 live).

## Carry-forward gotchas
- The two uncommitted files (`.cadence/STATE.md` + `state.json`) are **just the telemetry `subagentSpawns` counter** — intentionally left uncommitted per protocol; not pending work. Don't `git restore` state.json reflexively (see [[cadence-telemetry-drift-restore-hazard]]).
- **Non-TTY merge dance:** `gh pr merge --squash --delete-branch` fails its local checkout when the telemetry counter is dirty (`local changes … would be overwritten`). Workaround used this session: `git stash push -- .cadence/STATE.md .cadence/state.json` *before* merging, then drop the stash after (main's committed state is authoritative). The GitHub-side merge still succeeds even when the local step errors — verify with `gh pr view <n> --json state`.
- Approving/settling in a non-TTY agent session: feed the verdict via `CADENCE_PROMPTER_SCRIPT="y"` (records a real `yes`) rather than `--no-approve` (which skips the gate). NOTE: as of v1.29 a bare non-TTY approve would also auto-bypass now — but the script path records an explicit verdict, which is cleaner for dogfooding.
- `cadence draft approve`/`settle`'s num arg is the bare task num (`01`), not `<phase>-<num>` — passing `116-01` builds a doubled `116-116-01` path.
- `samplefile.md` (untracked, repo root) is the user's old onboarding sketch — safe to `rm`. Several untracked `SESSION-*.md` handoff docs are prior archives, kept uncommitted by protocol.

## Next action
**Action:** No mandated next step — v1.29 shipped, milestone complete. When picking up new work, the strongest candidate is **rec-20260617-003** (auto-derive phase id + collapse `--ac` syntax — `accepted/ready-for-milestone`, top score); scaffold as **phase 118** via `cadence draft new 118-<slug> 1 --from-rec rec-20260617-003`. Branch off fresh `main` first (admin-protected — land via PR).
**Verify:** `cadence progress` (→ IDLE, next-free hint 118); `cadence recommend` shows the active surface; `npm view @manehorizons/cadence-core version` reads `1.29.0`.
**If it fails:** `cadence resume` replays this doc. Nothing is broken — this is a clean milestone boundary.
