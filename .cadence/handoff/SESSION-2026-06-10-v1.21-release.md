---
cadence_handoff: 1
generated_at: 2026-06-10T19:30:33.336Z
label: v1.21-release
loop_position: IDLE
active_phase: 96-quickstart
active_draft: 
tier: 
git_branch: milestone/v1
git_dirty: true
git_head: d13efb9
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-06-10 (v1.21-release)

## TL;DR for the next session
- **The whole v1.21 four-slice arc is built and merged/in-review.** A (config explain, #63), B (deepen explain, #64), C (config edit wizard, #65) are **merged to main**; D (quickstart, #66) is **open and green-pending** (phase 96 settled locally; PR pushed).
- **Next session = cut the v1.21.0 release** once #66 merges: a changeset bumping all 4 published packages `1.20.0 → 1.21.0`, `changeset version`, CLAUDE.md narrative update (doc-sync gate), commit, tag — then the **manual `Release` workflow (user-triggered)** publishes to npm. The arg on this handoff was "then we'll start" = start that release.
- **Two human gates before the release phase:** (1) merge PR #66 when its CI is green (`gh pr merge 66 --merge`; the auto-approval classifier *has* allowed merges this session — #64/#65 merged fine); (2) the release phase runs on a fresh branch off merged main.
- **#66 CI was in progress at handoff** (4 legs green, 3 running, `ci-success` pending). A background poller was watching it (id `b5jrh4gtu`) but won't survive into the next session — re-check with `gh pr view 66 --json mergeStateStatus,statusCheckRollup`. Re-run any flaky Windows leg (`gh run rerun <id> --failed`) as was needed for #64.
- **Design + plan for slice D and the whole arc are local** under `docs/superpowers/{specs,plans}/` (gitignored). The release mechanics are scoped in the last assistant turn before this handoff.
- Current published version is still **1.20.0** (`packages/*/package.json`); nothing v1.21 is on npm yet.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `milestone/v1` (dirty), 0 ahead / 0 behind origin
- HEAD `d13efb9`
- Recent commits:
```
d13efb9 chore: settle 96-96 — cadence quickstart
c0d3908 docs(quickstart): command reference + ToC + DESIGN note
656b14a test(quickstart): integration — uninit, init, --json, corrupt-state fallback
3a2c083 feat(quickstart): cadence quickstart command (never-throws) + registration + drift-guard
ad8fc86 feat(quickstart): pure renderText/renderJson
a751711 feat(quickstart): pure buildQuickstart (state-aware orientation, reuses nextAction)
96dfe6f Merge pull request #65 from manehorizons/milestone/v1.21-config-edit
88c10fa Merge remote-tracking branch 'origin/main' into milestone/v1.21-config-edit
```
- Loop: IDLE · phase 96-quickstart · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - (none)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - (none)

## What landed this session
- **Resumed** the prior session and built **slice B** (phase 93, deepen `cadence explain`): a `config` concept + profile×tier→gate-set connection prose + reciprocal cross-links. Merged as PR #64.
- **Slice C** (config edit wizard) — full brainstorm → spec → plan → execute: phase 94 (pure core: field registry, parseChoice, assemble/diff/validate, render, injectable `runWizard`; lifted `setPath`/`coerce` shared with `config set`) + phase 95 (CLI: readline glue, `config edit [field]`, TTY guard, did-you-mean, docs). Subagent-driven via the cadence loop. Merged as PR #65 (resolved a `commands.md`/telemetry merge conflict on the way in).
- **Slice D** (quickstart) — brainstorm → spec → plan → execute: phase 96 (pure `buildQuickstart`/`render` reusing `progress`'s `nextAction`; never-throws CLI `cadence quickstart [--json]`; integration tests incl. corrupt-state fallback; docs + drift-guard). PR #66 open.
- Merged **#64 and #65** to main; re-ran a flaky Windows CI leg on #64 to clear it.
- 1619 core tests green on the quickstart branch; each phase settled with the two-commit convention.

## Carry-forward gotchas
- **Real branch = `milestone/v1.21-quickstart`** — the frontmatter's `git_branch: milestone/v1` is cadence's known dot-truncation display bug. Don't `git checkout milestone/v1`.
- **The uncommitted diff is ephemeral telemetry** (`.cadence/STATE.md`, `.cadence/state.json`) re-touched by `cadence progress`/settle — NOT real work. Safe to discard (`git checkout -- .cadence/STATE.md .cadence/state.json`). It was deliberately kept out of every PR this session. Don't `git restore state.json` mid-phase as a habit, but at IDLE it's fine.
- **Release-phase mechanics (verified):** `.changeset/config.json` has `fixed: []`, so the changeset must explicitly list **all four** packages (`@manehorizons/cadence-core`, `-types`, `-host-claude-code`, `-host-codex`) at `minor`. Bumping `packages/core/package.json` fires the **pre-commit doc-sync gate** → CLAUDE.md must mention `1.21.0` in the same commit (see prior release `2852c55 chore(release): version 1.20.0` + `db37ae8 chore: settle 90-01 — release v1.20.0` for the pattern). The npm publish is the **manual `Release` workflow** (`.github/workflows/release.yml`, `workflow_dispatch`, OIDC) — the user triggers it; you cannot.
- **PR-merge order discipline:** branch each new piece off *merged* main (not a sibling PR branch) to avoid the stacking/conflict tangle that hit #65. The release phase branches off main after #66 merges.
- **Specs/plans are gitignored** (`docs/superpowers/`): slice-D spec `2026-06-10-cadence-quickstart-design.md` + plan `2026-06-10-cadence-quickstart.md`; slice-C equivalents; the original 4-slice design `2026-06-10-cadence-config-explain-design.md`. They stay local — don't try to commit them.
- The settle phases used `--auto` AC verdicts; phase 94 (complex tier) needed `--allow-auto-complex` at approve+settle (profile is `auto`).

## Next action
**Action:** Land slice D, then cut the **v1.21.0** release.
1. Confirm #66 is green and merge it: `gh pr view 66 --json mergeStateStatus,statusCheckRollup` → if `ci-success` green, `gh pr merge 66 --merge` (re-run any flaky Windows leg first if needed). Then `git checkout main && git pull`.
2. Branch the release phase off merged main: `git checkout -b milestone/v1.21-release origin/main`, then `cadence draft new 97-release-v1.21 97 --title="release v1.21.0" --tier=quick-fix` (or standard).
3. In BUILD: add a changeset (`.changeset/<name>.md`) with `minor` bumps for all four published packages; run `pnpm changeset version` (1.20.0 → 1.21.0); update CLAUDE.md's narrative with the v1.21 four-slice summary (config explain · deepen explain · config edit · quickstart) AND the new version (doc-sync gate); commit `chore(release): version 1.21.0`.
4. Settle the phase, push, open the release PR; once merged + tagged `v1.21.0`, **ask the user to trigger the manual `Release` workflow** to publish to npm.

**Verify:** after `changeset version`, `node -p "require('./packages/core/package.json').version"` → `1.21.0` (and the other three match); the doc-sync pre-commit hook passes (CLAUDE.md mentions 1.21.0); `pnpm -w build && pnpm -w test` green; CI green on the release PR.

**If it fails:** if the doc-sync hook blocks the commit, add the `1.21.0` mention to CLAUDE.md and retry (don't `--no-verify`). If `changeset version` doesn't bump all four, the changeset front-matter is missing a package — list all four explicitly (`fixed: []` means no auto-grouping). If unsure whether to fold slice D's CLAUDE.md narrative into the release or a separate doc pass, fold it into the release commit (keeps doc-sync atomic).
