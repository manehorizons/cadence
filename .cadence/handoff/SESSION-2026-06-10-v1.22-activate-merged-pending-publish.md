---
cadence_handoff: 1
generated_at: 2026-06-10T23:52:30.827Z
label: v1.22-activate-merged-pending-publish
loop_position: IDLE
active_phase: 99-activate-doctor
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: a4f28a8
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-06-10 (v1.22-activate-merged-pending-publish)

## TL;DR for the next session
- **v1.22 (verification-activation) is fully built, released to `main`, and docs-synced — but NOT yet on npm.** `cadence activate` (the guided mock→real on-ramp) + a `doctor` `verification-readiness` check + quickstart/config-explain/init wiring. All four published packages are at `1.22.0` on `main`.
- **The single next action = the user triggers the manual `Release` workflow** (GitHub → Actions → Release → Run workflow, `dry_run` unchecked) to publish `1.22.0` to npm + push the `v1.22.0` tag. **You cannot trigger it.** After it runs, verify (`npm view @manehorizons/cadence-core@1.22.0`, tag exists) and flip the two memory files from "pending" to "shipped".
- Built through CADENCE's own loop: phase 98 (activate command) + phase 99 (doctor + wiring), both settled; release cut as phase "100" **failed** and was done outside the loop (see gotchas).
- **Dogfooding bug rec-20260610-001 (high):** CADENCE's phase-id schema `^\d{2}-\d{2}$` (`packages/types/src/{plan,spec}.ts`) caps phases at 99 — phase 100 is unrepresentable. Candidate next dev task; note it can't itself be planned as phase 100 until the schema is widened (chicken-and-egg).
- **Open decision for the user:** 7 remaining "ahead" remote branches (work shipped but tips diverged) — awaiting yes/no to delete (see gotchas). Also `#66`'s red main CI (19:40, 06-10) was never investigated (non-blocking).
- Loop is IDLE; the only uncommitted changes are ephemeral `.cadence/STATE.md`/`state.json` telemetry (safe to discard).

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `a4f28a8`
- Recent commits:
```
a4f28a8 Merge pull request #69 from manehorizons/docs/v1.22-doc-sync
0479443 docs: sync provider/doctor docs with cadence activate + v1.20/v1.22 doctor checks
c48e39f Merge pull request #68 from manehorizons/milestone/v1.22-activate
861d4a9 chore: record rec-20260610-001 — phase-id 99 ceiling (dogfooding finding)
0ea4982 chore(release): version 1.22.0 — verification-activation
62cffd7 chore: settle 99-99 — activate doctor check + wiring (slice 2)
f0edeaf test(activate): cover AC-5 init activate pointer
58370dd feat(activate): wire activate into quickstart, config explain, init
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```
- Loop: IDLE · phase 99-activate-doctor · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260610-001 — Phase-id schema caps phases at 99 (two digits) (candidate/needs-decision)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - `packages/types/src/plan.ts` — affected by rec-20260610-001 Phase-id schema caps phases at 99 (two digits)
  - `packages/types/src/spec.ts` — affected by rec-20260610-001 Phase-id schema caps phases at 99 (two digits)

## What landed this session
- Resumed prior session, cut + shipped **v1.21.0** (quickstart-onboarding): merged PR #66, release PR #67 merged + tagged, **published to npm** (user-triggered Release run, verified live).
- Built **v1.22 verification-activation** end-to-end via subagent-driven development (fresh subagent per task + two-stage spec/quality review): phase 98 `cadence activate` (pure `activate/{assess,plan,render,ping}.ts` + command shell + registration), phase 99 `doctor` `verification-readiness` check + quickstart/config-explain/init wiring. Both settled; reviewers caught + fixed 8 real issues.
- Cut the **v1.22.0** release (changeset → all four packages `1.21.0→1.22.0`, CLAUDE.md + config.md + commands.md + DESIGN.md), PR #68 — **merged to `main`** (`c48e39f`), CI green all 3 OS.
- **Doc-sync audit + fix (PR #69, merged `a4f28a8`):** added the missing `handoff-retention` (v1.20) + `verification-readiness` (v1.22) rows to the `commands.md` doctor table; added a "Fast path — `cadence activate`" section to `docs/providers.md`; pointed `docs/concepts.md` Providers at activate.
- Recorded dogfooding **rec-20260610-001** (phase-id 99 ceiling).
- **Branch cleanup:** deleted 11 merged branches (remote + local); 7 "ahead" branches remain pending user decision.
- Spec + plan for v1.22 are local under `docs/superpowers/{specs,plans}/2026-06-10-cadence-activate*` (gitignored).

## Carry-forward gotchas
- **`git_branch: main` is real this time** (genuinely on merged main, not the v1.21 dot-truncation display bug). HEAD `a4f28a8` = origin/main.
- **Uncommitted `.cadence/STATE.md` + `state.json` are ephemeral telemetry** re-touched by `cadence progress`/handoff — NOT work. Safe to discard (`git checkout -- .cadence/STATE.md .cadence/state.json`). No stash was taken.
- **v1.22.0 release was cut OUTSIDE the CADENCE loop** because `cadence draft new … 100` produces an invalid id `10-100` (schema `^\d{2}-\d{2}$` rejects 3-digit phases) — `draft approve`/coherence fail. So there is no phase-100 `.cadence/phases/` dir; the release is just the `0ea4982 chore(release): version 1.22.0` commit. This is rec-20260610-001.
- **Release mechanics (verified, same as v1.21):** `.changeset/config.json` has `fixed: []`, so list all four published packages explicitly at `minor`. Bumping `packages/core/package.json` fires the pre-commit doc-sync gate → CLAUDE.md must mention the new version in the same commit. npm publish is the **manual `Release` workflow** (`workflow_dispatch`, OIDC) — user-triggered only.
- **The 7 remaining "ahead" remote branches** (`chore/zod-4-migration`, `feat/49-cross-platform-ci`, `feat/52-preset-flag-rename`, `feat/phase-57-rec-promote`, `refactor/phase-54-…`, `refactor/phase-55-…`, `release/v1.6.0`) have commits not reachable from `main`, but their *work* shipped per the CLAUDE.md narrative — almost certainly diverged-but-landed, safe to delete. I did NOT auto-delete them; the user was deciding (offered: delete all 7, or spot-check `feat/49` first). Verify reachability before deleting if unsure.
- **Memory is in the "pending" state:** both `cadence-launch-positioning.md` and `MEMORY.md` say v1.22.0 is MERGED but npm-publish PENDING. Flip to "shipped" only AFTER the Release run succeeds and `npm view` confirms.

## Next action
**Action:** Get v1.22.0 onto npm, then settle the small loose ends.
1. Ask the user to trigger the manual **Release** workflow (GitHub → Actions → Release → Run workflow, `dry_run` unchecked). You cannot trigger it. (Optionally suggest a `dry_run` first.)
2. Once it completes, verify the publish:
   - `for p in core types host-claude-code host-codex; do npm view @manehorizons/cadence-$p@1.22.0 version; done` → all `1.22.0`
   - `git ls-remote --tags origin 'refs/tags/v1.22.0'` → tag exists
3. Flip both memory files (`~/.claude/projects/-home-thomas-projects-cadence/memory/cadence-launch-positioning.md` + `MEMORY.md`) from "MERGED … pending" to "shipped on npm".
4. Then surface the user's open decisions: delete the 7 "ahead" branches? (verify reachability first); scope **rec-20260610-001** (phase-id ceiling) as the next dev task — note it must widen `^\d{2}-\d{2}$` in `packages/types/src/{plan,spec}.ts` (+ id derivation, 99→100 boundary test) and, because phase 100 can't be scaffolded, must itself be done outside the loop or via a re-used 2-digit slot.

**Verify:** `npm view @manehorizons/cadence-core@1.22.0 version` returns `1.22.0`; `node -p "require('./packages/core/package.json').version"` on `main` is already `1.22.0`.

**If it fails:** if the Release run errors, read its logs in the Actions tab (OIDC/provenance + NPM_TOKEN are the usual suspects). Do NOT re-publish a version that already partially published — `npm view` per-package first to see what landed. The tag step is idempotent (skips if `v1.22.0` already on origin).
