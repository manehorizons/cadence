# Handoff — Slice 35 (--sort-by) shipped; v1.2 strategic direction is still the next meta-decision

> Generated by /handoff on 2026-05-28 00:25 UTC by Claude Opus 4.7 (1M context)

<!-- RESUME-KICKOFF-START -->
HANDOFF-VERSION: 1

You are resuming work that was paused by another LLM session.
The full handoff is in ./HANDOFF.md (this file). Before doing anything:

1. Read this entire file end-to-end. Do not skim.
2. Run these checks to verify the environment still matches:
   - `pwd` should be: /home/thomas/projects/cadence
   - `git rev-parse --abbrev-ref HEAD` should be: main
   - `git log -1 --oneline` should start with: eaf1e65
   If any differ, stop and tell the user before proceeding.
3. Pick up at the action described in "## Next step". Verify it with the
   command in that section's "Verify:" line.
4. Treat "## Key decisions" as settled — do not re-open them without the
   user's explicit OK.
5. If you hit anything in "## Open questions / blockers", ask the user
   rather than guessing.
<!-- RESUME-KICKOFF-END -->

## Goal

Pick what's next after Slice 35 (`--sort-by` on the three list commands) shipped cleanly to `origin/main`. The same v1.2 strategic-direction meta-question that the prior two handoffs raised is still open: continue Praxis polish (now 4 un-designed items left), pivot to v1.2 Public release, or pivot to v1.3 Architecture deepening. The polish backlog from here on requires fresh brainstorm sessions for each item; the locked-design backlog is exhausted. Surface this as the first thing.

## Current state

- Branch: `main` (clean working tree; everything pushed to `origin/main`)
- Last commit: `eaf1e65 docs: document --sort-by on list commands + backfill asn/dec list option tables (Slice 35)`
- Uncommitted: none
- Background processes: **`actions.runner.manehorizons-cadence.cadence-dev` systemd service** (self-hosted runner) running on this machine. CI on any push to `main` goes through it. Check via `sudo /home/thomas/actions-runner/svc.sh status` or `gh api repos/manehorizons/cadence/actions/runners --jq '.runners[]'`. Don't `sudo svc.sh stop` it.
- CI status: green on `eaf1e65` (verify with `gh run list --branch main --limit 3`).

## What's done this session

- Resumed prior handoff (`HANDOFF.md` @ 2026-05-27 22:50 UTC) via `/resume` autonomous mode; archived the source handoff to `HANDOFF.md.resumed-2026-05-27-2257` (gitignored) and committed the deletion (`33cebb1`).
- Surfaced the v1.2 strategic-direction meta-question via `AskUserQuestion`; user picked **continued Praxis polish** then chose **`--sort-by`** as the specific polish item.
- Brainstormed `--sort-by` design via `superpowers:brainstorming`: four Q&A resolved scope (rec+asn+dec), value grammar (single key with `:desc` suffix), key menu (broader set — 17 keys total: rec=9, asn=4, dec=4), and reverse interaction (compose, not mutex).
- Wrote and committed the design spec at `docs/superpowers/specs/2026-05-27-cadence-list-sort-by-design.md` (`511f687`). Self-review found one inconsistency (leverageScore test values didn't discriminate numeric from lex); fixed inline before commit.
- Wrote and committed the implementation plan at `docs/superpowers/plans/2026-05-27-slice-35-sort-by.md` (`b7b2ee8`). 6 tasks, ~1570 lines, paired-down structure: Tasks 1-3 per-command (no commit), Task 4 bundled feat commit, Task 5 docs commit, Task 6 verify + push.
- Executed Slice 35 via `superpowers:subagent-driven-development`. Tasks 1-3 each dispatched a Haiku implementer subagent + a Sonnet spec-compliance reviewer + a Sonnet code-quality reviewer. Tasks 4-6 ran inline (coordinator work — gate, commit, docs, push). Final holistic review (Sonnet) approved ship-as-is.
- Shipped feat at `af020b4` (3 commands, 6 source/test files, +932 LoC, +33 tests) and docs at `eaf1e65` (commands.md + backfilled `assumption list` and `decision list` option tables — same precedent as Slice 34.4 which backfilled the rec list table).
- `@cadence/core` test count: 1048 → **1081** (post-Slice-35 baseline). Full turbo gate 16/16 green at every checkpoint. CI on self-hosted runner green in 45s.
- The two follow-up notes from the final holistic review are recorded under `## Open questions / blockers` for the next session to consider.

## Next step

**Action:** Open the v1.2 strategic-direction meta-question with the user via `AskUserQuestion`. The same three options as the prior session: (a) v1.2 Public release — visibility-flip + self-hosted-runner disable + matrix restore + npm publish kickoff; (b) v1.3 Architecture deepening — cherry-pick from `parking/roadmap-v1.3-arch-deepening @ a050083`; (c) continued Praxis polish — **4 un-designed items remain** (one fewer than last session because `--sort-by` shipped): `--filter-text-exact`, `--filter-regex-flags`, `--filter-kind` on `intelligence audit`, graph viewer optimization. Each polish item needs its own brainstorm session.

**Verify:** User picks a direction. Then either (a) read the prior session notes on v1.2 release prep; (b) `git log --oneline -10 parking/roadmap-v1.3-arch-deepening` to scope the cherry-pick set; or (c) invoke `superpowers:brainstorming` on the picked polish item.

**If it fails:** If the user doesn't want any of those three, ask what they DO want — the polish backlog is provisional and the user has flagged it as such across multiple sessions.

## Key decisions

- **Slice 35 design is locked and shipped.** Single-key `--sort-by <key>[:desc]`; ascending default with `:desc` suffix; 17 sortable keys total (rec=9, asn=4, dec=4); enum sort uses Zod declaration order (no per-field collation); composes with `--reverse` (compose, not mutex); JS native stable sort is the tie-break (Node 20+); no shared `sort.ts` helper across the three commands (~45 LoC duplicated, per design Decision Log §9); `milestone list` and `intelligence audit` not touched; no `@cadence/types` schema change.
- **Docs precedent: backfill missing option tables when adding new flags.** Slice 34.4 backfilled `recommendation list`; Slice 35 backfilled `assumption list` + `decision list`. The pattern is established; future slices that add list flags should follow suit if option tables are still missing for that command.
- **Plan execution mode: subagent-driven-development.** Worked cleanly for Slices 34.3, 34.4, and now 35. Haiku implementer + Sonnet two-stage review per task is the proven shape. Code-quality stage catches things spec-compliance misses (in 34.3 it caught the indentation bug; in 35 it surfaced the schema-derived-order asymmetry between rec and asn/dec).
- **GitHub Actions billing reset is still pending.** Until the monthly free quota resets on the billing-cycle date, self-hosted is the path. The runner is DEV-TIME ONLY per the prior handoff's locked decision — never pair self-hosted with a public repo.
- **`ci-success` aggregator kept** even with single test job so the required-check name stays stable across dev-time → release-time matrix changes.

## Open questions / blockers

- **v1.2 strategic direction is the unresolved meta-question** (same as the last two handoffs). See Next step above. Three locked-design polish slices (34.3, 34.4, 35) are now all shipped; the remaining polish requires fresh brainstorms OR a pivot.
- **Slice 35 follow-up (Important, non-blocking from final review):** `ASN_STATUS_ORDER` and `DEC_STATUS_ORDER` in `assumption.ts:30-34` and `decision.ts:48-52` are hand-declared `ReadonlyArray<...>` constants. The recommendation command uses `.options.indexOf()` directly on the exported Zod enums; asn/dec status enums are inline in their object schemas and not top-level exports, so they used manual arrays. A future slice that touches asn/dec list code should refactor to `AssumptionZ.shape.status.options` / `IntelligenceDecisionZ.shape.status.options` to make the sort order schema-derived. Not a bug; cleanup nicety.
- **Slice 35 follow-up (Minor):** Empty-key paths (`--sort-by ''` and `--sort-by :desc`) are handled correctly by `parseSortBy` but have no dedicated test on any of the three commands. Two-liner tests per command would close the coverage gap.
- **Pre-existing observation (Slice 26-era, not Slice 35):** `--offset` appears in empty-result `filterDims` but `--limit` does not — asymmetric. Out of Slice 35 scope; worth a follow-up issue if anyone hits the inconsistency.

## Environment

- CWD: `/home/thomas/projects/cadence`
- OS/shell: Linux / bash
- Node: 20 (per CLAUDE.md `Node >=20`; CI pins exactly 20 during dev-time)
- pnpm: 9.12.0 (pinned in `package.json`)
- Vitest: 2.1.9; `maxForks: 12`; 20s timeouts — do not re-add per-test timeout band-aids
- `@cadence/core` test count: 1081 (post-Slice-35 baseline; was 1048 post-Slice-34.4)
- `@cadence/types` test count: 124
- Repo visibility: PRIVATE (load-bearing for self-hosted-runner safety)
- `gh` CLI: configured and authenticated
- Self-hosted runner: `cadence-dev` (systemd service; labels self-hosted/Linux/X64) — required for ANY CI run on `main`
- Pre-push hook: `.githooks/pre-push` runs the full `pnpm turbo run lint typecheck test build` on pushes to `main`. NOT firing on feature-branch pushes. Local-gate equivalent same command.
- Archived prior handoffs: `HANDOFF.md.resumed-2026-05-25-1530`, `HANDOFF.md.resumed-2026-05-26-2055`, `HANDOFF.md.resumed-2026-05-27-1640`, `HANDOFF.md.resumed-2026-05-27-2257` (gitignored; pattern `HANDOFF.md.resumed-*`).
