# Handoff — Slice 37 (--filter-regex-flags) design approved; next session writes the plan and ships

> Generated by /handoff on 2026-05-28 04:17 UTC by Claude Opus 4.7 (1M context)

<!-- RESUME-KICKOFF-START -->
HANDOFF-VERSION: 1

You are resuming work that was paused by another LLM session.
The full handoff is in ./HANDOFF.md (this file). Before doing anything:

1. Read this entire file end-to-end. Do not skim.
2. Run these checks to verify the environment still matches:
   - `pwd` should be: /home/thomas/projects/cadence
   - `git rev-parse --abbrev-ref HEAD` should be: main
   - `git log -1 --oneline` should start with: 2cffb82
   If any differ, stop and tell the user before proceeding.
3. Pick up at the action described in "## Next step". Verify it with the
   command in that section's "Verify:" line.
4. Treat "## Key decisions" as settled — do not re-open them without the
   user's explicit OK.
5. If you hit anything in "## Open questions / blockers", ask the user
   rather than guessing.
<!-- RESUME-KICKOFF-END -->

## Goal

Ship Slice 37 (`--filter-regex-flags` on the three list commands) end-to-end. The design spec is approved and committed at `2cffb82` — the next session writes the implementation plan via `superpowers:writing-plans`, executes via `superpowers:subagent-driven-development` (same shape that worked cleanly for Slices 34.3 / 34.4 / 35 / 36), and lands feat + docs commits + push. v1.2 strategic-direction meta-question stays open for the session AFTER Slice 37; surface it then.

## Current state

- Branch: `main` — **ahead of `origin/main` by 1 commit (`2cffb82` — Slice 37 design spec, unpushed)**
- Last commit: `2cffb82 docs: design — --filter-regex-flags on list commands (Praxis Slice 37)`
- Uncommitted: none (this handoff will become an untracked file after writing)
- Tags: `v1.1.0`, `v1.1.1` (v1.1.1 on origin; bundles Slices 34.3 / 34.4 / 35 / 36)
- Background processes: **`actions.runner.manehorizons-cadence.cadence-dev` systemd service** (self-hosted runner) running on this machine. CI on any push to `main` goes through it. Check via `sudo /home/thomas/actions-runner/svc.sh status` or `gh api repos/manehorizons/cadence/actions/runners --jq '.runners[]'`. Don't `sudo svc.sh stop` it.
- CI status: green on `afe69cb` (Slice 36 chore-handoff); `2cffb82` not yet pushed so no CI run yet.

## What's done this session

- Resumed prior handoff (`HANDOFF.md` @ 2026-05-28 03:48 UTC) via `/resume` autonomous mode; verified `v1.1.1` tag was already on origin and CI green on both `9a16f90` and `afe69cb`. Archived source handoff to `HANDOFF.md.resumed-2026-05-28-0356` (gitignored).
- Surfaced the v1.2 strategic-direction meta-question via `AskUserQuestion`; user picked **continued Praxis polish** then chose **`--filter-regex-flags`** as the specific polish item.
- Brainstormed `--filter-regex-flags` design via `superpowers:brainstorming`: four Q&A resolved allowlist (`{i, m, s, u}` curated, not pass-through-to-RegExp), value grammar (concatenated letter string mirroring JS RegExp's native second arg), orphan-flag policy (refuses without `--filter-regex`), and validation strictness (empty refuses, duplicates refuse, invalid letters refuse — each error names the specific letter).
- Wrote and committed the design spec at `docs/superpowers/specs/2026-05-27-cadence-list-filter-regex-flags-design.md` (`2cffb82`). Self-review found no issues (12-item Decision Log includes the no-Co-Authored-By policy correction as Decision #11).
- Verified the design commit body contains zero `Co-Authored-By` trailer instances — corrected the process note flagged in last session's holistic review.
- HANDOFF.md (the /resume source from earlier this morning at 03:48 UTC) archived to `HANDOFF.md.resumed-2026-05-28-0356` and its tracked deletion rode along with the spec commit, matching the `1b0fb34` and `33cebb1` precedents.
- User approved the design, then requested this handoff BEFORE writing the implementation plan. Plan + execution are the next session's work.

## Next step

**Action:** Invoke `superpowers:writing-plans` on the approved spec at `docs/superpowers/specs/2026-05-27-cadence-list-filter-regex-flags-design.md`. The plan should mirror Slice 35 / 36's shape: 6 tasks (Tasks 1-3 per-command implementation with TDD red-green; Task 4 bundled feat commit; Task 5 docs commit on `commands.md`; Task 6 final verify + push). 19 new tests target (rec=7, asn=6, dec=6 — the duplicate-letter parse test only lives in `recommendation.test.ts` to avoid 3× redundancy on the module-local helper). Test count delta: 1113 → ~1132.

After the plan is committed, push `main` to ship BOTH the design commit (`2cffb82`, currently unpushed) AND the new plan commit together (Praxis convention: design + plan committed before execution starts). Then transition to `superpowers:subagent-driven-development` to execute Tasks 1-6.

**Verify:** `docs/superpowers/plans/2026-05-27-slice-37-filter-regex-flags.md` exists and is committed; `git log --oneline -3` shows design + plan + (prior chore-handoff) on main; `git push origin main` succeeds; self-hosted runner CI green on the plan commit. Then proceed to executing Task 1 (rec).

**If it fails:** If `writing-plans` produces a plan with placeholders or non-existent helper references (the prior session caught a fake `listRecIds` helper that doesn't exist — actual is `addRecommendation` from `../../src/intelligence/store.js`), fix inline before committing. Use the Slice 36 plan as a structural reference; the test seed pattern for rec uses `await run(['recommendation', 'add', ...])` while asn/dec use `const rec = await addRecommendation(active.root, { ... })` with the canonical minimal seed shape.

## Key decisions

- **Slice 37 design is locked and approved.** Companion flag `--filter-regex-flags <flags>` to existing `--filter-regex`; curated allowlist `{i, m, s, u}` (no `g`/`y` because stateful, no `d`/`v` because YAGNI); letter-string grammar mirroring JS RegExp's native second arg; strict validation (empty/duplicate/invalid all refuse with specific errors); orphan use refuses; one-line apply change `new RegExp(opts.filterRegex, regexFlags)`; no participation in the three-way text-mode mutex (it's a parameter to --filter-regex, not a fourth filter mode); no shared `regex-flags.ts` helper (~14 LoC × 3 inline per Decision Log §8); `milestone list` and `intelligence audit` not touched; no `@cadence/types` schema change.
- **No `Co-Authored-By` trailer on any Slice 37 commit.** Verified absence on `2cffb82` via `git show --format=%B | grep -c Co-Authored-By` → 0. Future feat/docs commits in this slice must also be verified; the prior session's `1b0fb34` accidentally carried one and was the only such violation in the 4-slice polish bundle.
- **Plan execution mode: subagent-driven-development.** Proven through Slices 34.3 / 34.4 / 35 / 36. Haiku implementer + Sonnet spec-compliance reviewer + Sonnet code-quality reviewer per task, plus a final holistic Sonnet review before push. Tasks 4-6 run inline (coordinator work — gate, bundled commit, docs, push).
- **GitHub Actions billing reset is still pending.** Until the monthly free quota resets on the billing-cycle date, self-hosted is the path. The runner is DEV-TIME ONLY — never pair self-hosted with a public repo.
- **`ci-success` aggregator kept** even with single test job so the required-check name stays stable across dev-time → release-time matrix changes.

## Open questions / blockers

- **v1.2 strategic direction is still the unresolved meta-question** (same as the last seven handoffs). Surface AFTER Slice 37 ships. Five locked-design polish slices (34.3, 34.4, 35, 36, 37) will then all be shipped; the remaining polish backlog is **2 un-designed items**: `--filter-kind` on `intelligence audit`, graph viewer optimization. Each needs its own brainstorm session OR a pivot to v1.2 Public release / v1.3 Architecture deepening.
- **Slice 37 design commit (`2cffb82`) is unpushed** — main is 1 commit ahead of origin/main. The next session's push (after the plan commit lands) should ship both atomically. Don't push the design commit alone before the plan exists — that breaks the Praxis "design + plan committed before execution" convention.
- **Slice 36 follow-up still deferred (Important, non-blocking):** The Slice 36 plan doc's architecture summary paragraph misdescribes `decision.ts`'s `--filter-rec` position. Doc-prose cleanup, not a code bug; the Slice 36 implementation itself is correct. Tighten if the pattern recurs in this slice's plan.
- **Slice 36 follow-up still deferred (Minor):** AC-exact-9 (composition test) on each command does not exhaustively verify sort ordering when multiple entries survive all filters; this is fine because Slice 35's own sort tests cover the engine. Mirror this stance in Slice 37's AC-flags-6 if applicable.
- **Pre-existing observation (Slice 26-era, not Slice 37):** `--offset` appears in empty-result `filterDims` but `--limit` does not — asymmetric. Out of Slice 37 scope; worth a follow-up issue if anyone hits the inconsistency.

## Environment

- CWD: `/home/thomas/projects/cadence`
- OS/shell: Linux / bash
- Node: 20 (per CLAUDE.md `Node >=20`; CI pins exactly 20 during dev-time)
- pnpm: 9.12.0 (pinned in `package.json`)
- Vitest: 2.1.9; `maxForks: 12`; 20s timeouts — do not re-add per-test timeout band-aids
- `@cadence/core` test count: 1113 (post-Slice-36 baseline; Slice 37 target ~1132)
- `@cadence/types` test count: 124
- Repo visibility: PRIVATE (load-bearing for self-hosted-runner safety)
- `gh` CLI: configured and authenticated
- Self-hosted runner: `cadence-dev` (systemd service; labels self-hosted/Linux/X64) — required for ANY CI run on `main`
- Pre-push hook: `.githooks/pre-push` runs the full `pnpm turbo run lint typecheck test build` on pushes to `main`. NOT firing on feature-branch pushes. Local-gate equivalent same command.
- Tags: `v1.0.0`, `v1.1.0`, `v1.1.1` (all pushed). v1.1.1 annotates Slices 34.3 / 34.4 / 35 / 36 as the polish patch since v1.1.0.
- Archived prior handoffs: `HANDOFF.md.resumed-2026-05-25-1530`, `HANDOFF.md.resumed-2026-05-26-2055`, `HANDOFF.md.resumed-2026-05-27-1640`, `HANDOFF.md.resumed-2026-05-27-2257`, `HANDOFF.md.resumed-2026-05-28-0053`, `HANDOFF.md.resumed-2026-05-28-0356` (gitignored; pattern `HANDOFF.md.resumed-*`).
