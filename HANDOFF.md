# Handoff — Self-hosted runner branch ready; install runner + verify CI

> Generated by /handoff on 2026-05-26 04:24 UTC by Claude Opus 4.7 (1M context)

<!-- RESUME-KICKOFF-START -->
HANDOFF-VERSION: 1

You are resuming work that was paused by another LLM session.
The full handoff is in ./HANDOFF.md (this file). Before doing anything:

1. Read this entire file end-to-end. Do not skim.
2. Run these checks to verify the environment still matches:
   - `pwd` should be: /home/thomas/projects/cadence
   - `git rev-parse --abbrev-ref HEAD` should be: ci/self-hosted-runner
   - `git log -1 --oneline` should start with: 8ff749e
   If any differ, stop and tell the user before proceeding.
3. Pick up at the action described in "## Next step". Verify it with the
   command in that section's "Verify:" line.
4. Treat "## Key decisions" as settled — do not re-open them without the
   user's explicit OK.
5. If you hit anything in "## Open questions / blockers", ask the user
   rather than guessing.
<!-- RESUME-KICKOFF-END -->

## Goal

Restore CI from a 10-day red streak by switching to a self-hosted Linux runner during the development phase. The workflow change is already committed on this feature branch (`ci/self-hosted-runner`); the runner itself still needs to be installed on the user's dev machine and registered with GitHub. Once registered, verify CI passes on this branch, then merge to main. The user plans to switch back to the GitHub-hosted multi-OS matrix at the v1.2 public-release milestone.

NOTE: the user is on a DIFFERENT MACHINE than the one that produced this handoff. The runner-install step happens on whatever machine they want CI to run on; their handoff-target machine may or may not be that same machine.

## Current state

- Branch: `ci/self-hosted-runner` (tracks `origin/ci/self-hosted-runner`)
- Last commit: `8ff749e — ci: switch to self-hosted Linux runner during development`
- Uncommitted: `HANDOFF.md` (this file — about to be committed as the next session-handoff commit)
- Pre-existing untracked on the prior machine but won't be on the new one: `HANDOFF.md.bak`, `HANDOFF.md.resumed-2026-05-25-1530`. Both gitignored as of commit 2d56730. Safe to ignore if absent on the new machine; safe to delete if present.
- Background processes: none.
- `main` HEAD: `ebc65dd Revert "ci: switch to self-hosted Linux runner during development"` — i.e. main reverted the self-hosted change so the workflow there is back to the GitHub-hosted multi-OS matrix (which currently fails — see Open questions). The change lives only on this feature branch now.

## What's done this session

- Resumed prior handoff (`abdec06` — "Slice 34 brainstorm complete"); the brainstormed Slice 34 design was already approved, just needed writing.
- Shipped Praxis Slice 34 — wrote the upstream design doc at `docs/superpowers/specs/2026-05-25-cadence-rec-phase-linkage-design.md`. Single commit `911539b`.
- Shipped Praxis Slice 34.1 — recommendation convert transition (`Recommendation.convertedToPhaseId` schema field, `applyRecommendationTransition` pure helper, `runRecommendationTransition` I/O wrapper with `phaseDirectoryExists` FK check, `cadence recommendation convert` CLI subcommand, detail-render bullet). Two commits: `00facfe` (feat) + `00098aa` (docs). +24 core tests.
- Shipped Praxis Slice 34.2 — `intelligence audit` `stale-converted-phase` finding kind (8th audit kind; new `existingPhaseIds: Set<string>` param on `computeIntelligenceAudit` with defaulted-to-empty for backcompat; new render section + remediation hint). Two commits: `d11d8e6` (feat) + `b6db0a6` (docs). +16 core tests.
- Merged PR #9 (Praxis: strategic-intelligence layer) to `main` as merge commit `e34be04`. Used `--merge` (preserves all 225 slice commits, no squash). PR was DRAFT — had to `gh pr ready 9` first.
- Created and pushed annotated tag `v1.1.0 — Praxis: strategic-intelligence layer (Slices 1–34.2)` on `e34be04`.
- Refreshed `.cadence/MILESTONES.md` (commit `f8797e8`): moved v0.4–v1.0 entries from "Planned" to "Shipped" (they shipped per ROADMAP); added v1.1 milestone-work entry (Phases 29.x–38 shipped but never separately tagged); added the new v1.1.0 Praxis entry; replaced empty "Planned" with the two v1.2 tracks + v1.3 parking branch.
- Cleanup commit `2d56730`: tracked the previously-untracked agent-config files (AGENTS.md, CLAUDE.md, docs/agents/{domain,issue-tracker,triage-labels}.md); gitignored session-handoff backups (`HANDOFF.md.bak`, `HANDOFF.md.resumed-*`); removed the consumed `HANDOFF.md` from the praxis handoff cycle.
- Diagnosed the CI red-streak: 95 of last 100 runs failed in 2–3 seconds with `steps: []` in the job API response. Pattern is consistent with GitHub Free private-repo Actions minutes exhausted (2000/mo cap) AND spending limit at default $0 (which means overage fails fast instead of billing through). Workflow file content is fine. User confirmed quota theory; not willing to pay for overage yet; chose self-hosted runner during development.
- Committed self-hosted-runner workflow change to `main` as `8ff749e` (single Linux runner, Node 20 only, drops OS×Node matrix temporarily; `ci-success` aggregator kept so required-check name stays stable across the dev→release matrix change). User then requested this commit be moved to a feature branch instead.
- Created branch `ci/self-hosted-runner` from current main HEAD (carrying `8ff749e`), pushed with `-u`, switched back to `main`, reverted `8ff749e` on main (creating `ebc65dd`), pushed. Chose REVERT instead of force-push because user hadn't explicitly authorized force-push to main (CLAUDE.md rule). See "Key decisions" for the merge-back implication this creates.

## Next step

**Action:** Walk the user through installing a self-hosted runner. The playbook (delivered earlier in the prior session's chat):

1. **User opens browser:** `https://github.com/manehorizons/cadence/settings/actions/runners/new` → select Linux × x64. GitHub displays a copy-pasteable block (curl + tar + `./config.sh`) with a registration token baked in. Token expires in ~1 hour.
2. **User runs the install block** on whichever machine they want CI to run on (most likely the new Linux machine they just switched to):
   ```
   mkdir -p ~/actions-runner && cd ~/actions-runner
   # paste the curl + tar + ./config.sh from the UI
   # ./config.sh prompts: group (default), name (e.g. cadence-dev),
   #   additional labels (none — workflow only needs `self-hosted` + auto `linux`),
   #   work folder (default)
   ```
3. **User starts the runner.** Two modes:
   - Interactive: `./run.sh`
   - Service (recommended): `sudo ./svc.sh install && sudo ./svc.sh start`
4. **User tells you when registration is done** ("done" is enough — runner doesn't need to be fully started yet, registration is what matters).

**Verify:**
```
gh api repos/manehorizons/cadence/actions/runners --jq '.runners[] | {name, status, labels: [.labels[].name]}'
```
Should show at least one runner with `status: "online"` and labels including both `self-hosted` and `linux`.

Once the runner shows online, trigger CI verification:
```
gh run rerun --failed <most-recent-failed-run-id-on-ci/self-hosted-runner>
# or: push a trivial whitespace change to trigger a fresh run
```
Expected: the queued/pending CI runs on this branch immediately drain to the self-hosted runner and either pass (green) or fail with a real reason (now visible — actual step output, not 3-second `steps: []` failure).

**If it fails:**
- If the runner shows `offline`: `./run.sh` or `sudo ./svc.sh status` to diagnose.
- If the workflow still fails fast with `steps: []` even after the runner is online: runner installed but token expired or repo permissions denied — re-run `./config.sh` with a fresh token from the UI.
- If a real step fails: investigate the step output. Most likely the runner's Linux environment is missing a system dep (e.g. unprivileged user namespaces for sandboxing) — but pnpm + Node-only CI should be fine on standard Ubuntu/Debian.

After CI is green on `ci/self-hosted-runner`: see "Open questions / blockers" §1 for the merge-back-to-main approach (the revert-on-main creates a subtlety).

## Key decisions

- **v1.1.0 = Praxis (not the publish-pipeline track that was internally named "v1.1" but never tagged).** User explicitly chose this when prompted with three tag-name options. Settled.
- **Praxis closure path was "strategic" (per prior handoff at b34daae), not "quick":** ship Slice 34 (design) + 34.1 (core convert) + 34.2 (audit dim) before merging PR #9. All three shipped this session. Praxis v1 is complete; deferred polish (Slices 34.3 + 34.4 + `--sort-by` + `--filter-text-exact` + `--filter-regex-flags` + `--filter-kind` on audit + graph viewer optimization) are nice-to-have, not blockers.
- **Self-hosted runner is DEV-TIME ONLY.** Workflow comment block says "the full multi-OS × Node 20+22 matrix returns at the v1.2 public-release milestone — restore the previous strategy block before tagging." Don't normalize the self-hosted-only state.
- **Workflow uses `runs-on: [self-hosted, linux]`** — both labels required (`self-hosted` is auto-applied on registration; `linux` is auto-applied when installed on Linux). No custom labels needed.
- **Node 20 only in dev-time CI** (matches CLAUDE.md "Node >=20" minimum). The pre-push hook still does the four-command pipeline locally; lost remote multi-version coverage is acceptable during dev. Restore `node: [20, 22]` at release.
- **`ci-success` aggregator KEPT even with single test job** — so the required-check name stays `ci-success` across the dev-time → release-time matrix change. Don't have to retune branch protection when the matrix returns.
- **Self-hosted runner ⊕ public repo = NEVER.** Currently safe (repo is private). If the user later flips visibility to public (which is on the v1.2 Public-release roadmap), DISABLE the self-hosted runner BEFORE the visibility change. Public forks can run arbitrary code on a self-hosted runner.
- **Revert-not-force-push on main when moving `8ff749e` to the feature branch.** CLAUDE.md: "Never force push to main/master, warn the user if they request it." User hadn't explicitly authorized force-push, so I chose `git revert` instead. Trade-off: the next PR for `ci/self-hosted-runner` is degenerate (the branch's only commit is already in main's history as an ancestor of the revert; standard merge sees "no commits to add"). See Open questions §1 for the resolution menu the next LLM can walk the user through.
- **MILESTONES.md was substantially out of date pre-Praxis:** v0.4–v1.0 had been "Planned" even though ROADMAP confirmed they shipped. The MILESTONES rewrite this session corrected that staleness; don't re-introduce the lag.
- **ROADMAP.md INTENTIONALLY does not list Praxis as a phase entry.** Praxis used a different planning surface (`docs/superpowers/specs/`) than `.cadence/phases/` that ROADMAP indexes. The substance of Praxis lives in those design docs + CHANGELOG.md + the rec/assumption/decision artifact files Praxis itself created. Don't add a Praxis section to ROADMAP.

## Open questions / blockers

- **Merge-back approach for `ci/self-hosted-runner` once CI is green.** Because main was reverted (not force-pushed), commit `8ff749e` is already an ANCESTOR of main HEAD (`ebc65dd` reverted it). Standard PR merge sees no commits to add. Options to surface to the user when they're ready:
  - **(a)** Revert the revert on main (`git revert ebc65dd` → creates an un-revert commit; main history reads: change → revert → un-revert; mergeable but ugly).
  - **(b)** Cherry-pick `8ff749e` onto current main, force-push `ci/self-hosted-runner` with the new SHA. Cleanest history; needs force-push of the FEATURE branch (not main), which is fine without special authorization.
  - **(c)** Squash-merge the PR via GitHub UI — collapses to one new commit on main with the workflow change. Clean. May or may not be feasible depending on GitHub's behavior with the "no commits to add" state — try this first.
  - **(d)** Force-push main back to `2d56730` (before `8ff749e`) — would erase both `8ff749e` and `ebc65dd` from main. Requires explicit user authorization for force-push to main (CLAUDE.md rule). Cleanest result; highest blast radius.
  - **Recommended path:** try (c) first; if GitHub refuses, fall back to (b). Avoid (a). Only do (d) with explicit user OK and confirmation no one else has pulled main since `8ff749e`.
- **CI is blocked on `ci/self-hosted-runner` until the runner is installed.** No CI runs will execute on this branch until then. The 3-second-fail-with-`steps:[]` pattern from earlier this session is now replaced with "pending forever" (queued waiting for the runner). Once the runner appears, the queue drains immediately.
- **CI is ALSO still broken on main** (now with the same red signal — every push fails fast because the workflow there still targets GitHub-hosted runners and the quota is still out). User accepted this trade-off implicitly (they chose self-hosted for dev; main's CI red signal mirrors that). If anyone other than the user looks at main's checks they'll see red. The PR for this feature branch, once merged, will restore CI to working state on main.
- **v1.2 strategic direction is the next decision after CI is restored.** Three named options from the prior session, in user-pickable order:
  1. **v1.2 Public release** — gated on the repo-visibility decision (npm provenance requires public source; making the repo public exposes all `.cadence/` history). Concrete, scoped, ROADMAP-defined.
  2. **v1.3 Architecture deepening** — cherry-pick from `parking/roadmap-v1.3-arch-deepening` @ commit `a050083` onto a fresh branch off the new main. Bigger investment; needs a kickoff session to scope.
  3. **Praxis polish** — Slices 34.3 (`spec new --from-rec`) + 34.4 (`recommendation list --filter-converted-to`) + assorted small follow-ons. Smallest; good warmup.

   Don't pick for the user — surface as an `AskUserQuestion` after CI green.
- **Untracked files on the new machine:** `AGENTS.md`, `CLAUDE.md`, `docs/agents/` are now TRACKED as of commit `2d56730`. `HANDOFF.md.bak` + `HANDOFF.md.resumed-*` are now gitignored. Fresh `git clone` on the new machine should be clean.
- **GitHub Actions billing reset:** user noted not ready to pay yet. If/when they want GitHub-hosted CI back without paying, the monthly free quota resets on the calendar billing-cycle date (the user can check on `https://github.com/settings/billing/summary`). Until then, self-hosted is the path.

## Environment

- CWD: `/home/thomas/projects/cadence`
- OS/shell: Linux / bash
- Node: 20 (per CLAUDE.md "Node >=20" minimum; the workflow now pins exactly 20)
- pnpm: 9.12.0 (pinned in `package.json`)
- Vitest: 2.1.9 (per `vitest.shared.ts`; `maxForks: 12`; 20s timeouts)
- Tag just cut: `v1.1.0` (annotated, pushed)
- `@cadence/core` test count: 1034 (up from 994 at start of session)
- `@cadence/types` test count: 124 (up from 122)
- Repo visibility: PRIVATE (load-bearing — see Key decisions on self-hosted-runner safety)
- `gh` CLI is configured and authenticated (the prior LLM used it extensively for PR + tag + API queries this session).
- Pre-push hook: `.githooks/pre-push` runs full turbo gate on pushes to `main`. NOT firing on feature-branch pushes. Local-gate equivalent: `pnpm turbo run lint typecheck test build`.
