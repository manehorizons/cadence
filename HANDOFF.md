# Handoff — Slice 37 plan committed locally; Linux session executes Tasks 1-6 via subagent-driven-development

> Generated by /handoff on 2026-05-28 20:45 UTC by Claude Opus 4.7 (1M context)

<!-- RESUME-KICKOFF-START -->
HANDOFF-VERSION: 1

You are resuming work that was paused by another LLM session.
The full handoff is in ./HANDOFF.md (this file). Before doing anything:

1. Read this entire file end-to-end. Do not skim.
2. Run these checks to verify the environment still matches:
   - `pwd` should be: /home/thomas/projects/cadence
   - `git rev-parse --abbrev-ref HEAD` should be: slice-37-handoff-from-windows
   - `git log -1 --oneline` should start with: 87639de
   Branch expectation note: this handoff was pushed to a feature branch (NOT main) because the Windows pre-push hook would have blocked main due to 3 Windows-only test failures. The Next step's first action is to fast-forward main to this branch, switch to main, and push to origin/main from the (working) Linux environment. If you're on `main` directly via `git pull`, that ALSO works — the env-check is informational, not load-bearing.
   If anything else differs significantly, stop and tell the user before proceeding.
3. Pick up at the action described in "## Next step". Verify it with the
   command in that section's "Verify:" line.
4. Treat "## Key decisions" as settled — do not re-open them without the
   user's explicit OK.
5. If you hit anything in "## Open questions / blockers", ask the user
   rather than guessing.
<!-- RESUME-KICKOFF-END -->

## Goal

Execute Tasks 1–6 of Slice 37 (`--filter-regex-flags` on `recommendation list` / `assumption list` / `decision list`) per the implementation plan committed locally at `87639de`. End state: feat + docs commits on `main`, full turbo gate 16/16 green, `@cadence/core` test count 1113 → 1132, push successful, CI green on `cadence-dev` self-hosted runner. After Slice 37 ships, surface the v1.2 strategic-direction meta-question.

## Current state

- Branch: at handoff push time, the two new commits (`87639de` plan + this handoff commit) live on remote branch **`origin/slice-37-handoff-from-windows`** — NOT yet on `origin/main`. Local `main` on the Windows machine has both commits but the push to `origin/main` was deferred because the Windows pre-push hook fails on 3 known-Windows-only Slice 34.4 test failures (CI on Linux passes the same source). The Linux session's first job is to fast-forward `origin/main` to this branch (see Next step).
- Last commit on the feature branch: this handoff. Last commit before: `87639de docs: Slice 37 implementation plan (--filter-regex-flags on list commands)`. Source state at HANDOFF.md write time was `87639de` + an untracked HANDOFF.md.
- Plan file: `docs/superpowers/plans/2026-05-27-slice-37-filter-regex-flags.md` (1373 lines; six tasks; spec-coverage table at end)
- Design source (already on `origin/main`): `docs/superpowers/specs/2026-05-27-cadence-list-filter-regex-flags-design.md` (commit `2cffb82`)
- Uncommitted: none (handoff itself becomes the only untracked file, then commits)
- Background processes: **`actions.runner.manehorizons-cadence.cadence-dev` systemd service** (self-hosted runner) running on the Linux dev box. CI on any push to `main` goes through it. Check via `sudo /home/thomas/actions-runner/svc.sh status` or `gh api repos/manehorizons/cadence/actions/runners --jq '.runners[]'`. Don't `sudo svc.sh stop` it.
- CI status: green on `8919544` (prior chore-handoff). `2cffb82` (Slice 37 design) and `87639de` (Slice 37 plan) not yet pushed at handoff time; will land together when the Linux session pushes.

## What's done this session

- Resumed prior handoff (`HANDOFF.md` @ 2026-05-28 04:17 UTC on `origin/main`) via `/resume` on **Windows**. Env-check showed CWD mismatch (Linux→Windows) and HEAD `8919544` vs expected `2cffb82` (the chore-handoff commit landed one ahead of where the prior session's handoff was authored); both flagged as benign and approved by user to proceed. Source HANDOFF.md archived to `HANDOFF.md.resumed-2026-05-28-1645` (gitignored).
- Invoked `superpowers:writing-plans` on the approved Slice 37 design spec. Wrote `docs/superpowers/plans/2026-05-27-slice-37-filter-regex-flags.md` (1373 lines): six tasks, full code blocks, exact line-number anchors against post-Slice-36 source. Self-review covered spec coverage (no gaps), placeholder scan (none), and type consistency. One unilateral design choice flagged in Step 1.4: source-side `--filter-regex` description in commander is NOT modified in Slice 37 because the mutex matrix is unchanged from Slice 36 — cross-reference lives in the new flag's description and in docs (Task 5.2), not in the existing flag's source description. Differs from Slice 36 precedent intentionally.
- Committed plan at `87639de` (`docs: Slice 37 implementation plan (--filter-regex-flags on list commands)`). Bundled the tracked `HANDOFF.md` deletion (residue from /resume) into the same commit per `33cebb1` / `1b0fb34` precedents. Verified zero `Co-Authored-By` trailer instances on the commit body. **Plan commit `87639de` is unpushed.**
- Transitioned to `superpowers:subagent-driven-development`. Dispatched Task 1 implementer (Haiku). Implementer correctly applied all 11 steps: `parseRegexFlags` helper + `ALLOWED_REGEX_FLAGS` constant at module scope, `--filter-regex-flags` option declaration, action callback typing update, orphan-check + parse block before `--filter-regex` apply, one-line change `new RegExp(opts.filterRegex, regexFlags)`, filterDims push between `regex=` and `converted-to=`. Added 7 new tests in `recommendation.test.ts` — all 7 PASS on Windows.
- **Discovered 3 pre-existing Slice 34.4 tests fail consistently on Windows** (not introduced by Slice 37; Slice 37 source diff doesn't touch the code paths these tests exercise). Failing tests: `recommendation.test.ts > 'Slice 34.4 AC-3: candidate/accepted/deferred/rejected recs are excluded'`, `'Slice 34.4 AC-4: --filter-converted-to + --reverse → filter then reverse subset'`, and a third (likely `AC-5`) — all use `--filter-converted-to` + the dynamic-`writeFile`-then-`spawn` ledger-edit pattern. Confirmed pre-existing TS errors in `packages/core/src/intelligence/store.ts` (lines 901, 905 — `convertedToPhaseId` property type missing) ALSO exist on `origin/main`, so baseline parity is intact. The handoff originally said CI on `origin/main` is green — therefore these are Windows-only failures (Windows + pnpm + tempRepo + ledger-edit-then-spawn race), not a real regression.
- User authorized discarding the Windows Task 1 WIP and switching execution to Linux. WIP discarded via `git checkout -- packages/core/src/cli/commands/recommendation.ts packages/core/tests/cli/recommendation.test.ts`. Working tree clean at `87639de`. This handoff written to direct the next session to Linux.

## Next step

**Action:** Run /resume on Linux. Phase 1 discovery will find this HANDOFF.md on `refs/remotes/origin/slice-37-handoff-from-windows` (the freshest HANDOFF.md across all refs) and offer to switch to that branch. Accept the switch. Once on the local tracking branch `slice-37-handoff-from-windows`, run this preamble BEFORE invoking subagent-driven-development:

```bash
# Fast-forward main to the feature branch, push, clean up.
git fetch --all --prune
git checkout main
git pull --ff-only origin main   # ensure local main matches origin/main
git merge --ff-only slice-37-handoff-from-windows  # fast-forwards main locally
git push origin main             # triggers Linux pre-push hook (should pass 16/16)
# After origin/main is updated, clean up the now-redundant feature branch:
git branch -D slice-37-handoff-from-windows
git push origin --delete slice-37-handoff-from-windows
```

Then invoke `superpowers:subagent-driven-development` to execute Tasks 1-6 from `docs/superpowers/plans/2026-05-27-slice-37-filter-regex-flags.md`. Same shape proven through Slices 34.3 / 34.4 / 35 / 36: Tasks 1-3 (per-command) dispatch Haiku implementer + Sonnet spec-compliance reviewer + Sonnet code-quality reviewer per task; main thread reviews diff between tasks; subagents leave working tree dirty per the plan's explicit "Do NOT commit" instruction in Step X.11. Tasks 4-6 run inline (coordinator). Task 4 bundles Tasks 1-3 into one feat commit; Task 5 docs commit; Task 6 final verify + push.

**Verify:**
1. After the preamble: `git rev-parse --abbrev-ref HEAD` is `main`; `git log --oneline -3` shows handoff + `87639de` (plan) + (chore-handoff `8919544` or design `2cffb82` — whichever is the third-newest); `git branch -a | grep slice-37-handoff` returns nothing (branch cleaned up); CI on the `cadence-dev` runner green on the handoff sha.
2. After Task 1: `git status --porcelain` shows two modified files only (`rec.ts` + `rec.test.ts`); `pnpm --filter @cadence/core test -- recommendation.test` passes ALL tests (the 3 Slice 34.4 failures should NOT appear on Linux per the handoff's CI-green baseline).
3. After Task 4: `pnpm turbo run lint typecheck test build` → 16/16 green; `git log -1 --oneline` reads `<sha> feat(core): --filter-regex-flags on recommendation/assumption/decision list (Slice 37)`; commit body has zero `Co-Authored-By` instances (`git log -1 --format=%B | grep -c Co-Authored-By` → `0`).
4. After Task 6: `pnpm --filter @cadence/core test` total passed = **1132** (1113 baseline + 19 new). CI on the `cadence-dev` self-hosted runner green.

**If it fails:**
- If the preamble's `git push origin main` (after the fast-forward) is BLOCKED by the pre-push hook: the Linux gate is failing on a real issue (the Windows-only failures should NOT appear on Linux). Investigate the gate failure. Do NOT `--no-verify` — Linux is the canonical dev environment and a Linux gate failure is load-bearing.
- If `git merge --ff-only slice-37-handoff-from-windows` complains "not possible to fast-forward": something diverged on origin/main since the Windows session pushed the feature branch. Likely cause: someone pushed a hot-fix or dependabot landed something. Inspect with `git log --oneline origin/main..slice-37-handoff-from-windows` and `git log --oneline slice-37-handoff-from-windows..origin/main`. Probably needs a rebase, but check with user first.
- If the Linux session ALSO sees the 3 Slice 34.4 tests fail in Task 1: it's NOT Windows-only — investigate as a real regression. Re-examine the Task 1 source diff (`parseRegexFlags` helper + option + orphan-check + RegExp call change + filterDims) for any subtle effect on `--filter-converted-to`. The likely culprit would be a pipeline ordering issue, but the plan places the orphan-check block IMMEDIATELY BEFORE the `--filter-regex` apply (line 290), well after `--filter-converted-to` (which is in its own block at lines 311-313 in the plan's pre-state line map, untouched).
- If `Co-Authored-By` trailer slips into any commit body: `git commit --amend` to remove BEFORE pushing; the Slice 37 design Decision Log §11 makes this a hard rule (the 1b0fb34 precedent was a violation).
- If push fails on the pre-push hook for legitimate gate failures (lint/typecheck/test/build): fix the failing gate dimension and re-push. Do NOT `--no-verify` — the runner CI runs the same gate so bypassing locally just defers the failure.
- If implementer subagent reports DONE but spec-compliance reviewer finds drift (e.g., parseRegexFlags helper placed in wrong scope, or filterDims push order wrong): re-dispatch implementer with the specific spec-reviewer findings to fix; re-run spec review; then code-quality review; then mark task complete.

## Key decisions

- **Slice 37 design is locked and approved at `2cffb82`** (Praxis spec — 12-item Decision Log). All 12 design decisions hold: curated allowlist `{i, m, s, u}` (no g/y/d/v); letter-string grammar; strict validation (empty/duplicate/invalid all refuse with specific letter named); orphan use refuses; one-line apply change; no shared `regex-flags.ts` helper (~14 LoC × 3 inline); no participation in three-way text-mode mutex; `milestone list` and `intelligence audit` untouched; no `@cadence/types` schema change; symmetric across all three list commands.
- **Slice 37 plan is locked and committed at `87639de`** — six tasks, exact code blocks, line-number anchors against post-Slice-36 source. Self-reviewed; spec coverage table shows no gaps. Linux execution should follow the plan verbatim; deviations require user OK.
- **NO `Co-Authored-By` trailer on any Slice 37 commit.** Verified `0` on `2cffb82` (design) and `87639de` (plan). Every subsequent commit (feat at Task 4, docs at Task 5) MUST be verified the same way before push. The Slice 37 spec Decision Log §11 makes this a hard rule following the `1b0fb34` violation precedent. The plan includes verification steps at Tasks 4.2, 4.5, 5.8, 6.4.
- **Execution mode: subagent-driven-development.** Proven through Slices 34.3 / 34.4 / 35 / 36. Haiku implementer (cheap, fast for mechanical TDD) + Sonnet spec-compliance reviewer + Sonnet code-quality reviewer per task; main thread reviews diff between tasks; final holistic Sonnet review before push.
- **Source-side `--filter-regex` description in commander is NOT modified by Slice 37.** (One plan-author unilateral decision worth surfacing.) Slice 36 updated source descriptions to reflect new mutex relationships; Slice 37's mutex matrix is unchanged (the new flag doesn't participate in the text-mode mutex), so the existing `--filter-regex` source description doesn't need a back-reference. Cross-references live in the new flag's own description (`'... Requires --filter-regex.'`) and in `docs/reference/commands.md` (Task 5.2). If user wants source parity with the Slice 36 precedent, this is a small fix-up in Task 4 review.
- **Windows execution abandoned for Slice 37.** 3 pre-existing Slice 34.4 tests fail on Windows; root cause not investigated this session (out of scope). Linux execution is clean.

## Open questions / blockers

- **3 pre-existing Slice 34.4 tests fail on Windows (NOT a Slice 37 blocker):** `recommendation.test.ts > 'Slice 34.4 AC-3'` / `'AC-4'` / `'AC-5'` — all use `--filter-converted-to` + the `await wf(ledgerPath, JSON.stringify(ledger))` → `await run(...)` pattern. Pass on Linux CI per the baseline (1113 green on `8919544`). Probable cause: Windows + pnpm + tempRepo path handling, or a file-system cache flush race between Node's `writeFile` and the subsequent `spawn(...)` of the built CLI. Worth a separate investigation slice IF Windows is ever targeted as a dev platform; currently Linux is the canonical dev environment and Windows is opportunistic. NOT a Slice 37 concern — the failing tests don't exercise any Slice 37 code path, and the Slice 37 source diff is surgical (parseRegexFlags + option + orphan-check + parse + one-line RegExp call + filterDims push, all in the regex-related sections).
- **v1.2 strategic-direction meta-question is still the unresolved meta** (same as the last 7+ handoffs). Surface AFTER Slice 37 ships. Remaining polish backlog after Slice 37 = **2 un-designed items**: `--filter-kind` on `intelligence audit`, graph viewer optimization. Each needs its own brainstorm session, OR a pivot to v1.2 Public release / v1.3 Architecture deepening.
- **Plan commit `87639de` + this handoff commit are both unpushed** before this handoff itself commits and pushes. The /handoff side-effect step commits the handoff and (per user's explicit direction in skill args) pushes both to `origin/main`. After push, `git log origin/main..HEAD` should be empty on the Windows machine.
- **Slice 36 follow-up still deferred (Important, non-blocking):** The Slice 36 plan doc's architecture-summary paragraph misdescribes `decision.ts`'s `--filter-rec` position. Doc-prose cleanup, not a code bug; Slice 36 implementation is correct. Tighten if the pattern recurs in Slice 37's plan review.
- **Slice 36 follow-up still deferred (Minor):** AC-exact-9 (composition test) on each command does not exhaustively verify sort ordering when multiple entries survive all filters; fine because Slice 35's own sort tests cover the engine. Mirror this stance in Slice 37 if applicable.
- **Pre-existing observation (Slice 26-era, not Slice 37):** `--offset` appears in empty-result `filterDims` but `--limit` does not — asymmetric. Out of Slice 37 scope; worth a follow-up issue if anyone hits the inconsistency.

## Environment

- CWD: `/home/thomas/projects/cadence` (this is the EXPECTED resume CWD; the /handoff was authored from Windows `C:/Users/Thomas/Documents/Projects/cadence`, but the next session is intentionally on Linux per user direction)
- OS/shell: Linux / bash (next session); MINGW64 / bash on Windows (this session)
- Node: 20 (per CLAUDE.md `Node >=20`; CI pins exactly 20 during dev-time). This session's Windows run used Node 22 — irrelevant since the work didn't ship.
- pnpm: 9.12.0 (pinned in `package.json`)
- Vitest: 2.1.9; `maxForks: 12`; 20s timeouts — do not re-add per-test timeout band-aids
- `@cadence/core` test count baseline: **1113** (post-Slice-36; verified earlier this session). Slice 37 target: **1132**.
- `@cadence/types` test count: 124 (unchanged — Slice 37 has no schema change)
- Repo visibility: PRIVATE (load-bearing for self-hosted-runner safety)
- `gh` CLI: configured and authenticated on the Linux box
- Self-hosted runner: `cadence-dev` (systemd service; labels self-hosted/Linux/X64) — required for ANY CI run on `main`
- Pre-push hook: `.githooks/pre-push` runs the full `pnpm turbo run lint typecheck test build` on pushes to `main`. NOT firing on feature-branch pushes. Local-gate equivalent is the same command.
- Tags: `v1.0.0`, `v1.1.0`, `v1.1.1` (all pushed). `v1.1.1` annotates Slices 34.3 / 34.4 / 35 / 36 as the polish patch since `v1.1.0`. No new tag for Slice 37 unless explicitly requested (polish slices don't auto-tag).
- Archived prior handoffs (gitignored, pattern `HANDOFF.md.resumed-*`): `HANDOFF.md.resumed-2026-05-25-1530`, `HANDOFF.md.resumed-2026-05-26-2055`, `HANDOFF.md.resumed-2026-05-27-1640`, `HANDOFF.md.resumed-2026-05-27-2257`, `HANDOFF.md.resumed-2026-05-28-0053`, `HANDOFF.md.resumed-2026-05-28-0356`, `HANDOFF.md.resumed-2026-05-28-1645` (this session's archive).
