# Handoff — Slice 34.3 + 34.4 shipped; v1.2 strategic direction is the next decision

> Generated by /handoff on 2026-05-27 22:50 UTC by Claude Opus 4.7 (1M context)

<!-- RESUME-KICKOFF-START -->
HANDOFF-VERSION: 1

You are resuming work that was paused by another LLM session.
The full handoff is in ./HANDOFF.md (this file). Before doing anything:

1. Read this entire file end-to-end. Do not skim.
2. Run these checks to verify the environment still matches:
   - `pwd` should be: /home/thomas/projects/cadence
   - `git rev-parse --abbrev-ref HEAD` should be: main
   - `git log -1 --oneline` should start with: c6eacf3
   If any differ, stop and tell the user before proceeding.
3. Pick up at the action described in "## Next step". Verify it with the
   command in that section's "Verify:" line.
4. Treat "## Key decisions" as settled — do not re-open them without the
   user's explicit OK.
5. If you hit anything in "## Open questions / blockers", ask the user
   rather than guessing.
<!-- RESUME-KICKOFF-END -->

## Goal

Decide the v1.2 strategic direction for CADENCE. The two locked-design Praxis polish slices (34.3 `--from-rec` and 34.4 `--filter-converted-to`) are both shipped this session and live on `origin/main`; the rec↔phase linkage operator surface is now symmetric in both directions. The meta-question from the prior handoff returns: continue with un-designed Praxis polish, pivot to the v1.2 Public-release milestone, or pivot to v1.3 Architecture deepening. Surface this as the first thing.

## Current state

- Branch: `main` (clean working tree; everything pushed to `origin/main`)
- Last commit: `c6eacf3 docs: document --filter-converted-to flag on recommendation list (Slice 34.4)`
- Uncommitted: none
- Background processes: **`actions.runner.manehorizons-cadence.cadence-dev` systemd service** (self-hosted runner) running on this machine. CI on any push to `main` goes through it. Check via `sudo /home/thomas/actions-runner/svc.sh status` or `gh api repos/manehorizons/cadence/actions/runners --jq '.runners[]'`. Don't `sudo svc.sh stop` it.
- CI status: green on `c6eacf3` (verify with `gh run list --branch main --limit 3`).

## What's done this session

- Resumed prior handoff (Slice 34.3 plan ready) via `/resume` autonomous mode; archived the source handoff to `HANDOFF.md.resumed-2026-05-27-1640` (gitignored).
- Executed Slice 34.3 (7-task plan, subagent-driven-development): `--from-rec <recId>` added to `cadence spec new` AND `cadence draft new`. Pre-flight rec validation, chained `runRecommendationTransition`, race-recovery hint on chained-convert failure. ~80 LoC across spec.ts + draft.ts + two new test files; 9 new tests.
- Caught one real bug during 34.3 code-quality review: copy-paste from spec.ts left the inserted blocks in draft.ts at 10-space indent vs surrounding 8-space try-body. Fixed inline; also restored the longer "why" comments from spec.ts to keep symmetry.
- Slice 34.3 commits: `76bf035 feat(core): spec/draft new --from-rec` + `1a6c89c docs: --from-rec flag on spec new + draft new` + `50aa6a4 chore: archive resumed handoff`. Pushed; CI green.
- Wrote Slice 34.4 implementation plan via `superpowers:writing-plans` (5 tasks; design was already locked in the upstream Slice 34 doc, no brainstorm needed). Committed as `c8f9460`.
- Executed Slice 34.4 (5-task plan, subagent-driven-development): `--filter-converted-to <phaseId>` added to `cadence recommendation list`. Filter slots between text/regex and reverse; implies `status=converted` via field equality; empty-result dim `converted-to="<phaseId>"`. ~5 LoC source + 5 new tests in the existing `recommendation.test.ts`.
- During Task 4 (docs) the implementer made a courtesy expansion: `recommendation list` had no options block in `docs/reference/commands.md` at all, so the new table documents all four filter flags (`--filter-status`, `--filter-text`, `--filter-regex`, `--filter-converted-to`) — small backfill, accepted.
- Slice 34.4 commits: `c8f9460 docs: plan` + `631a9d9 feat(core): --filter-converted-to` + `c6eacf3 docs: --filter-converted-to flag`. Pushed; CI green.
- Test count for `@cadence/core`: 1034 → 1043 (after 34.3) → 1048 (after 34.4). Full turbo gate 16/16 green at every checkpoint.

## Next step

**Action:** Open the v1.2 strategic-direction meta-question with the user via `AskUserQuestion`. Three options on the table (from the prior session's chain of handoffs): (a) v1.2 Public release — requires the visibility-flip + self-hosted-runner disable + matrix restore + npm publish kickoff; (b) v1.3 Architecture deepening — cherry-pick from `parking/roadmap-v1.3-arch-deepening @ a050083`; (c) continued Praxis polish — 5 un-designed items remain (`--sort-by`, `--filter-text-exact`, `--filter-regex-flags`, `--filter-kind` on audit, graph viewer optimization), each needs its own brainstorm session.

**Verify:** User picks a direction. Then either (a) read `parking/roadmap-v1.3-arch-deepening` for option b, (b) read the prior session's notes on v1.2 release prep for option a, or (c) invoke `superpowers:brainstorming` on the picked polish item for option c.

**If it fails:** If the user doesn't want any of those three, ask what they DO want — the polish backlog is provisional and the user explicitly flagged it as such.

## Key decisions

- **Self-hosted runner is DEV-TIME ONLY.** Never pair a self-hosted runner with a public repo. The v1.2 visibility-flip (if chosen) MUST disable the runner first and restore the full Node 20+22 × {Ubuntu, Windows, macOS} matrix. Inline comment in `ci.yml` documents this. Don't normalize the current self-hosted-only state.
- **`ci-success` aggregator kept** even with single test job — so the required-check name stays `ci-success` across dev-time → release-time matrix changes. Branch protection doesn't need retuning when the matrix returns.
- **Praxis convention: two-commit slices, plan doc gets its own `docs:` commit ahead of the feat.** Slice 34.3 shape: feat + docs (the plan was committed by an earlier session before the handoff). Slice 34.4 shape: plan-doc + feat + docs (because the plan was written this session). For the next slice, follow whichever ordering fits the session boundary — both shapes are precedent.
- **No shared `from-rec.ts` helper across spec.ts/draft.ts.** Anti-scope from the upstream Slice 34 design. ~12 LoC duplication is cheaper than a shim file. Factor when a third command needs `--from-rec`.
- **No `--filter-converted-to-any` boolean** and **no reverse on `assumption list` / `decision list`.** Out of scope per the Slice 34.4 design — assumptions/decisions don't have a `convertedToPhaseId` field, and `--filter-status converted` already covers the "any conversion" case.
- **Plan execution mode: subagent-driven-development.** Worked cleanly for both 34.3 (7 tasks) and 34.4 (5 tasks): fresh subagent per task, spec-compliance review + code-quality review per task, plus a final holistic review. The code-quality stage caught the 34.3 draft.ts indentation bug that the other stages missed — keep the two-stage review.
- **GitHub Actions billing reset is still pending.** User noted not ready to pay yet. Until the monthly free quota resets on the billing-cycle date, self-hosted is the path.

## Open questions / blockers

- **v1.2 strategic direction is the unresolved meta-question.** See the Next step above. Two slices that had locked designs are shipped; the polish backlog from here on requires either fresh brainstorm sessions OR a pivot.
- **Slice 34.5+ items are speculative** (per the upstream Slice 34 design § Slice 34.5+): `recommendation retarget`, `intelligence stats --converted-count`, phase-side reverse marker (`.cadence/phases/<id>/.praxis-source` — would be Approach B and require re-litigating Decision Log §5). Don't ship any of these without an operator-driven trigger.
- **Slice 34.3 spec coverage table has a cosmetic order mismatch** (says "rec check → loopPosition check → scaffold → convert" but source order is loopPosition → rec → scaffold → convert). Behavior is correct (both fail before any fs write); the plan's coverage table phrasing is slightly imprecise. Not worth fixing — it's a self-review note in a shipped plan doc.

## Environment

- CWD: `/home/thomas/projects/cadence`
- OS/shell: Linux / bash
- Node: 20 (per CLAUDE.md `Node >=20`; CI pins exactly 20 during dev-time)
- pnpm: 9.12.0 (pinned in `package.json`)
- Vitest: 2.1.9; `maxForks: 12`; 20s timeouts — do not re-add per-test timeout band-aids
- `@cadence/core` test count: 1048 (post-Slice-34.4 baseline)
- `@cadence/types` test count: 124
- Repo visibility: PRIVATE (load-bearing for self-hosted-runner safety)
- `gh` CLI: configured and authenticated
- Self-hosted runner: `cadence-dev` (systemd service; labels self-hosted/Linux/X64) — required for ANY CI run on `main`
- Pre-push hook: `.githooks/pre-push` runs the full `pnpm turbo run lint typecheck test build` on pushes to `main`. NOT firing on feature-branch pushes. Local-gate equivalent same command.
- Archived prior handoff: `HANDOFF.md.resumed-2026-05-27-1640` (gitignored; ~/.gitignore pattern `HANDOFF.md.resumed-*`).
