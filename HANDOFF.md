# Handoff — Slice 34.3 implementation plan ready; execute via subagent-driven-development

> Generated by /handoff on 2026-05-27 03:00 UTC by Claude Opus 4.7 (1M context)

<!-- RESUME-KICKOFF-START -->
HANDOFF-VERSION: 1

You are resuming work that was paused by another LLM session.
The full handoff is in ./HANDOFF.md (this file). Before doing anything:

1. Read this entire file end-to-end. Do not skim.
2. Run these checks to verify the environment still matches:
   - `pwd` should be: /home/thomas/projects/cadence
   - `git rev-parse --abbrev-ref HEAD` should be: main
   - `git log -1 --oneline` should start with: 425de0f
   If any differ, stop and tell the user before proceeding.
3. Pick up at the action described in "## Next step". Verify it with the
   command in that section's "Verify:" line.
4. Treat "## Key decisions" as settled — do not re-open them without the
   user's explicit OK.
5. If you hit anything in "## Open questions / blockers", ask the user
   rather than guessing.
<!-- RESUME-KICKOFF-END -->

## Goal

Execute the Slice 34.3 implementation plan at `docs/superpowers/plans/2026-05-26-slice-34.3-from-rec.md` to ship `--from-rec <recId>` on both `cadence spec new` and `cadence draft new`. The plan has 7 tasks (4 TDD red→green cycles + turbo gate + docs commit + final verification). Expected scope: ~80 LoC across two source files + two new test files + one docs file. Two commits at the end per Praxis convention: `feat(core):` then `docs:`.

This is part of the broader "Praxis polish" backlog the user picked after CI was restored to green. After 34.3 lands, Slice 34.4 (`recommendation list --filter-converted-to`) is the next-most-natural slice — also already designed in the upstream Slice 34 doc.

## Current state

- Branch: `main` (we're on main directly — the CI work merged in via PR #11; no feature branch yet for Slice 34.3)
- Last commit at handoff-write time: `425de0f docs: Slice 34.3 implementation plan (Praxis polish kickoff)`. The kickoff block expects `git log -1` to match that — after the handoff commit itself lands, HEAD will be one commit ahead (a benign mismatch /resume handles by continuing). Two commits before that: `556e777 ci: switch to self-hosted Linux runner during development`.
- Uncommitted: none after the handoff commit
- Background processes: **`actions.runner.manehorizons-cadence.cadence-dev` systemd service** is running on this machine (the self-hosted runner). Don't `sudo svc.sh stop` it — CI on any push to main goes through this runner. Check status via `sudo /home/thomas/actions-runner/svc.sh status` or `gh api repos/manehorizons/cadence/actions/runners --jq '.runners[]'`.

## What's done this session

- Resumed prior handoff `HANDOFF.md.resumed-2026-05-26-2055` (full session resume + Phase 7 rename).
- Walked user through self-hosted runner install: `cadence-dev` registered, ran first as `./run.sh` (foreground), then reinstalled as systemd service after the foreground session ended mid-flight.
- PR #10 (degenerate verification PR, draft) opened then closed-without-merge: the 3-way merge of `ci.yml` gave main's revert priority over the unchanged head workflow, so the PR's CI ran main's GitHub-hosted matrix workflow (not the head's self-hosted workflow). The trap predicted in the prior handoff's Open question §1.
- Executed merge-back option (b): cherry-picked `8ff749e` onto current main `ebc65dd` as `030dede`. Force-pushed `ci/self-hosted-runner` with the new SHA (force-push allowed on feature branch per CLAUDE.md; not main).
- PR #11 opened (real, non-degenerate). Both `test` and `ci-success` jobs ran on the self-hosted runner. CI green.
- Rebase-merged PR #11 via `gh pr merge 11 --rebase --delete-branch`. Local main auto-switched to merged HEAD `556e777`. Remote `ci/self-hosted-runner` deleted; pruned local tracking ref.
- Post-merge push of merge commit to main triggered fresh CI on `556e777` — also green. **10-day CI red streak ended.**
- User picked "Praxis polish" from handoff Open question §3 (v1.2 strategic direction).
- Scoped the polish backlog: 7 items total, 2 with locked upstream designs (34.3 + 34.4), 5 un-designed (`--sort-by`, `--filter-text-exact`, `--filter-regex-flags`, `--filter-kind` on audit, graph viewer optimization).
- Used `superpowers:brainstorming` skill to converge: Slice 34.3 picked as entry slice.
- Resolved one open implementation choice from upstream design: `--from-rec` lands on BOTH `spec new` AND `draft new` (user-picked; alternative single-surface options rejected).
- Invoked `superpowers:writing-plans` skill; wrote `docs/superpowers/plans/2026-05-26-slice-34.3-from-rec.md` (7 tasks; ~700 lines; TDD red→green per command with complete code in every step).
- Self-review of plan: spec-coverage table inline at the bottom; placeholder scan clean; type consistency verified (`opts.fromRec: string | undefined` with `!== undefined` guards consistent across both command files).
- User chose `/handoff and push` instead of executing the plan in-session.

## Next step

**Action:** Execute the plan at `docs/superpowers/plans/2026-05-26-slice-34.3-from-rec.md` via `superpowers:subagent-driven-development` (the plan's recommended sub-skill). Start with Task 1, Step 1.1 (write the failing `spec new --from-rec` happy-path test). Fresh subagent per task; review between tasks before dispatching the next.

**Verify:** After all 7 tasks complete:
- `pnpm turbo run lint typecheck test build` shows 16/16 green
- `git log --oneline -3` shows two new commits (`feat(core): spec/draft new --from-rec one-shot rec→phase promotion (Slice 34.3)` then `docs: document --from-rec flag on spec new + draft new (Slice 34.3)`) on top of the handoff commit
- `pnpm --filter @cadence/core test 2>&1 | grep -E "Tests.*passed"` shows total grew by ~9 (5 spec-new-from-rec + 4 draft-new-from-rec) from the 1034 baseline

**If it fails:**
- If a TDD red test unexpectedly passes, check whether commander actually parsed the flag (run `node packages/core/dist/cli/index.js spec new --help` to see registered options).
- If typecheck fails on `opts.fromRec`, verify the `!== undefined` guard pattern — `exactOptionalPropertyTypes` means commander gives `undefined` for unset options and the truthy-check (`if (opts.fromRec)`) won't narrow the type. Use `if (opts.fromRec !== undefined)`.
- If the SPEC-seeded compose test (Task 4.3) fails because the parser rejects the test fixture's SPEC body, the inline string-replace chain might have produced an unparseable SPEC — adjust the seed values until `parseSpecMd` accepts it (the goal is "1 AC parses", not a specific AC body).
- If the scaffold-then-convert-fail race test is needed but flaky, the race is genuinely untestable from a single CLI process — verify the source code path exists by reading the diff, skip the test rather than chasing flakiness.

## Key decisions

- **Self-hosted runner is DEV-TIME ONLY.** The workflow's inline comment says: "the full multi-OS × Node 20+22 matrix returns at the v1.2 public-release milestone — restore the previous strategy block before tagging." Also: NEVER pair a self-hosted runner with a public repo. The v1.2 visibility-flip MUST disable the runner first. Don't normalize the current self-hosted-only state.
- **`ci-success` aggregator kept** even with single test job — so the required-check name stays `ci-success` across the dev-time → release-time matrix change. Don't have to retune branch protection when the matrix returns.
- **Slice 34.3 implements `--from-rec` on BOTH `spec new` AND `draft new`.** User-picked. Single-surface alternatives rejected because SPEC is opt-in (most operators use DRAFT) and the symmetry is small (~12 LoC duplicated; no shared helper file per the design's anti-scope).
- **Plan execution mode: subagent-driven-development (the plan's recommended sub-skill).** Each task in the plan is self-contained — fresh subagent per task with review-between is the right shape. If the resuming LLM wants inline-execution instead, the plan is structured to support that too (use `superpowers:executing-plans` with batch checkpoints).
- **Praxis convention: two commits per slice, no per-slice design doc.** Design for Slice 34.3 lives upstream in `docs/superpowers/specs/2026-05-25-cadence-rec-phase-linkage-design.md` § Follow-On. Slice 34.3 ships `feat(core):` + `docs:` only; no third design-commit (the upstream design is the design).
- **Race-condition handling: print + exit non-zero, don't try to auto-clean.** If pre-flight rec check passes, scaffold succeeds, then convert fails (rec mutated between pre-flight and convert), the scaffold stays on disk. The operator sees stderr explaining the situation and a one-liner to retry with `recommendation convert`. Don't try to roll back the scaffold automatically — that creates worse failure modes.
- **No shared `from-rec.ts` helper.** Anti-scope in the design recap. Duplicate ~12 LoC across `spec.ts` and `draft.ts`. If a third command ever needs `--from-rec`, factor then.

## Open questions / blockers

- **v1.2 strategic direction is still the unresolved meta-question.** Three options from the original prior handoff: v1.2 Public release (gated on repo-visibility decision), v1.3 Architecture deepening (cherry-pick from `parking/roadmap-v1.3-arch-deepening @ a050083`), or Praxis polish (chosen for now). The "Praxis polish" pick is provisional — after 34.3 (and likely 34.4) ship, the meta-question returns. Surface as `AskUserQuestion` when the polish backlog feels exhausted (or when the user explicitly asks "what's next").
- **Slice 34.4 is the next-most-natural slice after 34.3.** Design also locked in upstream Slice 34 doc (~40 LoC; reverse-lookup filter `recommendation list --filter-converted-to <phaseId>`). The forward direction (34.3) and reverse direction (34.4) pair naturally; shipping both completes the rec↔phase linkage operator surface. After 34.4, the un-designed polish items each need their own brainstorm session.
- **GitHub Actions billing reset:** user noted not ready to pay yet. If/when they want GitHub-hosted CI back without paying, the monthly free quota resets on the calendar billing-cycle date. Until then, self-hosted is the path.
- **The plan doc and HANDOFF.md will both be pushed to main.** Pre-push hook runs `pnpm turbo run lint typecheck test build` on pushes to main. The plan doc + handoff are pure docs — turbo will report cached/no-op on all gates. If turbo fails, something else is wrong with the working tree.

## Environment

- CWD: `/home/thomas/projects/cadence`
- OS/shell: Linux / bash
- Node: 20 (per CLAUDE.md "Node >=20" minimum; the CI workflow pins exactly 20 during dev-time CI)
- pnpm: 9.12.0 (pinned in `package.json`)
- Vitest: 2.1.9 (per `vitest.shared.ts`; `maxForks: 12`; 20s timeouts — do not re-add per-test timeout band-aids)
- `@cadence/core` test count: 1034 (baseline before Slice 34.3 begins)
- `@cadence/types` test count: 124
- Repo visibility: PRIVATE (load-bearing for self-hosted-runner safety)
- `gh` CLI: configured and authenticated
- Self-hosted runner: `cadence-dev` (systemd service; ID 21; labels self-hosted/Linux/X64) — required for ANY CI run on main. Check before pushing if you suspect issues.
- Pre-push hook: `.githooks/pre-push` runs full turbo gate on pushes to `main`. NOT firing on feature-branch pushes. Local-gate equivalent: `pnpm turbo run lint typecheck test build`.
