# Handoff — Slice 36 (--filter-text-exact) shipped, v1.1.1 tagged; v1.2 strategic direction is still next

> Generated by /handoff on 2026-05-28 03:48 UTC by Claude Opus 4.7 (1M context)

<!-- RESUME-KICKOFF-START -->
HANDOFF-VERSION: 1

You are resuming work that was paused by another LLM session.
The full handoff is in ./HANDOFF.md (this file). Before doing anything:

1. Read this entire file end-to-end. Do not skim.
2. Run these checks to verify the environment still matches:
   - `pwd` should be: /home/thomas/projects/cadence
   - `git rev-parse --abbrev-ref HEAD` should be: main
   - `git log -1 --oneline` should start with: 9a16f90
   If any differ, stop and tell the user before proceeding.
3. Pick up at the action described in "## Next step". Verify it with the
   command in that section's "Verify:" line.
4. Treat "## Key decisions" as settled — do not re-open them without the
   user's explicit OK.
5. If you hit anything in "## Open questions / blockers", ask the user
   rather than guessing.
<!-- RESUME-KICKOFF-END -->

## Goal

Pick what's next after Slice 36 (`--filter-text-exact` on the three list commands) shipped cleanly to `origin/main` and v1.1.1 was tagged (bundling Slices 34.3 / 34.4 / 35 / 36 as the post-Praxis-v1.1.0 polish patch). The same v1.2 strategic-direction meta-question that the prior five handoffs raised is still open: continue Praxis polish (now **3 un-designed items left**), pivot to v1.2 Public release, or pivot to v1.3 Architecture deepening. The polish backlog requires fresh brainstorm sessions for each item; the locked-design backlog is exhausted. Surface this as the first thing.

## Current state

- Branch: `main` (clean working tree; everything pushed to `origin/main`)
- Last commit: `9a16f90 docs: document --filter-text-exact on list commands + reconcile Slice-33 follow-ref (Slice 36)`
- Uncommitted: none (HANDOFF.md will be added by this commit)
- Tags: `v1.1.1` annotated tag created at `9a16f90` bundling Slices 34.3 / 34.4 / 35 / 36. **Tag NOT yet pushed.**
- Background processes: **`actions.runner.manehorizons-cadence.cadence-dev` systemd service** (self-hosted runner) running on this machine. CI on any push to `main` goes through it. Check via `sudo /home/thomas/actions-runner/svc.sh status` or `gh api repos/manehorizons/cadence/actions/runners --jq '.runners[]'`. Don't `sudo svc.sh stop` it.
- CI status: green on `9a16f90` (verify with `gh run list --branch main --limit 3`).

## What's done this session

- Resumed prior handoff (`HANDOFF.md` @ 2026-05-28 00:25 UTC) via `/resume` autonomous mode; archived the source handoff to `HANDOFF.md.resumed-2026-05-28-0053` (gitignored) and committed its tracked deletion alongside the design commit (matching the `33cebb1` precedent).
- Surfaced the v1.2 strategic-direction meta-question via `AskUserQuestion`; user picked **continued Praxis polish** then chose **`--filter-text-exact`** as the specific polish item.
- Brainstormed `--filter-text-exact` design via `superpowers:brainstorming`: five Q&A resolved semantics (whole-field equality, case-insensitive), scope (same as `--filter-text` per subject — ANY-of), flag shape (separate `--filter-text-exact`, not colon-suffix grammar), mutex rule (all three text-mode filters pairwise mutex), single-value semantics, and empty-literal rejection.
- Wrote and committed the design spec at `docs/superpowers/specs/2026-05-27-cadence-list-filter-text-exact-design.md` (`1b0fb34`). Self-review found one path issue (`packages/core/tests/cli/commands/` was wrong; actual is `packages/core/tests/cli/`); fixed inline before commit.
- Wrote and committed the implementation plan at `docs/superpowers/plans/2026-05-27-slice-36-filter-text-exact.md` (`af87f1a`). 6 tasks, ~1590 lines, paired-down structure mirroring Slice 35. Self-review caught a non-existent helper reference (`listRecIds`) and corrected to the actual `addRecommendation` store helper before commit.
- Executed Slice 36 via `superpowers:subagent-driven-development`. Tasks 1-3 each dispatched a Haiku implementer subagent + a Sonnet spec-compliance reviewer + a Sonnet code-quality reviewer. Tasks 4-6 ran inline (coordinator work — gate, bundled commit, docs, push). Final holistic Sonnet review approved ship-as-is.
- Shipped feat at `23740e0` (3 commands, 6 source/test files, +711 LoC, +32 tests) and docs at `9a16f90` (commands.md: 3 row inserts + 6 mutex-note refreshes on existing `--filter-text` / `--filter-regex` rows).
- `@cadence/core` test count: 1081 → **1113** (post-Slice-36 baseline). Full turbo gate 16/16 green at every checkpoint. CI on self-hosted runner green in 50s.
- Created annotated tag `v1.1.1` at `9a16f90` bundling Slices 34.3 / 34.4 / 35 / 36 with a slice manifest in the tag body. **Tag has NOT been pushed yet.**
- Final holistic review flagged one process note: the design commit `1b0fb34` carries a `Co-Authored-By` trailer that this project doesn't use on feat/docs commits. Decided not to amend (1 cosmetic line on a docs-only commit; rewriting 4 commits would invalidate the SHA reference in the plan commit body and add unnecessary risk).

## Next step

**Action:** Run `git push origin main --tags` to publish the `v1.1.1` annotated tag and the upcoming chore-handoff commit. Then open the v1.2 strategic-direction meta-question with the user via `AskUserQuestion`. The same three options as the prior session: (a) v1.2 Public release — visibility-flip + self-hosted-runner disable + matrix restore + npm publish kickoff; (b) v1.3 Architecture deepening — cherry-pick from `parking/roadmap-v1.3-arch-deepening @ a050083`; (c) continued Praxis polish — **3 un-designed items remain**: `--filter-regex-flags`, `--filter-kind` on `intelligence audit`, graph viewer optimization. Each polish item needs its own brainstorm session.

**Verify:** `git push origin main --tags` completes; `gh release list` shows v1.1.1 (or `git ls-remote --tags origin v1.1.1` confirms the tag is on origin). Then user picks a direction. Then either (a) read the prior session notes on v1.2 release prep; (b) `git log --oneline -10 parking/roadmap-v1.3-arch-deepening` to scope the cherry-pick set; or (c) invoke `superpowers:brainstorming` on the picked polish item.

**If it fails:** If the push fails on the tag, the pre-push hook may be objecting — but the working tree should be clean post-handoff-commit, so the gate should pass. If the user doesn't want any of the three direction options, ask what they DO want — the polish backlog is provisional and the user has flagged it as such across multiple sessions.

## Key decisions

- **Slice 36 design is locked and shipped.** Separate `--filter-text-exact <str>` flag (not colon-suffix grammar, not mode flag); case-insensitive whole-field equality; multi-field ANY-of scope matches `--filter-text` per subject (rec=title|summary, asn=text, dec=title|rationale); all three text-mode filters pairwise mutex; single value (commander last-wins); empty literal refuses with clear error; no trim; no shared `filter-exact.ts` helper (~10 LoC × 3 = ~30 LoC inline per Decision Log §8); `milestone list` and `intelligence audit` not touched; no `@cadence/types` schema change.
- **v1.1.1 tag created as a patch-level bundle of the 4 post-v1.1.0 polish slices.** Chosen over v1.2.0 (which conceptually reserves the strategic-direction decision) and a slice-specific lightweight tag (no precedent in this repo). Annotated tag with full slice manifest in the body.
- **Plan execution mode: subagent-driven-development.** Worked cleanly for Slices 34.3, 34.4, 35, and now 36. Haiku implementer + Sonnet two-stage review per task + final holistic Sonnet review is the proven shape. Code-quality stage continues to catch things spec-compliance misses (in 36 it noted that the plan's architecture summary paragraph mis-describes dec's `--filter-rec` position — doc-quality flag, not a code bug).
- **Docs precedent maintained: refresh mutex notes on existing flags when adding a new mutex partner.** Slice 36 updated 6 existing rows (3 cmds × `--filter-text` and `--filter-regex`) to enumerate all three text-mode flags pairwise. Follow this pattern if a fourth text-mode flag is ever added.
- **GitHub Actions billing reset is still pending.** Until the monthly free quota resets on the billing-cycle date, self-hosted is the path. The runner is DEV-TIME ONLY per the prior handoff's locked decision — never pair self-hosted with a public repo.
- **`ci-success` aggregator kept** even with single test job so the required-check name stays stable across dev-time → release-time matrix changes.

## Open questions / blockers

- **v1.2 strategic direction is the unresolved meta-question** (same as the last five handoffs). See Next step above. Four locked-design polish slices (34.3, 34.4, 35, 36) are now all shipped; the remaining polish requires fresh brainstorms OR a pivot.
- **`v1.1.1` tag is local-only** until the next push. The /handoff chore commit + `git push origin main --tags` should ship both atomically.
- **Slice 36 process note (cosmetic):** the design commit `1b0fb34` carries a `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer that this project doesn't use on feat/docs commits. Decided not to amend during shipping (4-commit rewrite risk > cosmetic gain). Future docs/feat commits in this session and beyond should suppress the trailer — verify by inspecting commit bodies before push.
- **Slice 36 follow-up (Important, non-blocking from final review):** The plan doc's architecture summary paragraph (line 7) says the apply block is placed "between text-mode filters and the subject-specific filter (--filter-rec for asn / dec)." For `decision.ts`, `--filter-rec` is BEFORE the text-mode filters, not after — so that sentence misdescribes dec's pipeline. The actual implementation correctly follows Step 3.5's specific instruction ("between --filter-regex and --sort-by"). Doc-prose cleanup, not a code bug; tighten if the pattern recurs in a future slice.
- **Slice 36 follow-up (Minor, deferred):** AC-exact-9 (composition test) for each command covers `--filter-text-exact + --filter-status + --sort-by` but not exhaustive sort ordering when multiple entries survive all filters. Coverage of sort engine lives in Slice 35's own tests; this is not a gap, just a note.
- **Pre-existing observation (Slice 26-era, not Slice 36):** `--offset` appears in empty-result `filterDims` but `--limit` does not — asymmetric. Out of Slice 36 scope; worth a follow-up issue if anyone hits the inconsistency.

## Environment

- CWD: `/home/thomas/projects/cadence`
- OS/shell: Linux / bash
- Node: 20 (per CLAUDE.md `Node >=20`; CI pins exactly 20 during dev-time)
- pnpm: 9.12.0 (pinned in `package.json`)
- Vitest: 2.1.9; `maxForks: 12`; 20s timeouts — do not re-add per-test timeout band-aids
- `@cadence/core` test count: 1113 (post-Slice-36 baseline; was 1081 post-Slice-35)
- `@cadence/types` test count: 124
- Repo visibility: PRIVATE (load-bearing for self-hosted-runner safety)
- `gh` CLI: configured and authenticated
- Self-hosted runner: `cadence-dev` (systemd service; labels self-hosted/Linux/X64) — required for ANY CI run on `main`
- Pre-push hook: `.githooks/pre-push` runs the full `pnpm turbo run lint typecheck test build` on pushes to `main`. NOT firing on feature-branch pushes. Local-gate equivalent same command.
- Tags: `v1.0.0`, `v1.1.0`, `v1.1.1` (local-only until pushed). v1.1.1 annotates Slices 34.3 / 34.4 / 35 / 36 as the polish patch since v1.1.0 (Praxis main milestone).
- Archived prior handoffs: `HANDOFF.md.resumed-2026-05-25-1530`, `HANDOFF.md.resumed-2026-05-26-2055`, `HANDOFF.md.resumed-2026-05-27-1640`, `HANDOFF.md.resumed-2026-05-27-2257`, `HANDOFF.md.resumed-2026-05-28-0053` (gitignored; pattern `HANDOFF.md.resumed-*`).
