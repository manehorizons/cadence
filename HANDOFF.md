# Handoff — Praxis Slices 29–33 shipped; Slice 34 (--sort-by) recommended next

> Generated by /handoff on 2026-05-25 20:53 UTC by Claude Opus 4.7 (1M context)

<!-- RESUME-KICKOFF-START -->
HANDOFF-VERSION: 1

You are resuming work that was paused by another LLM session.
The full handoff is in ./HANDOFF.md (this file). Before doing anything:

1. Read this entire file end-to-end. Do not skim.
2. Run these checks to verify the environment still matches:
   - `pwd` should be: /home/thomas/projects/cadence
   - `git rev-parse --abbrev-ref HEAD` should be: praxis-intelligence-ledger
   - `git log -1 --oneline` should start with: 30ae522
   If any differ, stop and tell the user before proceeding.
3. Pick up at the action described in "## Next step". Verify it with the
   command in that section's "Verify:" line.
4. Treat "## Key decisions" as settled — do not re-open them without the
   user's explicit OK.
5. If you hit anything in "## Open questions / blockers", ask the user
   rather than guessing.
<!-- RESUME-KICKOFF-END -->

## Goal

Continue shipping Praxis (strategic-intelligence layer) slices on `praxis-intelligence-ledger`. This session completed the `supersededBy` story arc (write → view → audit → inverse-link) and pivoted to list-shaping. Five slices delivered (29–33). PR #9 stays draft per established convention; CADENCE public release remains held pending Praxis integration completion. Next: continue picking from the remaining handoff candidates, or wrap Praxis if the operator decides the slice diet is sufficient.

## Current state

- Branch: `praxis-intelligence-ledger`, synced 0/0 with origin
- Last commit: `30ae522 — docs: document --filter-regex + reconcile Slice-25/27 follow-refs (Slice 33)`
- Uncommitted: AGENTS.md, CLAUDE.md, docs/agents/ — **all untracked from session start, NOT created this session**. Pre-existing. Status unchanged from prior handoff.
- Background processes: none

## What's done this session

- Applied the 2026-05-25 architecture review (`/tmp/architecture-review-20260525-103233.html`): drafted v1.3.0 Architecture Deepening milestone in `.cadence/ROADMAP.md` (11 phases: 39.1–39.7 gate extractions + 40.1 verifier factory + 41.1 backend.commit + 42.1 emitUnconverged + 43.1 boundary gate), and refreshed `.cadence/MILESTONES.md` from stale-at-v1.0.0 to current (v1.1, v1.2 shipped + v1.3, v1.4 planned).
- Recognized the roadmap/milestones edits don't belong on the Praxis branch → parked them via `git checkout -b parking/roadmap-v1.3-arch-deepening; commit; git checkout praxis-intelligence-ledger`. Parking branch commit: `a050083`. Cherry-pick onto a fresh branch off main when Praxis merges.
- Slice 29 (decision graph viewer): 3 commits `bac7ca3` / `7ce2933` / `f6d3116`. New `cadence decision graph <id>` subcommand; two-section ASCII output (`## Supersedes` indented bullets + `## Superseded by` arrow chain); `--format json` envelope with nested ancestors + flat descendants. +26 tests (10 builder + 10 renderer + 6 CLI). Used parallel subagents for builder + renderer (disjoint file ownership).
- Slice 30 (intelligence audit `stale-supersededby` dim): 3 commits `c3c466f` / `49b56d2` / `e031e36`. New finding kind detects decisions whose `supersededBy` references a missing id; rendered as `## Stale supersededBy Refs (N)` section before `## Remediation`; remediation bullet points at `reactivate` as clear-path. +10 tests.
- Slice 31 (`Decision.supersedes` derived inverse-link): 3 commits `a088751` / `8e2a6ea` / `a9f6318`. New `supersedes: string[]` field with `default([])` (Slice 11 pattern, NOT Slice 28's exact-optional); `deriveDecisionInverseLinks` helper wired into add/transition/reconcile; `decision show` surfaces the bullet. `store.ts` byte-equal to Slice 28 preserved. +18 tests (17 core + 1 types).
- Slice 32 (`--include-untied` on decision list): 3 commits `2337fd7` / `e621ce3` / `02f7016`. Predicate composition: `--filter-rec X --include-untied` returns `rec=X OR untied`; no-op alone; `untied=incl` empty-result dim. Decision-only (assumption/evidence schemas require recommendationId). +6 tests.
- Slice 33 (`--filter-regex` on all 3 list commands): 3 commits `b5b89c0` / `d535420` / `30ae522`. Multi-field regex match (mirrors `--filter-text` scope); mutually exclusive with `--filter-text`; invalid pattern refuses. **Mid-slice pivot**: original design claimed inline `(?i)foo` would work; Node 20 / V8 supports neither PCRE-style `(?i)foo` nor TC39 `(?i:foo)` (Stage 3). Confirmed `new RegExp('(?i)cycle')` → "Invalid group". Design + impl corrected to always-case-sensitive with character-class workaround (`[Cc]ycle`); `--filter-regex-flags` named as follow-on. +8 tests.
- Cumulative session test delta: `@cadence/core` 927 → 994 (+67); `@cadence/types` 121 → 122 (+1). Full turbo gate green at HEAD.

## Next step

**Action:** Pick the next Praxis slice. **Recommended: Slice 34 = `--sort-by <field>` stable sort on all three list commands.** It's the biggest remaining list-shaping item (Slice 27 follow-on) and completes the list-shaping arc (pagination via Slice 24/26/27 + filter via Slice 25/32/33 + sort would close it). Design questions to surface in brainstorming: which fields are sortable per subject (rec/assumption/decision have different sort-relevant fields), multi-key syntax (`priority,createdAt`?), direction modifier (`-priority` for descending?), default direction (ascending), tie-breaker (id?).

Alternative candidates if `--sort-by` feels too big or operator wants a different theme:
- **`--filter-text-exact`** (Slice 33 follow-on; tiny — case-sensitive substring without regex compile)
- **`--filter-regex-flags <flags>`** (Slice 33 follow-on; closes the case-insensitive gap that Slice 33's mid-slice pivot revealed)
- **`--filter-kind <kind>` on `intelligence audit`** (Slice 30 follow-on; tiny — single filter flag)
- **Slice-29 graph viewer optimization** (Slice 31 follow-on; pure perf, no behavior change — swap `walkAncestorTree`'s inverse-lookup for direct `supersedes[]` access)
- **Bulk transitions** (`cadence assumption validate --all-rec <recId>`; theme pivot from list-shaping to workflow)
- **Rec↔phase linkage** (biggest scope; needs upstream design first — separate slice for the design before any impl slice)

**Verify:** Before starting Slice 34, confirm the gate is still green:
```
pnpm turbo run lint typecheck test build  # → 16/16
pnpm --filter @cadence/core test           # → 994 tests
git log -1 --oneline                        # → 30ae522 docs: …
```

**If it fails:** The `milestone.test.ts` transient flake under heavy turbo pool load is a known pre-existing condition (documented in prior handoff carry-forward gotchas). Re-run cleared it during this session. If a non-flake failure appears, bisect against `30ae522` to identify the regression.

## Key decisions

- **All slices land on `praxis-intelligence-ledger`; PR #9 stays draft.** No merge to `main` this session. Tag pushes / PR merge / PR undraft remain out of scope for `/handoff`; need explicit user approval.
- **Per-slice three-commit Praxis convention preserved** (design → impl → docs-reconcile). Slice 33's mid-slice design correction shipped INSIDE the feat commit rather than amending the design commit — preserved history clarity.
- **Architecture review v1.3 roadmap is parked on a separate branch** (`parking/roadmap-v1.3-arch-deepening`, commit `a050083`). Cherry-pick onto a fresh branch off main when Praxis merges — DO NOT bring it onto the Praxis branch.
- **`store.ts` stays byte-equal to its Slice-28 state** across Slices 29, 30, 31. Slice 29 deliberately did NOT export `walkSupersededByChain` (the helper's `forbid`-parameter contract is shaped for cycle-refusal, not enumerate-and-emit; Slice 29 uses an inline forward walk that emits cycle and missing-id markers). Slice 31 only EXTENDS `store.ts` additively with `deriveDecisionInverseLinks`.
- **`supersedes: string[]` uses always-present `default([])` (Slice 11 pattern), NOT Slice 28's exact-optional.** Different cardinality (collection vs singleton) → different convention. Decision Log #8 in Slice 31's design.
- **`cycle: true` is exact-optional throughout Slice 29/31** — set only when truncated, never `false`. Consumers check `if (n.cycle)`.
- **Slice 29 graph viewer NOT optimized to use Slice 31's `supersedes[]`.** Current `walkAncestorTree` inverse-lookup works on any ledger including ones where `supersedes` arrays drifted; optimization would couple correctness to field consistency. Deferred to Slice 31 follow-on IFF performance matters.
- **`docs/reference/commands.md` deliberately UNCHANGED across all 5 slices** per Slice-28 precedent (the `decision` section has been drifting since Slice 13 — `add`/`list` listed but missing `show`/`supersede`/`rescind`/`reactivate`/`graph`). CHANGELOG carries the operator-facing news per-slice; commands.md reconciliation is parked as a separate concern.
- **CLI-reference drift guard UNCHANGED across all 5 slices** (graph is a sub-subcommand of `decision`; --include-untied and --filter-regex are options not commands; stale-supersededby is a new finding kind under existing `intelligence audit`).
- **Slice-17 AC-6 byte-equality invariant preserved by Slice 31** by constructing new decisions in schema-declaration property order (so `addIntelligenceDecision` writes match what `runIntelligenceReconcile` produces after Zod parse).
- **Slice 33's case-insensitive limitation is permanent for now.** Node 20.20 + V8 doesn't support `(?i)foo` or `(?i:foo)`. Operators use `[Cc]ycle` char-class workaround. `--filter-regex-flags <flags>` named as the clean follow-on slice.
- **Soft hint "superseded without `--by`" deliberately NOT a Slice-30 audit finding.** Would contradict Slice-28 Decision Log #1's optional-by-design `--by`. Deferred indefinitely unless the contradiction is reconciled.

## Open questions / blockers

- **Untracked files predate this session and remain unhandled.** `AGENTS.md`, `CLAUDE.md`, `docs/agents/` were already untracked when this session started (mentioned in prior handoff implicitly via "clean tree" — they may have appeared between sessions). Unclear what they are, whether they should be tracked, gitignored, or deleted. Recommend the operator inspect and decide before next session's commits.
- **Parking branch `parking/roadmap-v1.3-arch-deepening` (commit `a050083`) is fire-and-forget but needs eventual integration.** When Praxis merges to main, cherry-pick `a050083` onto a fresh branch off updated main, open a PR for v1.3 Architecture Deepening, then delete the parking branch.
- **Two named v1.2 tracks remain unstarted:** (1) **Public release** — gated on the repo-visibility decision (provenance requires public source repo, which would expose all `.cadence/` history); (2) **Rec↔phase linkage** — biggest remaining scope per handoff; needs upstream promotion-tracking design BEFORE any impl slice.
- **No `--filter-rec` on decision list when paired with `--include-untied` returns no tied entries** — current behavior (Slice 32) is: if rec-X has zero tied decisions but untied exists, result is just the untied set. Tested in Slice 32 AC-7. Operator can verify this matches their mental model.

## Environment

- CWD: `/home/thomas/projects/cadence`
- OS/shell: Linux / bash
- Node: 20.20.2 (load-bearing — Slice 33 hit V8 regex inline-modifier limitation; any Node upgrade should re-test Slice 33 ACs and potentially close the `--filter-regex-flags` follow-on by adopting inline modifiers if V8 ships them)
- pnpm: 9.12.0 (pinned in `package.json`)
- Vitest: 2.1.9 (per vitest.shared.ts; `maxForks: 12`; 20s timeouts)
- `@cadence/core` test count: 994 (up 67 this session)
- `@cadence/types` test count: 122 (up 1 this session)
